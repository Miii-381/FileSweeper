# VideoSweeper TODO

> 状态约定：`[x]` 已完成并有复验记录；`[~]` 已实现、待真实桌面验收；`[ ]` 未开始。

## 已知缺陷

- [~] 播放器已改为显式播放意图与流请求代次控制：手动暂停会阻止直连/转码晚到地址、回退和 Seek 自动恢复；仍待真实桌面覆盖自动播放开关与多编码矩阵。
- [~] 左栏目录树已监听全部展开节点并按 300ms 合并刷新，失效子树会清理缓存与展开状态；仍待 Explorer 与应用内新建、删除、重命名、移动桌面矩阵。
- [~] 左栏目录树已支持子目录“移到回收站”：根目录隐藏入口，成功后清理工作区、收藏与展开状态，失败保留 UI；仍待 Windows Shell、占用和权限错误矩阵。

## v0.2 收尾验收

- [x] 原文件优先播放；失败或视频尺寸为 0 时回退 FFmpeg。`mp4v` 音频可播、无图像样本已复验。
- [x] Windows Shell 文件拖出（COPY）已在资源管理器复验。
- [~] 完整重启 Tauri 后验证文件监听：外部新增、删除、重命名和拖入复制应在 300ms 合并刷新，日志不再出现 `Command watch not found`。
- [~] 回归全窗口边缘自动滚动框选：网格、列表、高 DPI、标题栏/侧栏附近、窗口底部和停止边界。
- [~] 回归主题化工作区菜单：文件项、列表表头、空白区、无视频工作区的定位、关闭、复制、回收站和刷新。
- [~] 回归转码流的快速切换、连续定位和更多真实编码；常规 H.264 需保持原文件直连。

## v0.3：可验证的模块化

- [x] 按工作区联合记忆排序与视频焦点，并由同一设置开关控制保存和恢复。
- [x] 建立 Rust 特征测试和统一 `npm run check` 质量入口。
- [x] 拆分 Rust 配置、工作区、缩略图、媒体、文件操作、Shell 与菜单职责。
- [x] 拆分前端共享类型、工具、缩略图、播放器和目录树组件。
- [x] 清理未使用依赖并统一版本为 `0.3.0`。
- [ ] 完成 v0.2 剩余真实桌面回归。

## v0.4-v1.0

详细范围、依赖顺序和验收标准见 [`ROADMAP.md`](ROADMAP.md)。

- [x] v0.4：配置由后端单一状态串行更新并原子替换；设置与焦点/排序分别写入 `config.json`、`workspace-state.json`，覆盖旧格式迁移、独立损坏恢复和并发隔离测试。
- [x] v0.4：落实 `.nomedia` 祖先语义，拆分目录树枚举与工作区视频扫描，移除 WebView 文件监听权限。
- [x] v0.4：工作区扫描采用代次控制；目录断连时停止预览并清空列表，后台探测恢复后重新扫描。
- [x] v0.4：删除焦点视频前先终止并确认其 FFmpeg 转码进程退出，超时则中止删除。
- [~] v0.4：完成可移动盘、映射盘/网络路径断连恢复，以及转码中删除的真实桌面验收。
- [~] v0.5：文件任务引擎、移动安全提交、系统文件剪贴板、快捷键、任务 UI 与全屏播放器悬浮控制已编码并通过自动质量门槛；待真实桌面完成资源管理器双向互操作、取消/冲突矩阵和全屏交互验收。
- [~] v0.6：缩略图缓存闭环与参考机一次性 5,000 文件扫描基线已完成；缓存容量、缩略图冷/热缓存、首屏、滚动与内存回落仍待真实桌面验收。
- [~] v0.7：惰性目录树、扫描代次取消、前端监听合并、周期校准和按 CPU 核数限制的媒体并发均已编码并通过自动质量门槛；待真实大目录桌面复测。
- [~] v0.8：单转码进程替换、逐次即时定位、时长复用、低延迟 FFmpeg 参数、CSP/最小能力和 HEAD/Range/路径自动测试均已完成；待执行真实播放兼容矩阵与孤儿进程桌面验收。
- [~] v0.9：已补充安装目录 `data` 清理钩子、窗口状态、背景、数据管理和诊断入口；仍待干净 Windows 的安装、升级、卸载、签名与 Shell 桌面矩阵。
- [ ] v1.0：冻结功能并通过全部发布门槛。

## 明确不做

- 不新增 FFmpeg 扩展或 `ffmpeg-next` FFI 集成。
- 不做自定义元数据列、全局检索、SQLite 业务索引。
- 不做自定义标签和文件哈希去重；全局搜索由 Windows 资源管理器承担。

## 2026-07-19 后续验收

- [x] 固定全局后台 sidecar 许可池、后端并发设置与运行时容量调整；移除动态并发和独立缩略图读取许可池。
- [x] 无上限 FIFO 缩略图队列、滚动只暂停入队、工作区切换不取消，以及三次总尝试与一次 FFmpeg 回退。

## 2026-07-19 缩略图延迟调整

- [x] 覆盖上方旧重试描述：ffmpegthumbnailer 只尝试一次，失败或不可用时立即进行一次 FFmpeg 回退；ffprobe 保留三次总尝试。
- [x] 媒体缓存索引迁移、元数据持久化、统一 ffprobe、文件日志哈希轮询和独立回归测试文件。
- [x] 删除外部播放器设置与原生菜单事件桥，统一主题化 WebView 菜单和复制任务入口。
- [x] 拆分四类 CSS 与导航、工作区、预览、设置/日志 TS 模块，删除 Tailwind 生成残留而不引入 Tailwind。
- [~] 在真实大目录中确认数千项待处理队列保持响应，任务管理器中的后台 sidecar 数不超过设置值。
- [~] 复验缩略图/元数据满载时的连续即时定位、Explorer 双向拖放和系统剪贴板 STA 消息循环。
- [~] 复验菜单边缘定位/键盘导航、深浅主题/DPI/面板缩放，以及日志面板关闭后停止轮询。

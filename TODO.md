# VideoSweeper TODO

> 状态约定：`[x]` 已完成并有复验记录；`[~]` 已实现、待真实桌面验收；`[ ]` 未开始。

## v0.2 收尾验收

- [x] 原文件优先播放；失败或视频尺寸为 0 时回退 FFmpeg。`mp4v` 音频可播、无图像样本已复验。
- [x] Windows Shell 文件拖出（COPY）已在资源管理器复验。
- [~] 完整重启 Tauri 后验证文件监听：外部新增、删除、重命名和拖入复制应在 300ms 合并刷新，日志不再出现 `Command watch not found`。
- [~] 回归全窗口边缘自动滚动框选：网格、列表、高 DPI、标题栏/侧栏附近、窗口底部和停止边界。
- [~] 回归主题化工作区菜单：文件项、列表表头、空白区、无视频工作区的定位、关闭、复制、回收站和刷新。
- [~] 回归转码流的快速切换、连续定位和更多真实编码；常规 H.264 需保持原文件直连。

## v0.3：可维护性与 Windows 剪贴板

- [ ] 为 `src-tauri/src/main.rs` 制定并执行无行为变化的模块拆分：配置、工作区、缩略图、媒体流、文件操作、Windows Shell、菜单、状态/类型。
- [ ] 为 `src/App.tsx` 拆分工作区、播放器、设置、目录树、右键菜单和共享工具；拆分期间保持现有交互与日志行为。
- [ ] 每个拆分阶段执行 `cargo fmt --check`、`cargo check`、`npm run build` 与对应人工回归，不将重构和新功能混在同一阶段。
- [ ] 实现资源管理器兼容的 `CF_HDROP` 系统剪贴板复制（`Ctrl+C`）。
- [ ] 实现剪切/移动语义（`Ctrl+X`、`Ctrl+V`），为 STA 文件操作队列补充移动任务；处理同路径、冲突命名、失效剪贴板路径和取消。
- [ ] 实现复制粘贴（`Ctrl+V`）到当前工作区，并与资源管理器互操作；复制/移动后走统一刷新链路。
- [ ] 建立大目录性能基线：记录首屏、稳定滚动、缩略图缓存命中、工作区刷新和内存占用，再针对热点优化。

## v1.0：性能与稳定性

- [ ] 依据 v0.3 性能基线优化缩略图队列、缓存清理、Base64/图片内存和大目录交互。
- [ ] 完成 Windows 权限级别、目录断连、高 DPI、文件监听、文件操作失败和常见媒体编码的回归矩阵。
- [ ] 改善错误提示、可访问性、安装/升级体验，并持续修复实际使用中发现的 bug。

## 明确不做

- 不新增 FFmpeg 扩展或 `ffmpeg-next` FFI 集成。
- 不做自定义元数据列、全局检索、SQLite 业务索引。
- 不做自定义标签和文件哈希去重；全局搜索由 Windows 资源管理器承担。

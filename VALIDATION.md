# VideoSweeper v0.6-v0.8 验收基线

> 基线日期：2026-07-19。自动结果与真实桌面结果分开记录，不以构建成功代替交互验收。

## 参考机

- Windows 11 家庭中文版 10.0.26200（Build 26200）
- AMD Ryzen 9 7940H，8 核 16 线程
- 15.2 GiB 系统可见物理内存
- ZHITAI PC005 Active 1TB SSD

## 自动基线

统一入口：`npm run check`。

| 项目 | 数据集/条件 | 结果 | 自动上限 |
| --- | --- | ---: | ---: |
| 工作区首次扫描 | 本地 SSD、5,000 个 1 字节 `.mp4`、一次性 Debug 基线首次枚举（未清空操作系统文件缓存） | 48.5 ms | 10 s |
| 工作区重复扫描 | 同一进程立即再次完整枚举 | 49.4 ms | 10 s |
| Rust 回归 | 配置、文件任务、缓存、扫描取消、HEAD/Range、路径与真实子进程回收 | 25 项 | 全部通过 |
| FFmpeg 定位参数烟测 | 8 秒 640×360 合成 MPEG-4，准确定位至 6.5 秒并输出 0.5 秒 fMP4 | 182 ms，154,361 bytes | 退出码 0 且输出非空 |
| Tauri Debug 构建 | 新 CSP、最小能力清单、sidecar 配置 | 成功生成并启动 `video-sweeper.exe` | 构建/启动成功 |

合成扫描只衡量枚举、扩展名过滤、文件元数据和缓存索引查询，不代替真实缩略图生成、WebView 渲染或网络路径测试；结果保留为历史基线，5,000 文件生成不进入默认回归套件。FFmpeg 烟测只确认当前 sidecar 接受低延迟参数并能快速产出，不代替不同编码、关键帧间隔和磁盘条件下的真实连续定位验收。

## v0.6-v0.7 真实桌面性能矩阵

每轮记录目录位置、视频数量/总大小、视图、窗口尺寸/DPI、Debug/Release，并至少执行三次取中位数。

| 场景 | 记录起点 → 终点 | 通过条件 | 结果 |
| --- | --- | --- | --- |
| 冷缓存首屏 | 清空测试用缩略图缓存后打开目录 → 首屏卡片全部可辨识 | UI 持续响应，无批量终端闪烁 | 待桌面复测 |
| 热缓存首屏 | 重启后重新打开同目录 → 首屏缩略图全部显示 | 明显快于冷缓存且无重新生成日志 | 待桌面复测 |
| 5,000+ 滚动 | 连续快速滚动 2 分钟并返回焦点/开头 | 无长时间白屏、跳位或输入冻结 | 待桌面复测 |
| 内存回落 | 冷缓存浏览 10 分钟 → 静置 5 分钟 | 工作集不持续单调增长，内存 LRU 保持有界 | 待桌面复测 |
| 快速目录切换 | 大目录间连续切换 20 次 | 最终目录不被旧响应覆盖，后台 sidecar 数量始终受 CPU 并发上限约束并自然完成 | 待桌面复测 |
| 文件监听 | 连续新增/重命名/删除 100 个文件 | 合并刷新后结果正确，30 秒校准无差异 | 待桌面复测 |

## v0.8 真实播放矩阵

| 维度 | 样本/操作 | 通过条件 | 结果 |
| --- | --- | --- | --- |
| 原文件直连 | H.264/AAC MP4、浏览器可解码 WebM | 不启动 FFmpeg，HEAD/Range 正常 | 待桌面复测 |
| 回退转码 | `mp4v`、浏览器不支持的视频轨、MKV 组合 | 自动回退，有画面和声音 | 待桌面复测 |
| 快速切换 | 可直连与需转码样本间连续切换 50 次 | 无旧画面覆盖，无残留 `ffmpeg` | 待桌面复测 |
| 连续定位 | 转码样本连续快速点击/前后跳转 50 次 | 每次点击均立即请求其目标位置，不合并定位；最新画面不被旧进程覆盖 | 待桌面复测 |
| 路径 | 中文、空格、`#`/`%`、长路径、UNC/映射盘 | 可播放或给出明确错误，不越权访问 | 待桌面复测 |
| 编码 | H.264/H.265/AV1/VP9/mp4v 与 AAC/MP3/Opus/AC3 | 直连或回退策略一致，无无声假成功 | 待桌面复测 |
| 断连与退出 | 播放中切目录、关闭预览、关闭主窗口 | 转码进程停止，3 秒内无本应用所属 FFmpeg | 待桌面复测 |

桌面复测时同时观察应用日志和任务管理器；任何源文件损坏、旧响应覆盖、无界内存增长或孤儿 FFmpeg 都视为失败。

## 2026-07-19 结构重构自动回归

统一入口 `npm run check` 已再次通过：TypeScript 类型检查、Vite 生产构建、Rust 格式检查、Clippy `-D warnings` 和 32 个 Rust 测试全部成功。

新增的结构回归用例集中在独立的 `src-tauri/src/regression_tests.rs`，未与业务实现混放，覆盖：

- 不同逻辑线程数下的三分之二默认值、配置夹紧、许可池并发上限，以及运行中扩容/缩容；
- 后台 sidecar 恰好三次总尝试的重试预算与重试间许可释放；
- 旧缩略图索引迁移、损坏恢复、缩略图 LRU 淘汰后保留元数据；
- 日志首次读取、哈希未变不返回正文、尾部快照变化；
- 转码所有权回收和默认缩略图缓存容量。

真实桌面待验项目：数千项无上限逻辑队列、后台进程峰值、满载时连续定位、Explorer 双向拖放/复制/剪切/粘贴、菜单焦点与键盘操作、2.5 秒日志轮询、主题/DPI/面板缩放。自动测试通过不替代这些 Windows 与 WebView 交互验收。

## 2026-07-19 重新拆分后的后续测试项

> 本节追加于结构重拆完成后；其中“32 项 Rust 回归”覆盖并更新上方自动基线表中较早记录的“25 项”。

### 自动化候选项

- 为 `useWorkspaceController` 补充前端测试：连续发起多个工作区扫描时，仅最新请求可以更新工作区；过期成功和过期失败都不得覆盖新状态。
- 为 `useThumbnailQueue` 补充假时钟测试：滚动期间只暂停新增入队，已有 FIFO 不清空；180 ms 后恢复；相同路径只能存在一个待处理或运行任务。
- 为 `useWorkspaceGestures` 补充指针序列测试：单选、Ctrl 多选、Shift 范围、矩形框选和拖出手势互不抢占。
- 为 `useFileTasks` 补充入口一致性测试：拖入、粘贴和“复制到”最终都调用同一文件任务命令；拖出和系统剪贴板仍只经过 OLE/STA 适配层。
- 为 `PreviewPanel` 补充键盘测试：焦点位于面板主体时空格、左右键控制播放器，焦点位于按钮或输入框时不得拦截。
- 为 `useLogViewer` 补充串行轮询测试：关闭面板、页面隐藏、已有请求未结束时不得再次请求；哈希不变时不得替换正文。
- 为模块边界增加静态约束：`main.rs` 只负责组装，`App.tsx` 只负责跨模块协调；禁止重新引入原生菜单桥接、前端完整默认配置和动态 sidecar 调度状态。

### 真实桌面重拆回归

- 在 5,000 个以上视频的目录中快速滚动两分钟，确认待处理队列不丢失、不重复，停止滚动后继续补齐，后台 sidecar 峰值不超过设置值。
- 将后台许可池调为 1、默认值和逻辑线程上限，分别同时触发缩略图与元数据，确认它们共用许可池；播放器 FFmpeg、OLE 拖放和 STA 剪贴板仍可即时响应。
- 快速点击转码视频进度条 50 次，逐次核对日志中的目标秒数、旧 PID 终止和新 PID 接管，确认定位未被合并。
- 切换工作区后等待旧缩略图任务自然完成，再返回旧工作区，确认结果已写缓存且没有重复生成。
- 在 Explorer 中完成拖入、拖出、复制、剪切、粘贴、复制到和回收站矩阵，确认重拆后的前端 hook 没有改变各路线的职责。
- 分别用鼠标和键盘操作左栏、工作区、单视频与多视频菜单，检查边缘定位、外部点击、Escape、方向键、Enter、焦点进入和焦点恢复。
- 打开日志面板观察至少一分钟，确认 2.5 秒轮询串行执行、哈希不变时界面不刷新，关闭或隐藏页面后停止请求。
- 在 100%、125%、150% DPI、深浅主题、预览栏展开/折叠和左右栏缩放下检查布局，确认组件拆分没有造成视觉回归。

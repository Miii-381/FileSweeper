# FileSweeper 开发笔记

## 注：开发笔记是对开发过程中遇到的问题及解决方案的记录，并非单纯的开发功能整理


记录日期：2026-07-14

## 1. Rust 工具链未在 PATH 中

### 现象

初次执行 `rustc`、`cargo` 时，终端提示命令不存在。

### 原因

Rust 已安装或刚安装完成，但当前终端进程没有获取到新的环境变量。

### 解决方法

确认 Rust 的 `bin` 目录已加入用户 PATH。例如本机使用：

```text
D:\Rust\cargo\bin
```

关闭并重新打开终端或 Codex 后，使用以下命令确认：

```powershell
rustc --version
cargo --version
rustup show active-toolchain
```

本项目随后确认可用的版本为 Rust/Cargo `1.97.0`。

## 2. npm 缓存目录权限与 registry

### 现象

npm 曾尝试向 `D:\Node.js\node_cache` 写入缓存并出现 `EPERM`；使用 `https://mirrors.tuna.tsinghua.edu.cn/npm/` 时，查询 `create-tauri-app` 出现 `404`，后续 `npm install` 也可能长时间没有输出。

### 原因

- npm 缓存目录可能没有当前进程的写权限，或被安全软件、其他进程占用。
- registry 可能未同步所需包，或者镜像服务不可用。

### 解决方法

检查根目录 `.npmrc` 中的配置：

```ini
registry=https://mirrors.tuna.tsinghua.edu.cn/npm/
cache=D:/Node.js/node_cache
```

确保缓存目录对当前用户可写。若 registry 无法获取 Tauri 相关包，改用可用源后重新安装，例如：

```ini
registry=https://registry.npmjs.org/
```

然后执行：

```powershell
npm install
```

## 3. Tauri v2 单实例不需要前端 npm 插件

### 现象

将单实例能力视为独立的前端 npm 插件安装，容易导致依赖理解或安装配置错误。

### 原因

Tauri v2 的单实例限制通过 Rust 端的 `tauri-plugin-single-instance` crate 实现，不需要安装对应的前端 npm 包。

### 解决方法

在 `src-tauri/Cargo.toml` 中保留 Rust 依赖：

```toml
tauri-plugin-single-instance = "2"
```

在 `src-tauri/src/main.rs` 中注册插件：

```rust
.plugin(tauri_plugin_single_instance::init(|app, _, _| {
    if let Some(window) = app.get_webview_window("main") {
        let _ = window.unminimize();
        let _ = window.set_focus();
    }
}))
```

不要仅为单实例功能安装前端 npm 插件。

## 4. Tauri v2 NSIS 配置字段

### 现象

`tauri.conf.json` 的 NSIS 配置不符合 Tauri v2 Schema，导致打包配置校验失败。

### 原因

Tauri v2 中字段名为 `installMode`，不是 `installerMode`。

### 解决方法

使用以下结构：

```json
{
  "bundle": {
    "windows": {
      "nsis": {
        "installMode": "currentUser"
      }
    }
  }
}
```

## 5. Vite 监听 Rust 构建目录导致 EBUSY

### 现象

运行 Tauri 开发命令时，Vite 可能报出类似错误：

```text
Error: EBUSY: resource busy or locked, watch
'...\\src-tauri\\target\\debug\\build\\...\\build_script_build-....exe'
```

随后 Tauri 会报告 `beforeDevCommand` 以非零状态结束。

### 原因

Vite 的文件监听器会递归观察项目根目录。`src-tauri/target/` 是 Cargo 构建产物，其中的 `.exe` 文件会在 Rust 编译期间被 Windows 锁定，因此不应被 Vite 监听。

### 解决方法

在 `vite.config.ts` 的 `server` 配置中忽略整个 Rust 目录：

```ts
export default defineConfig({
  // 其他配置
  server: {
    watch: {
      ignored: ["**/src-tauri/**"],
    },
  },
});
```

这会阻止 Vite 监听 Cargo 产物，同时不会影响前端 `src/` 的热更新。

## 6. FFmpeg sidecar 命名

### 现象

Tauri 打包外部二进制文件时，不能直接使用下载包中的 `ffmpeg.exe`、`ffprobe.exe` 文件名。

### 原因

`bundle.externalBin` 会按当前 Rust target triple 查找 sidecar。

### 解决方法

Windows x64 MSVC 目标使用以下文件名：

```text
sidecars/ffmpeg-x86_64-pc-windows-msvc.exe
sidecars/ffprobe-x86_64-pc-windows-msvc.exe
```

并在 `src-tauri/tauri.conf.json` 中配置：

```json
{
  "bundle": {
    "externalBin": [
      "../sidecars/ffmpeg",
      "../sidecars/ffprobe"
    ]
  }
}
```

## 7. Tauri Windows 构建缺少应用图标

### 现象

Cargo 在执行 `file-sweeper` 的自定义构建脚本时失败，日志包含：

```text
`icons/icon.ico` not found; required for generating a Windows Resource file
during tauri-build
```

### 原因

Tauri 在构建 Windows 应用时，需要
`src-tauri/icons/icon.ico` 生成 Windows 资源文件（`.rc`）。初始空项目未包含 `src-tauri/icons/` 下的图标资源，因此构建无法继续。

### 解决方法

推荐准备一张至少 `1024 x 1024` 像素的 PNG 图片，例如 `app-icon.png`，然后在项目根目录执行：

```powershell
npm run tauri icon app-icon.png
```

该命令会在 `src-tauri/icons/` 下生成 `icon.ico`、`icon.png`、`32x32.png`、
`128x128.png` 等所需图标文件。

若已有 ICO 图标，也可以直接将文件放到以下路径，并确保文件名为 `icon.ico`：

```text
src-tauri/icons/icon.ico
```

## 8. 界面圆角与微交互

### 目标

在不牺牲桌面工具信息密度的前提下，界面采用接近 Material Design 3 的表面层次：

- 常规按钮、输入框和导航项使用 `10px` 至 `12px` 圆角。
- 强调操作使用 `12px` 圆角，图标承载区使用更柔和的 `16px` 至 `18px` 圆角。
- 三栏工作区仍是连续的应用表面，不把整页拆成大量悬浮卡片。

### 微交互原则

交互动画只使用 `transform`、`opacity`、颜色和阴影变化，避免为普通悬停效果触发布局重排或复杂的重绘：

- 普通按钮悬停时轻微上移，按下时缩放至 `0.97`。
- 主题色面板使用短暂的位移和透明度进入动画。
- 欢迎页与预览栏使用一次性进入动画，避免持续循环效果。

`src/styles.css` 同时包含 `prefers-reduced-motion` 规则。系统开启“减少动态效果”时，动画与过渡会缩短到近乎即时，保留状态变化但不产生明显运动。

### 注释约定

对涉及状态持久化、CSS 变量、Cargo/Vite 构建边界和 Tauri 原生插件注册的代码添加了简短注释。纯声明性 JSON 文件不添加注释，因为标准 JSON 不支持注释，强行加入会导致 Tauri 配置解析失败。

## 9. 亮色模式

主题面板中的月亮与太阳图标分别切换深色、亮色模式。模式选择使用 `file-sweeper-color-mode` 保存到浏览器 `localStorage`，与主题色选择相互独立。

亮色模式使用中性表面，主要层级为：

```text
应用基底：#F2F2F7
工具栏与侧栏：#FFFFFF
输入框和普通控件：#FFFFFF
悬停状态：#E5E5EA
分隔线：#D1D1D6
```

主题色仍只用于强调操作、选中状态与图标承载区。每组预设颜色都额外配置了较浅的强调容器色，避免深色模式中的深色强调背景直接用于亮色界面。

## 10. 修改背景色后视觉没有变化

### 现象

将应用外层背景改为 `#1A1A1A` 后，中央区域视觉上仍接近原来的颜色，看起来像是修改没有生效。

### 原因

界面使用了多层表面色。根节点和 `.app-shell` 虽然已更新，但真正占据主视觉面积的 `.workspace` 仍使用单独的背景色，因此覆盖了外层背景。

### 解决方法

检查并统一各视觉层级的背景色，而不是只修改根节点。最终采用中性 macOS 深色灰阶：

```text
应用基底与主工作区：#1C1C1E
工具栏、侧栏、预览栏：#242426
普通控件：#2C2C2E
悬停状态：#3A3A3C
分隔线：#38383A
```

主题色只用于强调操作和选中状态，避免固定表面色混入青绿等色相。

## 11. 主题面板只能再次点击设置按钮关闭

### 现象

主题色面板打开后，只能再次点击设置按钮才会收起。用户点击其他工作区、导航栏或工具栏时，弹层仍会停留在界面上。

### 原因

主题面板仅绑定了设置按钮的切换状态，没有监听面板外部交互。

### 解决方法

使用 `useRef` 持有主题面板容器，并在面板打开期间注册两个临时全局监听器：

- `pointerdown`：点击目标不在主题面板容器中时关闭面板。
- `keydown`：按下 `Escape` 时关闭面板。

面板关闭或组件卸载时移除监听器，避免残留全局事件处理器。

## 12. 工作区交互与主题变量

### 现象

Tauri 使用 WebView2，直接右键会出现浏览器上下文菜单，不能执行文件管理操作。

### 原因

WebView2 默认按网页环境处理右键操作，不会自动提供针对本地视频和目录的 Windows 文件管理命令。

### 解决方法

- 前端阻止 WebView 默认 `contextmenu`。
- Rust 端使用 Tauri 原生 `Menu` 创建上下文菜单。
- 视频右键菜单提供打开、在资源管理器中显示和移到回收站；目录只提供实际可执行的目录操作。
- 回收站删除通过单一 STA 工作线程中的 Windows `IFileOperation` 执行，批量任务逐文件返回成功或失败结果，绝不降级为永久删除。

## 13. 工作区网格在窗口缩放后被裁切

### 现象

网格作为 CSS Grid 子项时，未声明可收缩的最小高度；父工作区又隐藏溢出，缩小窗口高度后卡片无法在自身区域内滚动。

### 解决方法

- 为 `.video-grid` 添加 `min-width: 0`、`min-height: 0` 和内部滚动。
- 固定卡片尺寸为 `220 x 180px`，网格只根据可用宽度增减列数。
- 左、中、右栏改用 `react-resizable-panels`；中栏以 `ResizeObserver` 动态保留一张卡片和内边距所需的最小像素宽度。

## 14. 列表表头直接拖拽换序错位

### 现象

最初仅记录指针下方的目标字段，未记录落在目标字段的左半区或右半区。源字段位于目标字段之前时，删除源字段会改变旧下标；相邻字段向右拖动会重新插回原位。

### 解决方法

- 使用 Pointer 事件而不是 WebView2 下不稳定的 HTML5 Drag and Drop。
- 根据目标表头中点记录 `before` 或 `after` 插入位置，并显示对应侧的主题色插入标记。
- 先移除源字段，再重新查找目标字段下标后插入，避免相邻列与跨列操作错位。
- 表头右侧保留独立的列宽拖拽命中区；名称列固定为第一列，不参与换序。

## 15. 深浅色主题依赖散落的 CSS 覆盖规则

### 现象

早期只将强调色注入 CSS 变量，正文、表面、边框和滚动条颜色仍在 CSS 中硬编码，并通过大量 `.light-theme` 规则逐项覆盖，容易遗漏输入框和新增控件。

### 解决方法

- 新建 `src/theme.ts`，集中维护强调色预设与深浅模式的语义令牌。
- `App.tsx` 将当前模式令牌和强调色一起注入 `.app-shell`。
- `styles.css` 使用 `--text-*`、`--surface-*`、`--border-*`、`--icon-muted` 和滚动条变量，移除旧的逐项浅色覆盖。

## 16. 缩略图链路实现与故障处理（2026-07-15）

### 目标

缩略图链路现由 Rust 后台批处理执行：前端只在当前视口及其附近的卡片稳定可见时入队；每批最多 10 个文件并行生成，批次结束后合并更新界面，并对 `data/thumbnails/index.json`进行一次格式化、原子写入。缓存索引在应用启动时一次性加载到内存，查找只做规范化路径键、文件大小、最后修改时间和取帧模式的比较。

主路径使用 `ffmpegthumbnailer`；它异常退出、没有输出图片或不支持特定编码时，自动回退到`ffprobe` 取时长和完整 `ffmpeg` 取帧。所有 sidecar 以低于 GUI 的 Windows 调度优先级运行。

### 现象、原因与解决方法

| 问题 | 根因 | 解决方案 |
| :--- | :--- | :--- |
| 没有可查看的诊断信息 | 原先没有持久日志与界面读取入口 | 接入 `tauri-plugin-log`，日志写入 `data/logs/file-sweeper.log`；Rust 和前端均记录关键错误，并提供日志面板。单文件轮转上限设为 1 MiB。 |
| FFmpeg 未生成 JPEG | 临时名使用 `.jpg.tmp`，FFmpeg 无法据此判断 JPEG muxer | 临时文件改为 `.tmp.jpg`，让最终扩展名保持可见。 |
| 缩略图显示为缺省图 | WebView asset protocol 访问缓存文件不稳定 | `read_thumbnail` 在 Rust 端读取 JPEG 并返回 Base64 data URL；前端按缩略图路径记忆数据 URL 和请求 Promise。 |
| 生成时 GUI 卡顿 | 进程等待、CPU/磁盘竞争，以及每张完成时替换整个工作区数组 | 进程控制放入 `spawn_blocking`；sidecar 降低 Windows 优先级；前端使用独立缩略图路径覆盖表、`memo` 和 `startTransition`，不再逐张重建/排序完整视频数组。 |
| 缩略图重复生成 | 缓存只依赖磁盘文件或索引未稳定落盘 | 使用 `index.json` 保存路径键、文件大小、修改时间、取帧模式和 JPEG 文件名；成功后更新内存索引并原子写入。 |
| 大目录进入慢且日志爆量 | 索引未命中后仍逐文件探测缓存 JPEG，并记录每条 miss | 删除旧的磁盘回退探测；目录扫描只查询内存 Map，未命中立即返回空，不再逐文件打印 miss。 |
| 取帧位置容易落在 OP | 旧固定早期取帧不适合电视剧和动画 | 设置提供开头、前段、正中、后段、结尾；默认正中 50%。开头改为固定 1 秒快速模式，可跳过时长探测。 |
| 改变取帧语义后误用旧缓存 | 缓存条目只记录位置名称 | 缓存键中加入取帧模式与生成器版本；切换位置或生成器后旧帧自动失效重建。 |
| 首屏后生成速度不足 | 每张都启动 `ffprobe` 和 `ffmpeg` | 主路径改用 `ffmpegthumbnailer` 的 `-t 25%/50%/...` 参数，百分比取帧不需要先启动 `ffprobe`。 |
| HEVC MKV 不能生成 | 日志显示 `ffmpegthumbnailer` 对部分 MKV 以 `0xffffffff` 异常退出 | 捕获缩略图器错误并回退到 `ffprobe + ffmpeg`。日志样本中的 HEVC MKV 已用回退命令验证可生成 JPEG。 |
| 高速滚动误生成途经卡片 | `IntersectionObserver` 在进入视口时立刻入队 | 滚动期间暂停派发并清空未开始队列；停止 180ms 后重新观察，只生成最终停留区域及附近卡片。 |
| 高频完成造成重复 JSON/UI 写入 | 单张 IPC 结果单独提交 | `generate_thumbnails` 批处理命令聚合最多 10 条任务；同批只提交一次缩略图状态与一次 JSON 原子写入。 |

### 原因与关键决策

1. **先建立可观测性**：用户最早反馈“没有缩略图、也没有日志”。因此优先加入文件日志、Rust 侧日志读取 command 和前端日志面板，而不是继续猜测 FFmpeg 行为。后续 `.jpg.tmp`、asset protocol、缓存 miss、`ffmpegthumbnailer` 崩溃等问题都依赖这条日志链路定位。
2. **修复生成文件扩展名**：日志和 FFmpeg stderr 表明临时输出后缀导致 muxer 识别失败；改为 `.tmp.jpg` 后，普通视频能够生成 JPEG，确认 sidecar、路径解析和生成基本链路可用。
3. **放弃 WebView asset protocol**：当 JPEG 已生成但界面仍显示缺省图时，尝试过 `convertFileSrc`/asset protocol。该路径在当前 Tauri/WebView2 配置中没有稳定渲染，最终改成后端 IPC 返回 Base64 data URL。该方案会增加少量 IPC 和内存开销，但消除了本地协议和路径授权的不确定性；前端用两个 Map 去重，避免同一 JPEG 被重复读取或编码。
4. **将媒体工作移出交互线程**：生成、读取和目录扫描均改用 `tauri::async_runtime::spawn_blocking`。这保证 Rust/Tauri 事件循环不等待外部进程，但并不能自动消除 CPU、磁盘和 React 渲染竞争；后续还需降低 sidecar 优先级、限制/分批并发和拆分前端缩略图状态。
5. **从逐文件缓存探测改为内存索引**：讨论过 JSON 与 SQLite。当前缓存规模和访问模式只需要“路径 → 单个缩略图”的 O(1) 查询，JSON 更轻、可直接人工检查，也已在项目依赖中可用 `serde_json`。SQLite 仍保留给未来需要 LRU 淘汰、跨进程一致性、复杂检索或大量媒体元数据时使用。
6. **调整取帧语义**：固定前 20% 容易命中动画/电视剧 OP，默认改为正中 50%。为兼顾速度，“开头”选项明确改成固定 1 秒快速模式。每种取帧模式写入缓存身份，避免“设置变了但仍显示旧帧”。
7. **验证资源管理器与本项目速度差异**：资源管理器通常借助 Shell 扩展、系统级缓存和渐进调度；本项目初次面对大量未缓存视频时必须真实解码。应优先优化首屏、缓存命中和队列调度，而不是尝试在目录进入时立刻生成全部缩略图。
8. **从串行到受控并发**：最初前端 `.slice(0, 1)` 和后端互斥锁构成双重串行，首屏很慢。后端改为许可池，前端改为批处理队列；用户要求后将上限设为 10。所有 sidecar 通过 Windows `BELOW_NORMAL_PRIORITY_CLASS` 创建，尽量让 GUI 输入优先。10 路对机械盘、低核心或内存紧张设备仍可能产生吞吐竞争，属于后续自适应并发优化范围。
9. **移除 600ms 全目录刷新**：为实现“生成后立即显示”曾加入周期性 `list_directory` 刷新；在含5165 个视频的目录中这等同于反复遍历整个工作区，反而造成卡顿和大量日志。生成 command 的结果已包含源路径与缩略图路径，因此改为直接更新对应缩略图状态。
10. **从整数组替换改为覆盖表**：早期每完成一张缩略图都会 `map` 整个工作区数组，并重新计算排序；在数千项列表中会让 GUI 抖动。现在工作区元数据保持不变，单独维护`thumbnailPathOverrides: Map<文件路径, 缩略图路径>`，并由 `React.memo` 的 `FileThumbnail`和 `startTransition` 只更新变化项。
11. **批量化状态与索引写入**：单张任务会让 JSON 格式化/原子替换和前端状态更新频繁发生。现在一个 `generate_thumbnails` command 最多接收 10 个路径，在后台线程中分别执行，汇总成功和失败项后只写入一次索引快照；前端对同批结果只更新一次覆盖表。单张失败会写入失败集合，不影响同批成功项。
12. **滚动停止后再派发**：`IntersectionObserver` 的自然行为会报告快速拖动时经过的每张卡片。当前实现把滚动事件作为门控：滚动期间清空尚未开始的队列并拒绝新任务；连续 180ms 无滚动后递增可见性 revision，重新观察最终位置。已经启动的任务不取消，以免重复解码；这是一种“允许完成、不再扩散”的折中。
13. **引入 ffmpegthumbnailer 并保留回退**：`ffmpegthumbnailer` 可直接使用 `-t 50%`，省去正中取帧之前的 ffprobe 进程，普通 MP4 测试约 166ms。实际 HEVC/FLAC MKV 日志显示多个任务以`exit code: 0xffffffff` 退出，因此不能把它作为唯一实现；现采用“快速主路径 + 兼容回退”模式。

### 当前数据模型与调用链

```text
目录扫描
  -> 读取文件元数据（大小、mtime）
  -> 查询内存 ThumbnailIndex.entries
  -> 命中：返回 data/thumbnails/<hash>.jpg 路径
  -> 未命中：只显示占位，不扫描缓存目录

稳定视口卡片
  -> 前端累积至最多 10 个路径
  -> invoke(generate_thumbnails)
  -> Rust 许可池控制 sidecar 并发
  -> ffmpegthumbnailer(-t 百分比或 1s)
  -> 失败时 ffprobe(duration) + ffmpeg(-ss 时间点)
  -> 成功后更新内存 index
  -> 批次结束，pretty JSON 写入临时文件并原子替换 index.json
  -> 返回 thumbnails[] / failures[]
  -> 前端合并 thumbnailPathOverrides，并按需 read_thumbnail
  -> Rust 读取 JPEG、Base64 编码，<img> 显示 data URL
```

`ThumbnailIndexEntry` 的字段含义如下：

| 字段 | 作用 |
| :--- | :--- |
| 路径键 | Windows 下使用小写的规范化路径字符串，避免盘符/扩展名大小写导致重复缓存。 |
| `size` | 文件大小变化时缓存失效。 |
| `modifiedAt` | 最后修改时间变化时缓存失效。 |
| `capturePosition` | 包含取帧位置和生成器版本，例如 `middle-thumbnailer-v1`。 |
| `thumbnailFile` | 缓存目录中的 JPEG 文件名；文件名由路径 FNV-1a 哈希生成。 |

### 验证状态

- 大目录日志曾报告 `videos=5165` 且每个项目都打印 `Thumbnail cache miss`。排查发现这是旧的磁盘回退探测和 debug 日志造成的噪声，现已移除；只有索引过期才记录简要诊断。
- 缓存目录中存在 JPEG 与 `index.json` 时，索引记录可正确映射到文件；此前误以为缓存不存在，实际是查询了错误的 `%APPDATA%` 目录，而项目的便携模式数据位于可执行文件同级 `data`。
- `ffmpegthumbnailer -h` 已验证命令参数；对一个 MP4 使用 `-s 480 -t 50% -q 7 -c jpeg` 成功。
- HEVC MKV 的日志同时出现“开始生成”和 `0xffffffff`；使用相同 MKV 由 `ffprobe -> ffmpeg -ss <duration * 0.5>` 取帧，成功产出 20,829 字节 JPEG。
- 每次重要改动后执行 `cargo fmt`、`cargo check` 与 `npm run build`；这些验证覆盖 Rust 编译和 TypeScript/Vite 构建，不替代运行中的 WebView、滚动行为与 NSIS 安装包人工验收。

### 暂缓方案

- **全工作区逐文件查找缓存**：对大目录是线性额外 I/O，已删除。
- **生成期间每 600ms 重扫目录**：会重复枚举数千文件，已删除。
- **只用 asset protocol 显示 JPEG**：当前环境下不稳定，已改为 IPC data URL。
- **让 ffprobe 常驻“热启动”**：当前缩略图主路径不再需要它；回退时仅按需启动。若后续需要大量
  媒体元数据，可单独设计常驻 worker 或批量探测协议。
- **直接上 SQLite 管缓存**：当前 JSON 内存索引足够，避免在缩略图问题尚未稳定时扩大持久化复杂度。

### 后续风险

- 10 路并行是用户指定的上限，不保证所有硬件的最佳吞吐；高码率视频、机械硬盘和 HEVC 解码可能需要根据 CPU 核心数、磁盘类型、电源状态和失败率自适应调低并发。
- 本节完成时卡片仍会一次性挂载，万级目录的 DOM/Observer 数量是主要瓶颈；该问题随后在第 19 节通过虚拟滚动处理。
- `thumbnailCacheGb` 配置和缓存清理/LRU 淘汰尚未真正执行；当前缓存会持续增长。
- 读取 JPEG 使用 Base64 data URL，首屏数量受控时可接受；虚拟滚动完成后应评估 Blob URL、图像解码和内存回收策略。
- Windows 原子替换已经实现，但应用异常退出发生在批次写入前时，本批 JPEG 可能成为孤儿文件；后续应在启动时清理无索引 JPEG，并在退出前尽力 flush 脏索引。

### 关键实现约束

- 不因目录进入而递归预生成全部媒体；只生成首个稳定视口和后续滚动停留区域。
- 已启动的批次不因滚动强杀，避免浪费已进行的解码；尚未启动的任务可以丢弃。
- `index.json` 使用 `serde_json::to_vec_pretty` 格式化写入；Windows 通过 `MoveFileExW` 完成替换。

## 17. 右栏内嵌播放器与音量记忆（2026-07-15）

### 目标

选中视频后在右栏直接预览，并在切换视频或重启应用后保留音量与静音状态。播放器需要支持播放、定位、速率、音量、全屏和外部播放器回退，同时不能把任意本地路径直接暴露给 WebView。

### 原因

- WebView2 不能稳定地直接读取任意本地文件路径。
- 播放器若只在组件局部保存音量，切换视频或组件重建后会恢复默认值。
- 用户快速切换选择时，立即为每个候选文件创建媒体流会造成无效 I/O。

### 解决方法

- Rust 启动只绑定 `127.0.0.1` 的本地 HTTP 服务，使用 `axum` 和 `tower-http::ServeFile` 提供原文件，并支持 Range 与 HEAD 请求。
- 前端传入 URL 编码后的绝对路径；后端每次请求重新规范化路径，确认文件存在、属于常规文件且扩展名仍在白名单内。
- 右栏先显示缓存缩略图和文件信息，选择稳定 250ms 后再请求播放地址；`canplay` 前不移除缩略图。
- 自定义控件提供播放/暂停、进度、时间、静音、音量、速率和全屏；内嵌播放失败时保留错误状态，并允许用户使用系统默认播放器打开。
- `volume` 与 `muted` 写入全局偏好：音量在停止操作 400ms 后保存，静音立即保存；静音状态与静音前音量分开记录。

### 验证状态

`cargo check` 和 `npm run build` 已通过。真实 WebView2 下的 Range/Seek、中文路径、快速切换和安装包场景仍需桌面验收。

## 18. 日志面板引发缩略图期间根组件刷新（2026-07-15）

### 现象

缩略图生成期间界面仍有卡顿；即使日志面板关闭，根组件也会频繁重新渲染。

### 原因

Rust 日志同时发送到 WebView，而前端启动后无条件调用 `attachLogger`。每条缩略图 `info/debug` 日志都会执行 `setLiveLogs`；该状态位于 `App` 根组件，因此每条日志都会触发整棵工作区的 React 协调。文件日志写入本身不是主要瓶颈。

### 解决方法

- 只在日志面板打开期间订阅 WebView 日志和读取文件日志，关闭时解除订阅并清空面板状态。
- 面板默认只显示 `WARN`/`ERROR`，用户可临时切换到信息或调试级别。
- Rust 控制台与文件日志继续保留全量级别，前端筛选不降低诊断能力。

## 19. 缩略图完成事件、读取限流与虚拟滚动（2026-07-15）

### 现象

一个 10 项批次必须等待全部 worker 结束后才返回；慢速 HEVC 回退会阻塞已完成 JPEG 的显示。缓存命中后，每张卡片又分别触发 IPC、磁盘读取和 Base64 编码；网格和列表还会对全部可见结果建立组件与观察器。

### 原因

- 批次结果只在全部 `join` 完成后统一返回。
- 缩略图读取没有独立并发上限。
- 大目录没有虚拟化，任意根状态更新都可能协调数千个项目。
- 旧工作区的后台结果可能在目录切换后继续回写。

### 解决方法

1. 单张 JPEG 写入并更新内存索引后立即发出 `thumbnail-generated`；批次结束时仍只原子写入一次 `index.json`。
2. `read_thumbnail` 使用独立许可池，最多并发读取和编码 4 张 JPEG，避免与最多 10 路 sidecar 解码叠加。
3. 网格按固定行高、列表按固定项目高度接入 `@tanstack/react-virtual`，只挂载视口和少量缓冲项。
4. 网格列数由容器 `ResizeObserver` 计算，并通过回调 ref、布局阶段重算和显式 CSS 变量保证首帧稳定。
5. 前端维护当前工作区路径集合；目录切换或文件删除后丢弃旧任务事件。

### 验证状态

实际体验中，大目录滚动和缩略图显示已明显流畅。Base64 data URL 的内存回收和 sidecar 自适应并发仍留待后续版本处理。

## 20. v0.1 文件操作、媒体信息与键盘交互补齐（2026-07-15）

### 目标

补齐安全复制、内联重命名、键盘选择和按媒体字段排序，使 v0.1 具备基本文件整理能力。

### 解决方法

- 工作区支持将资源管理器拖入的受支持视频复制到当前目录；同名文件按 `文件名 (1).扩展名` 递增命名。v0.1 阶段同目录源文件会被跳过，v0.5 已调整为“同目录复制生成副本、同目录移动才跳过”。
- `F2` 只编辑扩展名前的文件主名。后端会校验 Windows 非法字符、保留设备名、空名称与末尾点/空格，并通过单 STA 文件操作队列执行重命名。
- 写入型复制采用 Windows `IFileOperation`：先写入唯一临时文件，再校验字节长度，最后重命名提交；失败会尝试清理临时文件，工作区枚举时也会清除遗留的应用临时文件。删除、重命名和复制不会并发交叉执行。
- 补齐 `Ctrl+A`、可见结果范围选择、上下方向键与工作区空格选择；播放器获得焦点时仍由播放器消费空格。
- 列表视图和按时长/分辨率排序会按每批最多 10 个视频调用 `ffprobe`。探测期间显示阻塞等待层并锁定操作；完成后一次性合并元数据并排序，避免排序过程中跳动。

### 现象、原因与解决方法

| 问题 | 原因 | 解决方案 |
| :--- | :--- | :--- |
| 拖入复制可能覆盖同名文件 | 目标目录已有同名媒体 | 预先选择未占用名称，按 Windows 常见的 ` (n)` 规则递增，绝不覆盖。 |
| 复制中断可能留下半成品 | 直接写入最终文件名无法区分完成与中断 | 使用隐藏临时文件、长度校验、成功后改名；失败路径清理临时文件。 |
| 时长和分辨率排序会阻塞或频繁重排 | 每个视频都要启动一次 `ffprobe`，且元数据逐项到达 | 限制为小批并发，等待全部批次完成后一次性写入前端状态；遮罩期间禁止其他交互。 |
| 文件操作与回收站调用线程模型不一致 | Shell 文件操作要求 STA 且不应并发 | 统一交给已有专用 STA FIFO 队列，所有用户可见写入操作串行执行。 |

## 21. 资源管理器式选择、拖入与内联重命名（2026-07-15）

### 目标

让网格、列表、拖入、实时刷新和重命名尽量符合 Windows 资源管理器的交互习惯，并保证这些操作继续服从单一 STA 文件写入边界。

### 解决方法

- 网格和列表的空白区域点击会清空多选与选择锚点；点击卡片、行和列表列头不会误清除选择，行为与资源管理器一致。
- 使用 Webview 级原生拖放事件而非窗口级监听，并显式启用 `dragDropEnabled`。进入工作区时显示放置提示，放下后记录文件数量并执行复制。
- 此阶段使用 `tauri-plugin-fs` 对当前工作区进行非递归监听；任意新增、删除、重命名经 300ms 合并后重新枚举目录，同时保留搜索、排序、现存选择和已探测的媒体信息。v0.4 后该监听已迁移到 Rust `notify`。
- F2 不再调用浏览器提示框。选中单个视频后，卡片或列表名称原位变为输入框；Enter 或失焦提交，Escape 取消，扩展名保持不可编辑。
- `ffmpegthumbnailer` 的 target-triple 副本位于
  `sidecars/ffmpegthumbnailer-x86_64-pc-windows-msvc.exe`，以便 Tauri/NSIS 打包。

### 验证状态

- `cargo check` 与 `npm run build` 均已通过。
- `ffmpegthumbnailer` 对一个 MP4 的中点取帧冒烟测试成功，约 166ms。
- `ffprobe + ffmpeg` 回退链路对日志中的 HEVC MKV 冒烟测试成功，约 876ms。
- 尚未进行最终 NSIS 安装包、真实 WebView 播放与万级文件目录的人工验收。

## 22. 资源管理器式交互、实时刷新与 Windows 拖放权限排障（2026-07-15）

### 目标

本轮在已有“复制、重命名、文件监听”实现上，根据实际交互反馈完成四项收口：

1. 多选后点击工作区空白处应取消选择，行为参照 Windows 资源管理器。
2. 外部新增文件、拖入复制、重命名或删除后，工作区应自动更新，而不能要求用户手动切换目录。
3. 将视频拖到 GUI 时应显示可复制状态并进入复制链路。
4. `F2` 不应使用浏览器 `prompt`；应在当前卡片或列表行中直接修改名称。

### 解决方法

#### 1. 空白处取消多选

原有选择逻辑只处理文件卡片/列表行点击，没有定义工作区背景点击。因此多选完成后，用户必须再点另一个文件才能改变选择状态，和资源管理器的操作习惯不一致。

实现时没有给全局容器无差别绑定“清空选择”，否则点击工具栏、列表列头、卡片内部缩略图都会意外清空。新增背景点击判定流程：

1. 网格、列表和空工作区分别接收点击事件。
2. 从事件目标向上查找 `.video-card`、`.video-list-row`、`.video-list-header`。
3. 若命中这些可交互元素，则保留原有的选择、范围选择、右键菜单和列布局行为。
4. 仅当命中工作区空白、虚拟列表的未占用区域或空目录区域时，清空 `selectedFiles` 和 `selectionAnchor`。

卡片/列表行由原生 `button` 改为带 `role` 与 `tabIndex` 的容器，避免在 `button` 内嵌入重命名输入框这一无效 HTML 结构；同时补齐鼠标指针、不可选中文本和键盘焦点样式。

#### 2. 当前工作区实时刷新

之前拖入复制结束后调用完整的 `activateWorkspace`。它适合“切换目录”，但会重置搜索条件、选择与锚点；而手动在资源管理器添加文件时根本没有刷新入口。

解决流程如下：

1. 前端通过 `tauri-plugin-fs` 对当前工作区建立**非递归**监听。
2. 同一时间段的多个文件系统事件使用 300ms 定时器合并，避免复制临时文件、最终改名和系统元数据更新造成连续重扫。
3. 触发后调用新的 `refreshWorkspace`，而不是目录切换函数。
4. `refreshWorkspace` 重新调用 `list_directory`，但保留用户的搜索词、排序方式、视图模式和仍然存在的选中项。
5. 以路径为键复用已有时长/分辨率，避免一次普通刷新把已探测媒体信息恢复为 `null`。
6. 后端在成功列出用户打开的目录后，使用 `FsExt::fs_scope().allow_directory(...)` 精确授予该目录的监听权限；这避免为了监听而给 Webview 全盘文件权限。

拖入复制完成后也调用同一刷新函数，因此复制成功、手动新增、资源管理器重命名和删除的 UI 收敛到一条刷新链路。

#### 3. 卡片/列表内联 F2 重命名

重命名流程从 `window.prompt` 改为状态驱动的原位编辑：

1. 全局键盘处理器在当前仅选中一个视频时捕获 `F2`，确定当前选择锚点对应的文件。
2. 取文件名主干（不含扩展名）写入 `renameDraft`，并设置 `renamingPath`。
3. 对应网格卡片或列表名称单元渲染 `<input>`，扩展名作为不可编辑文本紧随其后。
4. 渲染后用 `requestAnimationFrame` 聚焦并全选输入框内容。
5. Enter 或输入框失焦调用后端 `rename_video`；Escape 设置取消标记，避免 Escape 导致的随后的 blur 又错误提交。
6. 后端继续复用单 STA FIFO `IFileOperation` 队列以及非法名称、保留设备名、重名检查；成功后刷新工作区并选中新路径。

这样既保留了 v0.1 的“只编辑主名、扩展名不变”约束，也符合 Windows 文件管理器的视觉和键盘反馈。

#### 4. 原生拖入链路与可视反馈

为避免使用窗口对象监听导致事件落点不一致，前端改为对当前 **Webview** 注册 `onDragDropEvent`：

1. `enter`/`over`：设置 `isExternalDropActive`，在工作区上层显示“松开以复制文件到当前工作区”。
2. `leave`：清除提示。
3. `drop`：记录拖入路径数，调用 `copy_videos_to_workspace`，复制完成后走 `refreshWorkspace`。
4. Tauri 配置显式设置 `dragDropEnabled: true`，并在 capability 中启用文件监听所需的 `fs:allow-watch`/`fs:allow-unwatch`。

配置项在 Webview 创建时才生效；仅前端热重载不会重建原生 Webview。因此修改该配置后必须完全退出旧 GUI，再重新启动 Tauri 进程。

### “禁止”鼠标指针的排障结论

用户反馈把视频拖到 GUI 时，鼠标仍显示禁止图标。该现象比前端事件更早发生：禁止光标表示 Windows OLE 拖放目标没有接受 `CF_HDROP` 文件对象，`drop` 回调、复制命令和目录刷新均不会执行。

排查过程：

1. 检查 `tauri.conf.json`：主窗口已显式开启 `dragDropEnabled`。
2. 检查 Tauri/Wry 的 Windows 实现：开启时会向 WebView2 子窗口注册 `IDropTarget`；其 `DragEnter` 只要能取得 `CF_HDROP`，便返回 `DROPEFFECT_COPY`，不会由前端过滤视频格式来决定光标。
3. 因而“禁止”不可能由扩展名白名单、`copy_videos_to_workspace`、前端事件订阅失败或刷新失败引起。
4. 检查应用 manifest，未请求管理员权限；再结合用户说明“PowerShell 默认以管理员权限启动”，确认实际原因是**进程完整性级别不一致**：管理员 PowerShell 启动的 GUI 是高完整性进程，而普通资源管理器是中等完整性进程。Windows UIPI 会禁止普通资源管理器向高完整性窗口发送拖放数据。

这是 Windows 安全边界，不应通过放宽应用文件权限绕过。应用新增 `is_running_as_administrator` 检测；若自身以管理员权限运行，启动后会显示明确说明，并写入 warning 日志，避免把系统拒绝误判为应用复制失败。

### 临时运行方案

不改变用户“默认管理员 PowerShell”的习惯时，可由普通权限的资源管理器进程启动已构建程序：

```powershell
chcp 65001 > $null
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::InputEncoding  = [System.Text.Encoding]::UTF8
$OutputEncoding           = [System.Text.Encoding]::UTF8
$PSDefaultParameterValues['*:Encoding'] = 'UTF8'

explorer.exe 'D:\Desktop\Coding\FileSweeper\src-tauri\target\debug\file-sweeper.exe'
```

不要直接在管理员终端运行 `file-sweeper.exe`、`npm run tauri dev` 或 `cargo tauri dev`；这些子进程会继承管理员令牌。开发模式若需要验证拖入，应从普通权限终端/IDE 启动一次 Tauri 进程。

### 验证状态

- `cargo fmt`、`cargo check`、`npx tsc --noEmit` 与 `npm run build` 均通过。
- `cargo test -- --list` 已通过；当前项目没有 Rust 单元测试，因此输出为 0 个测试。
- `tauri build --no-bundle --debug` 的尝试在本轮 60 秒执行窗口内超时，未产生配置错误输出；不把它视为打包验收通过。
- 仍需在**普通权限**的真实 Tauri GUI 中人工确认：资源管理器拖入时出现复制光标和放置提示、复制完成后列表自动新增、外部删除后列表消失、F2 的 Enter/失焦/Escape 三种路径，以及高 DPI 下拖放提示的位置。

## 23. v0.2 播放回退、转码定位与右栏媒体信息（2026-07-15）

### 解决方法

- 内嵌预览改为原文件直连优先：初次选中只创建本地 HTTP 原文件地址，不预先启动 FFmpeg 或 ffprobe；浏览器播放失败后才回退到 H.264/AAC fragmented MP4。
- 回退转码流支持定位。进度条拖动期间只更新预览时间，松手或键盘调整结束后以目标秒数作为输入前 `-ss` 重启 FFmpeg；前端保存流的绝对起点，避免时间轴错误回到 0。
- 右栏时长、宽度或高度缺失时，仅对当前选中视频调用 ffprobe；结果只合并回该视频，不扫描整个目录。

### 现象、原因与解决方法

| 问题 | 结论与处理 |
| :--- | :--- |
| 预先探测/转码会增加 I/O 与进程压力 | 大多数文件可由 WebView2 直连，因此采用真实失败后回退，而不是按扩展名预判。 |
| fragmented MP4 没有完整字节—时间索引 | 定位时重启一个带 `-ss` 的 FFmpeg；保持 accurate seek，以启动延迟换取更准确落点。 |
| `.mp4` 可能有声音却没有画面 | MP4 是容器而非编码保证；在 `loadedmetadata` 与 `canplay` 检查 `videoWidth`/`videoHeight`，尺寸为 0 时只回退一次。`mp4v` 样本已实播验证。 |
| `ffmpeg-next` | 当前项目仅分发 FFmpeg 可执行 sidecar，未分发 `libav*` 开发库、DLL 与头文件；继续使用 CLI sidecar，避免引入完整 FFI 分发链。 |

### 验证状态

FFmpeg sidecar 冒烟测试和 `mp4v` 无画面回退已通过；更多编码组合、快速切换和连续定位仍需桌面回归。

## 24. v0.2 框选与 Windows 文件拖出（2026-07-15）

### 解决方法

- 网格和列表均可从空白处拖出矩形框；无修饰键替换选择，Ctrl/Cmd/Shift 追加。
- 文件项左键移动超过阈值后启动系统拖放；若起点已选中则一次拖出全部选中视频，否则先只选中该项。
- 后端通过 Windows Shell `IDataObject` 与 `DoDragDrop` 协商 `DROPEFFECT_COPY`，不移动源文件。

### 现象、原因与解决方法

| 排障点 | 处理 |
| :--- | :--- |
| 浏览器原生图片拖拽抢占手势 | 卡片、列表行和缩略图全部阻止原生拖拽，超过阈值后才调用后端。 |
| 选择框相对鼠标上移 | 统一使用视口坐标绘制和命中，避免容器滚动坐标混入。 |
| 虚拟化导致跨屏项目不在 DOM | 自动滚动时只检查视口与 overscan，累计滚动途中命中的路径，不强制全量渲染。 |

### 验证状态

Windows Shell 文件拖出已在真实资源管理器复验成功；应用与资源管理器仍需保持相同 Windows 完整性级别。

## 25. v0.2 交互收口与列表性能（2026-07-15）

本节汇总第 23、24 节之后的实际联调结果。状态严格区分“已编码”“已构建”和“已在真实桌面环境验收”，不以编译成功代替功能完成。

### 完成度索引

| 范围 | 实现摘要 | 状态 | 验收证据或限制 |
| :--- | :--- | :--- | :--- |
| 原文件优先预览与 FFmpeg 回退 | 直连失败后转 H.264/AAC fragmented MP4；转码流可重启定位 | 🟡 | sidecar 冒烟和 `mp4v` 回退通过；仍需更多编码、快速切换和 seek 回归 |
| 右栏媒体信息 | 缺失时长或宽高时只探测当前选中单文件 | ✅ | 避免 5,000+ 文件目录的全量探测 |
| 右栏键盘控制 | 焦点提升到整个预览栏；空格播放/暂停，左右键 ±5 秒 | 🟡 | 按钮、输入框、下拉框保持自身键盘语义，待持续桌面回归 |
| 支持格式设置 | 受管扩展名列表：添加、删除、勾选、取消勾选 | ✅ | 前后端归一化，至少保留一种启用格式 |
| 列表虚拟滚动 | 网格、列表均用 `@tanstack/react-virtual` | ✅ | 切换列表不再读取全目录元数据；媒体排序才批量探测 |
| Windows 文件拖出 | Shell `IDataObject` / `DoDragDrop`，只协商 COPY | ✅ | 已在真实资源管理器复验 |

### 播放兼容性：容器不等于视频编码

用户提供的样本扩展名为 `.mp4`，外部播放器报告视频编码为 `mp4v`（MPEG-4 Part 2）、音频为 AAC。MP4 仅说明容器，不能推导视频轨一定是 H.264。外部播放器的 FFmpeg 解码器可正常解出 `mp4v`，而 WebView2 的 Chromium 媒体管线不保证能渲染该视频轨。

原回退条件只有 `<video onError>`。该样本中 AAC 可加载，媒体元素可能进入可播放状态并输出声音，却不把视频轨不可解码报告为整体错误，造成“有播放进度/音频但没有图像”。因此在 `loadedmetadata` 与 `canplay` 阶段读取 `videoWidth`、`videoHeight`；任一尺寸为 0 时记录事件阶段并只回退一次，避免重复创建转码会话。此策略已由用户实播验证。

### 选择、虚拟化与滚动

网格、列表均为虚拟滚动，未挂载项目没有 DOM。框选不能为了命中全目录而关闭虚拟化或枚举所有文件，否则会抵消大目录性能优化。当前策略是：普通框选仅命中视口与 overscan 中的元素；自动滚动开始后，把新命中路径加入手势集合，保证前一屏元素卸载后不会被错误取消选择。

选择框本身使用固定视口坐标，命中也使用 `getBoundingClientRect()`。此前选择框高出鼠标约 50px 的根因是容器滚动坐标与视口坐标混用；修复后滚动容器不会再影响框的视觉位置。

### 列表与网格性能

一次真实目录读取可以包含 5,000 余个视频。曾出现“切换到列表模式后连续调用 ffprobe”的日志，根因是将列表视图切换与媒体排序的元数据准备逻辑耦合。现在仅在按时长、分辨率排序时批量探测；右栏补全信息也严格只处理单个选中项。

网格采用固定最小卡片宽度和较小列间距。JavaScript 用“卡片最小宽度 + gap”计算列数阈值；列数确定后，CSS 以 `repeat(..., minmax(最小宽度, 1fr))` 均分剩余宽度。该组合既避免宽度接近下一列阈值时出现突兀的大右侧空隙，也不破坏虚拟行高度的稳定性。

### 右栏焦点与格式设置

播放器最初只有预览组件本身可获得焦点，点击“文件信息”后空格与左右键仍被工作区处理。现将焦点容器提升到整个 `.preview-panel`，通过 ref 暴露播放器的播放和定位接口；右栏非交互区域点击后聚焦，交互控件保持自己的默认语义。

格式白名单从逗号分隔自由文本改为受管列表，避免空项、重复项、大小写不一致和“删除最后一种格式”造成的无效配置。前端添加时统一小写并补充 `.`，拒绝 Windows 路径非法字符；后端保留最终归一化与至少一种启用格式校验。

## 26. v0.2 文件监听、全窗口框选与工作区菜单（2026-07-15）

### 现象与原因

运行日志出现 `Command watch not found`。这不是 capability 拒绝，而是 `tauri-plugin-fs` 的可选 Cargo `watch` 特性未启用。

### 解决方法

启用 Cargo `watch` 特性：

```toml
tauri-plugin-fs = { version = "2", features = ["watch"] }
```

既有 `fs:allow-watch` / `fs:allow-unwatch` 权限保持不变。完整重启 Tauri 后，监听应以 300ms 合并刷新处理外部新增、删除和重命名，并保留搜索、排序、视图、选择与已探测元数据。

### 框选与右键菜单

- 框选使用 pointer capture，因此可将鼠标移到整个应用窗口顶部或底部 56px 触发工作区滚动，而不局限于中栏容器。
- 中栏右键菜单改为主题化 WebView 浮层，文件项、列表表头、空白处和无文件工作区都可呼出。文件项提供默认应用打开、资源管理器定位、复制到、回收站和刷新；空白区域提供打开当前文件夹与刷新。
- 资源管理器定位由 `reveal_path` command 调用 Explorer；刷新继续走 `refreshWorkspace`，不把刷新误当作重新进入目录。

### 验证状态

1. 完整重启后确认日志出现“工作区监听已启动”，且不再出现 `Command watch not found`。
2. 在网格、列表、高 DPI 下，从标题栏/侧栏附近和窗口底部测试框选自动滚动、选择框位置与停止边界。
3. 分别在文件项、列表表头、空白区和无文件工作区回归右键菜单位置、关闭逻辑和每项操作。

## 27. 工作区排序与文件焦点记忆、虚拟网格定位与静默预览（2026-07-16）

### 目标与边界

为每个工作区记住排序字段、升降序和最后一个单选视频；再次进入同一目录后先恢复原排序，再恢复选中状态并滚动到该项。该功能不记忆播放器播放进度。用户可在“偏好设置 → 行为”通过“记忆工作区排序与文件焦点”统一开关控制两类状态；关闭后两者都不读取、不更新，原记录保留，重新开启后可继续使用。

### 持久化与切换时序

配置使用 `workspaceFocus: { [workspacePath]: { videoPath } }` 保存焦点，并新增 `workspaceSort: { [workspacePath]: { key, ascending } }` 保存排序。后端 `set_workspace_focus` 对工作区和文件路径进行规范化，要求二者均存在、视频为常规文件且其直接父目录就是工作区；`set_workspace_sort` 同样规范化工作区路径，并只接受前端支持的五种排序字段。两张表都带 serde 默认空值，旧配置无需迁移即可加载。既有 `rememberWorkspaceFocus` 配置字段继续作为联合开关，避免破坏已保存的用户偏好。

前端不会每次选择变化就立刻写磁盘，而是在单选焦点稳定 350ms 后保存；排序变化立即保存。目录切换时会确认当前焦点与排序均已进入持久化队列。实现中额外维护内存快照与共享的串行写入 Promise：

1. 内存快照在 IPC 发起前更新，因此“刚选中就切走又切回”的恢复读取不会依赖尚未刷新的 React 配置闭包。
2. 焦点和排序写入共用同一队列；切换目录和应用设置会等待正在进行的写入结束，避免多个命令各自读改写同一配置文件而互相覆盖。
3. 记录了前端的延迟保存、取消、写入成功/失败、恢复命中/未命中，以及后端的路径拒绝原因和状态结果，便于从日志定位状态停在哪一环。

### 排序与焦点必须联合恢复

只记忆文件路径、却不记忆该工作区的排序，会让重启后的可见顺序回到“创建日期降序”。虽然仍能找到同一个文件，虚拟列表中的索引和滚动位置却可能完全不同，用户看到的上下文也随之改变。因此排序与焦点使用同一个设置开关，并遵守固定恢复顺序：

1. 读取当前工作区的排序字段和升降序；无记录时使用创建日期降序。
2. 若按时长或分辨率排序，等待批量媒体探测结束；探测失败的文件也标记为已尝试，避免焦点恢复永久等待。
3. 等待网格视口挂载和列数稳定，再从最终 `visibleFiles` 顺序计算焦点索引并滚动。

应用初始化时必须把磁盘中读取到的联合开关显式传给首次工作区激活。仅先调用 `setConfig` 再依赖闭包默认参数，会在同一轮异步初始化中仍读取 React 初始配置，导致用户已关闭记忆时依然恢复状态。

### 虚拟网格恢复偏移：实际根因与修复

初版恢复使用 `gridColumns` 计算 `rowIndex = floor(videoIndex / gridColumns)` 后直接调用虚拟器定位。`gridColumns` 初始值为 1，真实列数由视口挂载后的 `ResizeObserver` 异步计算。在首次进入大网格时，恢复逻辑有机会按 1 列算行号并清除恢复标记；随后页面变为多列，却不会再次校正。因此焦点越靠后，滚动位置越偏下，实际焦点就越容易落在可视区上方。

修复流程：

1. 恢复前先等待网格视口 ref 挂载。
2. 以视口实际宽度、卡片宽度和横向 gap 重算应有列数；仅当它与 React 状态中的 `gridColumns` 一致时才计算目标行。
3. 焦点命中后以 `align: "start"` 定位；无记录、关闭记忆或记录文件已不存在时统一回到滚动顶部。
4. 网格虚拟行步长保持 **196px**：它由 180px 卡片高度和 16px 的纵向行间距组成。曾误将其改成 180px，虽然看似贴合卡片高度，却消除了卡片上下间距；虚拟器的 `start` 同时决定绝对定位，因此 196px 才是正确的行轨道尺寸。列表行高度固定为 40px，不存在同类误差。

这说明虚拟化布局中必须区分“可见元素高度”和“相邻项目起点间距”；恢复定位的行列计算还必须等待响应式布局稳定，不能使用初始化占位值。

### 恢复时不自动播放

焦点恢复会设置单选状态，原有“选择文件后自动播放”会因此启动预览。新增 `suppressPreviewAutoplay`：由工作区记忆恢复的选择在播放器创建时传入 `autoplay = false`，所以仍加载预览画面和控制器但保持暂停。用户之后以鼠标点击或上下方向键主动选择文件时清除该标记，原有自动播放设置立即恢复生效。

### 验证状态

- `cargo fmt --check`、`cargo check`、`npm run build`、`git diff --check` 均通过。
- 尚需在真实 Tauri GUI 中回归：不同窗口宽度（特别是首次进入时从 1 列变为多列）、网格/列表切换、五种排序及升降序的跨目录/重启恢复、关闭再开启联合记忆、目标文件被删除、媒体探测失败，以及恢复后预览保持暂停而主动选择仍自动播放。

## 28. v0.3 可验证模块化（2026-07-16）

### 目标

保持核心交互语义不变，将代码拆成可维护模块，并建立统一自动质量门槛，为配置、文件任务、缓存和性能版本提供稳定基线。

### 路线调整

原路线把模块拆分、Windows 剪贴板、性能优化和稳定版打磨集中在 v0.3/v1.0 两个阶段，无法为每类风险设置独立验收门。现新增 `ROADMAP.md`，将配置与文件系统、文件任务与剪贴板、缩略图缓存、大目录、播放稳定性和发布候选分别安排到 v0.4-v0.9；v0.3 负责建立自动验证、完成模块化并补齐工作区排序与焦点的状态连续性。

### 代码结构

- Rust 从单一 `main.rs` 拆出 `config_store`、`domain`、`workspace`、`sidecar`、`media_processing`、`media_stream`、`file_operations`、`windows_shell` 和 `menus`。
- React 从 `App.tsx` 拆出共享类型、格式化/日志工具、缩略图、播放器和目录树组件。
- 移除未使用的 SQL、Store、Zustand 和 Tailwind Vite 插件，继续使用现有手写 CSS 与 JSON 持久化。
- npm、Cargo 和 Tauri 应用版本统一为 `0.3.0`。

### 验证状态

- 新增领域规则单元测试，首批覆盖扩展名归一化、列表列约束、Windows 文件名、缩略图缓存身份和 FNV-1a 稳定向量。
- 新增 `npm run check`，串联前端构建、`cargo fmt --check`、严格 Clippy 和 Rust 测试。
- 新增 Windows CI 工作流；编译检查使用空 sidecar 占位文件，真实 sidecar 和媒体播放仍由桌面验收覆盖。

## 29. v0.3 Release 打包、NSIS 下载超时与 sidecar 静默启动（2026-07-16）

### 发行目标与最终产物

v0.3.0 使用 Tauri Release profile 和 NSIS `currentUser` 模式打包，安装器携带以下三个 x64 Windows sidecar：

```text
ffmpeg-x86_64-pc-windows-msvc.exe
ffprobe-x86_64-pc-windows-msvc.exe
ffmpegthumbnailer-x86_64-pc-windows-msvc.exe
```

正常打包命令为：

```powershell
npm run tauri -- build --bundles nsis
```

修复后的最终安装包位于：

```text
src-tauri/target/release/bundle/nsis/FileSweeper_0.3.0_x64-setup.exe
```

最终产物版本为 `0.3.0`，大小 `129,789,636` 字节，SHA-256 为：

```text
173A49E26F81B797644AE5AF34EBF197F60F6D80A71B7BBB7FC0E6ACB5C173F8
```

安装器和主程序当前均未签名，公开分发时 Windows SmartScreen 可能提示未知发布者；这不影响本地运行，但代码签名仍属于 v0.9 发布准备范围。

### 问题一：Release 已编译成功，但 NSIS 下载触发 `timeout: global`

#### 现象

首次执行 `npm run tauri -- build` 时，前端构建和 Rust Release 编译均成功，主程序也已生成：

```text
Finished `release` profile [optimized]
Built application at: src-tauri/target/release/file-sweeper.exe
```

失败发生在安装器阶段：

```text
Info Verifying NSIS package
Downloading https://github.com/tauri-apps/binary-releases/releases/download/nsis-3.11/nsis-3.11.zip
failed to bundle project: `timeout: global`
```

重试仍在同一步失败。对 URL 执行 HEAD 请求能返回 200，说明不是地址失效；实际下载速度很慢且 GitHub Release 连接曾被重置，Tauri 内置下载器先触发全局超时。

#### 根因定位

检查当前 `@tauri-apps/cli 2.11.4` 对应的官方 bundler 源码，确认 Windows NSIS 工具链的关键约束：

- NSIS 版本固定为 `3.11`。
- 工具缓存目录为 `%LOCALAPPDATA%\tauri\NSIS`。
- `nsis-3.11.zip` 的 SHA-1 必须为 `EF7FF767E5CBD9EDD22ADD3A32C9B8F4500BB10D`。
- 额外插件必须放在 `Plugins\x86-unicode\additional\nsis_tauri_utils.dll`。
- 插件 SHA-1 必须为 `75197FEE3C6A814FE035788D1C34EAD39349B860`。
- 只放压缩包没有作用；缓存目录缺少任一必需文件时，Tauri 会删除不完整目录并重新下载。

#### 解决方法

1. 从 NSIS 官方镜像下载 `nsis-3.11.zip`，避免不稳定的 GitHub Release 大文件连接。
2. 使用 `Get-FileHash -Algorithm SHA1` 校验压缩包；必须与 Tauri 源码中的固定哈希完全一致。
3. 将压缩包解压到 `%LOCALAPPDATA%\tauri`，把解压得到的 `nsis-3.11` 重命名为 `NSIS`。
4. 下载 Tauri 官方 `nsis_tauri_utils-v0.5.3` 插件并校验 SHA-1。
5. 将插件复制到：

   ```text
   %LOCALAPPDATA%\tauri\NSIS\Plugins\x86-unicode\additional\nsis_tauri_utils.dll
   ```

6. 确认以下文件至少存在后再重新打包：

   ```text
   %LOCALAPPDATA%\tauri\NSIS\makensis.exe
   %LOCALAPPDATA%\tauri\NSIS\Bin\makensis.exe
   %LOCALAPPDATA%\tauri\NSIS\Include\MUI2.nsh
   %LOCALAPPDATA%\tauri\NSIS\Plugins\x86-unicode\additional\nsis_tauri_utils.dll
   ```

本次曾在 Tauri 已生成 `src-tauri/target/release/nsis/x64/installer.nsi` 后直接调用校验过的 `makensis.exe`，证明最终脚本和资源可以正常压缩；缓存补齐后再次执行标准 Tauri 命令也成功生成安装包。因此后续构建应继续使用标准命令，手动运行 `makensis` 只作为“Release 和安装脚本都已生成、但 Tauri 最后一步意外中断”时的排障手段。

### 问题二：打包后的程序生成缩略图时反复闪出命令行窗口

#### 现象与根因

开发环境中不明显，但 Windows Release GUI 程序批量加载缩略图时，每次启动 `ffmpegthumbnailer`、`ffprobe` 或 FFmpeg 都可能短暂闪出控制台窗口。

项目原先只调用：

```rust
command.creation_flags(0x0000_4000); // BELOW_NORMAL_PRIORITY_CLASS
```

`BELOW_NORMAL_PRIORITY_CLASS` 只调整调度优先级，不会隐藏控制台。三个媒体工具都是控制台子系统程序；从不附带控制台的 Windows GUI 主进程启动它们时，必须显式传入 `CREATE_NO_WINDOW`，仅把 stdin/stdout/stderr 设为 null 或 pipe 并不能阻止窗口创建。

#### 解决方法

在 `sidecar.rs` 集中定义 Windows 创建标志：

```rust
const BELOW_NORMAL_PRIORITY_CLASS: u32 = 0x0000_4000;
const CREATE_NO_WINDOW: u32 = 0x0800_0000;

pub(super) const fn sidecar_creation_flags() -> u32 {
    BELOW_NORMAL_PRIORITY_CLASS | CREATE_NO_WINDOW
}
```

所有同步媒体命令继续通过 `configure_sidecar_command` 设置该组合标志，覆盖：

- `ffmpegthumbnailer` 百分比缩略图；
- `ffprobe` 时长、分辨率和编码探测；
- FFmpeg 缩略图回退；
- 超时清理时调用的 `taskkill`。

异步转码使用 `tokio::process::Command`，需要通过底层标准命令设置同一标志：

```rust
command
    .as_std_mut()
    .creation_flags(sidecar::sidecar_creation_flags());
```

不要只修缩略图入口，否则媒体排序、右栏元数据补全、播放回退或超时清理仍可能在其他时机闪窗。把创建标志集中为单一函数，可以避免同步与异步调用再次分叉。

### 打包体积为何从约 454 MiB 降到约 124 MiB

三个 sidecar 与主程序的未压缩安装内容约为 454 MiB。NSIS 本次使用 `lzma (compress whole)` 固实压缩，实际日志为：

```text
Install data:    476292010 bytes
Compressed data: 129753906 / 476460418 bytes
Total size:      129806646 / 476499334 bytes (27.2%)
```

`ffmpeg.exe` 和 `ffprobe.exe` 来自同一套 FFmpeg 构建，包含大量相同代码和数据；整包固实压缩能跨文件消除重复，因此安装包远小于安装后的磁盘占用。它没有减少安装体积，安装后仍会展开为接近半个 GiB。

当前保留 `ffmpegthumbnailer`：它能在一个进程内按 `25%/50%/75%/90%` 定位缩略图，而现有 FFmpeg 回退在非开头位置还需要先通过 ffprobe 获取时长。后续若要优化安装体积，应优先评估裁剪版或共享库版 FFmpeg，而不是为了少一个文件牺牲稳定的百分比截图能力。

### 验证状态

修复后执行：

```powershell
npm run check
npm run tauri -- build --bundles nsis
```

验证结果：

- TypeScript/Vite 生产构建通过。
- `cargo fmt --check` 通过。
- Clippy `-D warnings` 通过。
- 6 项 Rust 单元测试通过。
- Rust Release 编译和标准 Tauri NSIS 打包通过。
- 安装器版本、文件大小和 SHA-256 已重新核对。
- Git 仅保留 sidecar 静默启动与本节开发笔记改动，构建产物未进入版本控制。

仍需在真实安装版 GUI 中人工复验：清空一批缩略图缓存后打开大目录，确认缩略图生成、媒体排序、右栏信息探测、播放转码回退和 sidecar 超时清理全过程均不再出现控制台窗口。

## 30. v0.4 配置与文件系统正确性（2026-07-16）

### 目标

解决配置并发覆盖、损坏恢复、目录断连、`.nomedia` 祖先语义、监听竞态和转码中删除等文件系统正确性问题。

### 原因

- 多个命令分别读改写整份配置，晚到响应可能覆盖无关设置或工作区状态。
- 目录树展开、文件扫描和文件监听边界混杂，断连与快速切换时旧结果可能回写。
- 删除焦点视频前若不终止 FFmpeg，Windows 文件占用会使删除行为不确定。

### 解决方法

- `config.json` 升级为版本 2。后端使用单一 `ConfigStore` 串行合并更新；设置、收藏夹和最后工作区保存在 `config.json`，焦点与排序保存在独立的 `workspace-state.json`。旧配置中的两张工作区表会自动迁移，焦点落盘从物理上无法再覆盖设置。
- 前端不再用任何命令返回的整份配置直接替换 React 状态；音量、列表列、收藏夹和最后工作区响应只合并各自字段，避免晚到的旧响应回滚无关设置。
- 配置提交先写唯一临时文件并调用 `sync_all`，再使用 Windows `MoveFileExW` 的替换与写穿标志原子提交；迁移和损坏恢复复用同一路径。新增版本迁移、损坏备份恢复和并发更新测试。
- 目录树改用仅枚举子目录的命令，工作区命令只扫描直接文件；`.nomedia` 检查覆盖当前目录及全部祖先。
- 文件监听改由 Rust `notify` 持有单个非递归监听器，移除前端 `tauri-plugin-fs` 依赖与能力权限。扫描和刷新带代次编号，旧响应不能覆盖新工作区。
- 每 2.5 秒进行轻量目录可访问性探测。断连时清空视频与选择以停止预览，保留路径；恢复后自动重新扫描并重建监听。
- 删除当前焦点视频时，前端先暂停并取消待发预览，后端终止匹配路径的 FFmpeg 进程树并等待退出；3 秒内无法确认退出则取消删除，不进入回收站队列。

### v0.4 Release 产物

标准命令 `npm run tauri -- build --bundles nsis` 已成功生成：

```text
src-tauri/target/release/bundle/nsis/FileSweeper_0.4.0_x64-setup.exe
```

安装包大小为 `129,612,535` 字节，SHA-256：

```text
6D88757908F690317ECEA5DBCE8DFE784AD77BB814A36136F1626E44B61CDE40
```

安装包版本为 `0.4.0`，当前未签名。

### 验证状态

自动质量门槛和 Release 打包已通过；可移动盘、映射盘/网络路径断连恢复、异常退出和转码中删除仍需真实桌面矩阵验收。

## 31. v0.5 文件任务、系统剪贴板与全屏播放器控制（2026-07-16）

### 目标

实现 `ROADMAP.md` 的 v0.5：将同步文件队列升级为可观察、可取消的任务引擎，与 Windows 资源管理器双向交换复制/剪切文件，并补齐右栏播放器进入全屏后缺少控制 UI 的问题。

### 原因

- 原 STA 队列只能同步等待整批结果，无法显示任务编号、逐文件进度或取消未开始项目。
- 应用与资源管理器互操作需要同时提供 `CF_HDROP` 路径列表和 Preferred DropEffect 复制/移动意图。
- 原全屏根节点只有媒体画面，控制条位于全屏 DOM 树之外，因此进入全屏后不可见。

### 解决方法

#### 文件任务引擎

- 原单一 STA 队列保留串行写入边界，新增单调递增任务 ID、排队/运行/完成/取消状态、逐项目进度和逐文件结果。
- 前端通过 `file-task-progress` 接收快照，显示任务编号、进度、成功/跳过/失败统计及“取消”入口；取消在当前单文件操作完成后阻止剩余未开始项目。
- 任务注册表只保留有限数量的近期任务，避免长时间运行后状态无限增长。

#### 安全复制与移动

- 复制继续写入目标目录临时文件并校验长度，最终提交改为不带覆盖标志的 `MoveFileExW`，消除目标名称在检查后被外部占用时的覆盖竞态。
- 移动采用“复制并校验、提交目标、永久删除源文件”；若源删除失败，立即回滚目标。回滚也失败时逐文件结果同时保留目标路径和错误，源文件仍保持不变。
- 同目录移动跳过；同目录复制和其他同名冲突使用 `文件名 (n).扩展名`；不支持的文件逐项跳过，其余错误不会中止整个任务。

#### Windows 文件剪贴板

- `Ctrl+C`/`Ctrl+X` 将当前视频选择交给 `SHCreateDataObject` 创建的 Shell `IDataObject`，由 OLE 发布资源管理器可读取的宽字符 `CF_HDROP` 和 `CFSTR_SHELLIDLIST`，同时附加 Preferred DropEffect 的 COPY/MOVE 值。
- `Ctrl+V` 读取资源管理器或应用自身写入的 `CF_HDROP`，依据 Preferred DropEffect 创建复制或移动任务；缺少效果格式时默认复制。
- 工作区右键菜单同步增加复制、剪切、粘贴入口，并保留“复制到…”目录选择流程。
- 文件任务与剪贴板分别使用独立线程。剪贴板线程初始化 OLE STA，通过线程消息接收任务，并长期运行 `GetMessage`/`DispatchMessage`，保证 Explorer 可以跨进程调用仍然存活的 `IDataObject`。
- `OleSetClipboard` 发布后不立即调用 `OleFlushClipboard`，也不在发布返回前用 `OpenClipboard` 自读；当前数据对象保存在同一 STA 线程，直到下一次发布或进程退出。

#### 全屏播放器

- 全屏根节点从单独媒体画面提升为完整播放器，使播放、进度、音量、速率和退出全屏控件同时进入全屏树。
- 全屏控制条使用底部渐变和模糊背景悬浮；播放时无指针活动 2.2 秒后隐藏，指针移动后恢复；双击画面可切换全屏。

### Windows 文件剪贴板故障复盘

这次问题不是单一 API 调用错误，而是“数据格式正确”和“OLE 对象可被外部进程实时调用”两个层面被混为一谈。整个排障过程经历了原始 Win32 剪贴板、Shell 数据对象、立即物化、活对象轮询、格式对照和替换构造器等多轮实验，最终才定位到 STA 消息循环。

#### 初始现象与验收边界

- 从资源管理器复制或剪切文件后，应用能够读取 `CF_HDROP` 并在工作区创建复制/移动任务，说明“外部到应用”的读取链路已经打通。
- 从工作区复制或剪切文件后，应用日志显示写入成功，但资源管理器右键“粘贴”为灰色，`Ctrl+V` 也不能执行，说明“应用到外部”的发布链路未被 Explorer 接受。
- 同目录复制被错误地按同目录移动处理并直接跳过，没有生成递增命名副本。这是独立的文件任务分流错误，不能与剪贴板问题混在一起判断。
- 排障期间还出现过应用写入后系统其他程序也无法正常复制粘贴的严重退化，因此验收标准不仅是“应用报告成功”，还必须包含 Explorer 可粘贴、其他程序可重新接管剪贴板、重复复制不会阻塞系统。

#### 第一轮：手工写入 CF_HDROP

第一版使用传统 Win32 剪贴板接口：`OpenClipboard`、`EmptyClipboard` 和 `SetClipboardData`，手工构造宽字符 `DROPFILES`，同时注册并写入 Preferred DropEffect。为确认所有权，还把 Tauri 主 WebView HWND 传给后端，并记录请求所有者、实际所有者、序列号、格式 ID、文件数和载荷大小。

日志证明以下步骤都成功：

- 剪贴板能够打开和清空；
- `CF_HDROP` 与 Preferred DropEffect 均成功提交；
- 剪贴板所有者与请求 HWND 一致；
- 路径包含中文、空格、`[]`、`&` 和连字符时仍能完整回读。

但 Explorer 仍然禁用粘贴，并且一次 `EmptyClipboard` 调用出现约 5 秒阻塞。由此得到第一个重要结论：`SetClipboardData` 返回成功只证明 Windows 接受了两个静态格式，不等于 Explorer 接受了完整的 Shell 文件传输对象。特殊字符路径也没有造成二进制截断，不能继续把问题归因于 UTF-16 或 `DROPFILES::fWide`。

同一轮修正了同目录复制：只有移动到源目录时跳过，复制到源目录仍进入冲突命名流程，生成 `文件名 (1).扩展名`，并继续执行临时文件、长度校验和无覆盖提交。

#### 第二轮：改用 Shell IDataObject

为补齐 Shell 格式协商，移除手工写入链路，改由 `SHCreateShellItemArrayFromIDLists` 创建 Shell Item Array，再通过 `BHID_DataObject` 获取 `IDataObject`。应用调用 `IDataObject::SetData` 附加 Preferred DropEffect，随后使用 `OleSetClipboard` 发布。

这一版增加了写后回读：关闭发布阶段后重新打开剪贴板，用 `DragQueryFileW` 检查 `CF_HDROP` 文件数，并读取 Preferred DropEffect 的 DWORD。日志多次显示预期文件数与实际文件数相等、操作效果一致，但 Explorer 仍然无法粘贴。

这暴露了第二个关键误区：应用自己通过 `GetClipboardData` 读回数据，只覆盖静态 HGLOBAL 路径；Explorer 实际会通过 `OleGetClipboard` 获得 `IDataObject`，再调用 `QueryGetData`、`GetData` 和可能的结果回告接口。内部回读不是外部消费者验收，不能把它命名为“Explorer-compatible verified”。

#### 插曲：资源管理器定位错误

排查剪贴板时发现“在资源管理器中显示”跳到 Documents。原因不是剪贴板状态，而是把 `/select,<路径>` 拼成一个 `Command` 参数；路径包含空格时，Rust 会为整个参数加引号，Explorer 的旧式参数解析无法正确识别。

最终改用 `SHOpenFolderAndSelectItems`：把目标解析为绝对 PIDL，交给 Shell 打开父目录并选中项目。该问题与剪贴板主故障分开修复，避免错误地把“打开了错误目录”当成“目标目录不允许粘贴”。

#### 第三轮：与 Explorer 原生剪贴板对照

为了判断缺少什么，采集 Explorer 原生复制与应用复制的数据对象格式：

- Explorer 原生对象枚举出 13 种高层 Shell 格式；
- 应用对象只有 7 种：`Shell IDList Array`、Preferred DropEffect、`CF_HDROP`、`FileName`、`FileContents`、`FileNameW` 和 `FileGroupDescriptorW`；
- 原生对象额外包含 `DataObjectAttributes`、`DataObjectAttributesRequiringElevation`、`DropDescription`、`UIDisplayed`、`Shell Object Offsets` 和 `AsyncFlag`；
- 一次原生 COPY 观测到 Preferred DropEffect 为 `0x5`（COPY|LINK），应用当时使用标准 COPY `0x1`；
- 应用与 Explorer 均处于 Medium 完整性级别，测试目标为可写、在线且空间充足的 NTFS 磁盘，排除了 UIPI、管理员边界、离线磁盘和容量不足。

当时形成两个假设：一是立即调用 `OleFlushClipboard` 使动态格式丢失，二是 COPY 效果必须模仿 Explorer 写成 `0x5`。于是把文件任务与剪贴板拆成独立线程，尝试保持实时 `IDataObject`，并把 COPY 临时改为 COPY|LINK。

该实验没有解决粘贴，反而导致系统剪贴板被占住：应用复制后，系统其他程序也无法正常复制粘贴。序列号变化和所有者切换表明系统最终可以恢复，因此这不是“剪贴板缓存没有清理”，而是活的 OLE 对象仍由一个不能正确服务 COM 调用的线程持有。

#### 第四轮：立即 Flush 与失败回滚

为先恢复系统安全性，暂时回退为 `OleSetClipboard` 后立即 `OleFlushClipboard`，把实时对象物化并释放。现场出现 `CLIPBRD_E_CANT_OPEN (0x800401D0)`：发布已经成功，但 Flush 恰好与其他进程打开剪贴板发生争用。若此时直接返回，未完成物化的对象仍留在一个不处理 COM 消息的线程上，会再次阻塞系统交接。

临时安全修复为：

- Flush 最多重试 40 次，每次间隔 25ms；
- 约 1 秒后仍失败，则有限重试 `OleSetClipboard(NULL)`，主动撤销不完整对象；
- 日志区分发布失败、物化争用、回滚成功和回滚失败；
- 结束遗留调试进程后，用独立 STA 进程写入并读回文本，确认 Windows 剪贴板本身没有永久损坏。

这轮解决的是“失败后不能拖死系统”，并没有解决 Explorer 粘贴。它说明生命周期错误必须具备回滚，但重试次数再多也不能把错误的线程模型变成正确模型。

#### 第五轮：Flush 前后格式诊断

为了验证“Flush 剥离动态格式”的假设，在保持同一构造器不变的情况下增加三阶段诊断：

1. `source-before-publish`：枚举原始 Shell `IDataObject`；
2. `ole-after-publish-before-flush`：通过 `OleGetClipboard` 枚举发布后、Flush 前的对象；
3. `ole-after-flush`：再次通过 `OleGetClipboard` 枚举物化后的对象。

每个阶段都记录格式 ID、名称、`tymed`、`dwAspect`、`lindex`、目标设备，并对 `CF_HDROP`、`Shell IDList Array`、Preferred DropEffect 和 `DataObjectAttributes` 调用 `QueryGetData`。

实测三个阶段始终是相同的 7 种格式；`DataObjectAttributes` 从源对象阶段就不存在，而不是在 Flush 时消失。该结果推翻了“立即 Flush 导致格式数量从 13 降为 7”的直接因果解释。Explorer 原生 13 种与应用 7 种确实存在差异，但格式数量并不能解释为什么一个包含有效 `CF_HDROP` 的对象完全禁用粘贴。

#### 第六轮：切换 SHCreateDataObject

随后按官方参数约束增加 `SHCreateDataObject` 候选：

- 使用共同父目录的绝对 PIDL；
- 对每个绝对文件 PIDL 调用 `SHBindToParent`，取得恰好一层的相对子 PIDL；
- 所有文件必须共享父目录；
- `pdtInner` 保持 `NULL`；
- 先只枚举候选对象，不写入系统剪贴板。

候选对象默认提供 6 种格式，手动附加 Preferred DropEffect 后仍是与旧 `BHID_DataObject` 路径相同的 7 种。将生产剪贴板切换到 `SHCreateDataObject` 后，PIDL 创建、格式枚举、三阶段查询和静态回读全部成功，但 Explorer 仍然禁用粘贴。

这轮是有价值的负结果：问题不在 `SHCreateShellItemArrayFromIDLists` 与 `SHCreateDataObject` 的选择，也不在父/子 PIDL 的基本结构；继续替换数据对象工厂不会触及根因。

#### 最终定位：STA 存活不等于 STA 可调度

重新检查剪贴板线程后发现，线程虽然调用了 `OleInitialize` 并长期存活，但空闲时阻塞在 Rust `mpsc::Receiver::recv()`。先前的“活对象”实验只是使用短超时轮询并间歇处理消息，没有建立由 Windows 驱动的持续消息循环。

`OleSetClipboard` 放入的是 `IDataObject` 接口指针，不是已经完成的文件复制结果。Explorer 在另一个进程中粘贴时，需要通过 COM 回到创建对象的 STA，调用 `QueryGetData`/`GetData` 获取实际格式。STA 线程若停在 Rust channel 等待中，即使对象内存仍然存在，COM 调用也无法被及时分派。于是出现看似矛盾的状态：

- 应用本线程能枚举对象；
- 静态 `CF_HDROP` 可以自读；
- 日志中的文件数和效果值全部正确；
- Explorer 却无法完成真实调用，因此保持“粘贴”灰色；
- 若强行保持活对象，其他进程还可能因调用无法返回而无法接管剪贴板。

最终修复不是继续增加格式，而是重写任务唤醒方式：

1. 剪贴板线程启动后调用 `OleInitialize`，用 `PeekMessageW` 确保线程消息队列已经创建，并把线程 ID 返回给任务队列。
2. Rust channel 只负责保存任务；发送任务后调用 `PostThreadMessageW` 投递自定义 `WM_APP` 消息，不再依赖阻塞式 `recv()` 唤醒。
3. STA 线程长期阻塞在 `GetMessageW`。收到自定义任务消息时，用 `try_recv()` 排空当前任务；收到其他窗口/OLE 消息时执行 `TranslateMessage` 和 `DispatchMessageW`。
4. `SHCreateDataObject` 创建的数据对象通过线程局部存储保留在同一个 STA 上，下一次复制时再替换旧对象。
5. `OleSetClipboard` 成功后立即返回消息循环，不调用 `OleFlushClipboard`，也不执行可能占住剪贴板的同步 `OpenClipboard` 回读。
6. Preferred DropEffect 回到官方语义：COPY 使用 `0x1`，MOVE 使用 `0x2`。不再把 Explorer 某次内部对象的 `0x5` 当作外部实现必须模仿的协议要求。

修改后日志出现 `Clipboard STA message loop started` 和 `Live OLE clipboard object published and retained ... immediate_flush=false`，不再出现 `materialized and released` 或发布后的同步回读。真实桌面复验确认：从工作区复制文件后，Explorer 的粘贴恢复可用。最终根因由此闭环为“STA 线程没有持续调度 COM/OLE 消息”，而不是路径编码、剪贴板缓存、格式数量、PIDL 工厂或 DropEffect 位值。

#### 为什么保留纯 Rust

排障末期评估过增加小型 C++ helper：由独立 EXE 承担 COM STA、Shell 命令和消息循环，Rust 通过 JSON/管道发送路径。该方案能隔离崩溃，也更容易直接参考 Win32 C++ 示例，但会新增 CMake/MSVC 构建、sidecar 打包与签名、进程保活、IPC、日志汇合和退出清理；更重要的是，它仍然必须正确实现同一套 STA 与消息循环语义。

纯 Rust 方案在真实 Explorer 中通过后，C++ helper 不再提供足以抵消复杂度的收益。最终决定保留 `windows` crate 的实现，并把 Windows 互操作限制在 `windows_shell.rs` 与剪贴板队列边界。只有未来需要虚拟 Shell 对象、Explorer 原生 Context Menu verb，或出现无法隔离的 COM 崩溃时，才重新考虑 helper。

#### 经验总结

- “格式存在”与“数据对象可服务”是两个独立条件。OLE 剪贴板问题必须同时检查 FORMATETC/STGMEDIUM 和 COM apartment 调度。
- 自己写入后再自己读回不是端到端测试。真正验收必须由 Explorer 执行粘贴，并确认其他进程还能覆盖剪贴板。
- 日志中的 `S_OK` 只能证明当前 API 边界成功，不能向外推导整个协议成功；日志命名应避免在未经过外部消费者时使用“Explorer-compatible verified”。
- 对照实验必须明确哪些是事实、哪些是假设。13 对 7、`0x5` 对 `0x1` 都是真实差异，但最终均不是根因。
- 当系统级功能可能阻塞全局资源时，实验方案必须同时设计失败回滚；结束遗留进程可以恢复现场，但不能代替修正所有权和消息调度。
- 遇到 Rust COM 问题时，换成 C++并不会自动解决 apartment、生命周期和消息泵。先理解 Windows 协议，再决定语言边界。


#### 人话补充

> 也就是把承载 OLE 剪贴板对象的 STA 线程的“等待方式”，从 Rust 的 channel.recv() 替换成了 Windows 的 GetMessageW 消息泵。

- **为什么必须这么做？**
    - Rust 的 recv() 是与 Windows 消息队列无关的阻塞等待。它会让承载 OLE IDataObject 的 STA 线程停在 channel 上休眠，同时不会泵送 Windows 消息。等于线程被堵死了，在 recv() 上卡着睡觉。
    - 而 STA 中的跨线程、跨进程 COM 调用依赖 COM/OLE 的消息调度机制。Explorer 调用 IDataObject::GetData() 时，请求需要由该 STA 线程的消息泵调度执行。
    - 如果线程一直阻塞在 recv()，相关消息即使已经进入 Windows 消息队列，也无法被线程取出和分派，等于说 STA 线程这个“中间人”宕机了，无法处理消息。对象虽然仍然存活，应用本线程也能读取其中的格式，但 Explorer 无法及时获得响应。
    - Explorer 等待超时后，会认为当前数据对象不可用，因此保持“粘贴”禁用。
    - 改为 GetMessageW/DispatchMessageW 后，线程仍然可以休眠，但它等待的是 Windows 消息队列。COM/OLE 请求到达时，线程会被唤醒并完成调度。
    - Rust 任务则通过 PostThreadMessageW 投递自定义消息来唤醒同一个消息泵，再使用 try_recv() 从 channel 中取出实际任务。
    - 消息流转方式如下：
```
Rust 发送任务到 channel
        ↓
PostThreadMessageW 投递自定义 WM_APP 消息
        ↓
GetMessageW 被唤醒
        ↓
try_recv() 取出任务并发布 IDataObject
        ↓
继续 GetMessageW / DispatchMessageW
        ↓
响应 Explorer 的 COM/OLE 调用
```

## 31.5 补充一下剪贴板的调用详解：

可以把整个过程拆成“线程初始化”“应用复制”“Explorer 粘贴”三个阶段。

### 一、剪贴板 STA 线程初始化

应用启动时创建专用剪贴板线程：

```text
创建线程
  ↓
OleInitialize()，将线程初始化为 OLE STA
  ↓
PeekMessageW()，确保 Windows 为该线程建立消息队列
  ↓
GetCurrentThreadId()，把线程 ID 交给其他 Rust 线程
  ↓
进入 GetMessageW() 消息循环
```

此时 STA 线程大部分时间都在 `GetMessageW()` 中休眠。

这不是忙轮询。没有消息时不消耗 CPU；Windows 消息、OLE/COM 请求或自定义任务消息到达时，线程才会被唤醒。

### 二、用户在应用中复制文件

#### 1. Rust 把任务放入 channel

用户按下 `Ctrl+C` 后，调用方构造任务：

```rust
WriteClipboard {
    paths,
    operation: Copy,
    response,
}
```

然后调用：

```rust
clipboard_sender.send(task)
```

此时只发生了一件事：

> 任务被放进 Rust channel。

但是 STA 线程正在等待 Windows 消息，而不是等待 Rust channel，所以仅调用 `send()` 不能唤醒它。

#### 2. 用 Windows 消息唤醒 STA

调用方紧接着执行：

```rust
PostThreadMessageW(
    clipboard_thread_id,
    WM_CLIPBOARD_TASK,
    ...
)
```

这条自定义 `WM_APP` 消息被放入 STA 线程的 Windows 消息队列。

于是：

```text
STA 正在 GetMessageW() 中休眠
        ↓
WM_CLIPBOARD_TASK 到达
        ↓
GetMessageW() 返回
        ↓
STA 线程恢复执行
```

这里形成了一座桥：

```text
Rust channel：保存任务内容
Windows 消息：负责唤醒 STA
```

不能只使用其中一个：

- 只有 channel：STA 的 `GetMessageW()` 不会被唤醒。
- 只有消息：不方便在消息参数中安全传递复杂的 Rust 路径和响应通道。

#### 3. STA 从 channel 取出任务

消息循环识别到自定义消息后，不把它交给普通窗口处理，而是执行：

```rust
while let Ok(task) = clipboard_receiver.try_recv() {
    process_clipboard_operation(task);
}
```

这里必须使用 `try_recv()`，不能重新使用 `recv()`。

区别是：

```text
recv()
  没有任务时继续阻塞
  可能再次堵死 STA

try_recv()
  有任务就取出
  没有任务立即返回
  STA 可以迅速回到 Windows 消息循环
```

使用循环是为了一次处理已经排队的多个任务。多余的唤醒消息即使稍后到达，也只是发现 channel 为空，不会造成问题。

#### 4. 创建并发布 IDataObject

STA 线程处理任务：

```text
解析父目录绝对 PIDL
        ↓
解析文件相对子 PIDL
        ↓
SHCreateDataObject()
        ↓
附加 Preferred DropEffect
        ↓
OleSetClipboard()
```

其中：

- COPY 使用 `0x1`
- MOVE 使用 `0x2`
- `IDataObject` 提供 `CF_HDROP`、`Shell IDList Array` 等格式

`OleSetClipboard()` 放入剪贴板的核心不是已经完成的文件副本，而是指向这个 `IDataObject` 的接口。

#### 5. 保持 IDataObject 存活

发布成功后，数据对象被保存在 STA 线程的线程局部存储中：

```rust
LIVE_FILE_CLIPBOARD_OBJECT = Some(data_object);
```

这样任务函数返回后，对象不会被 Rust 自动释放。

同时不再立即调用：

```rust
OleFlushClipboard()
```

因此 Explorer 后续仍然可以向这个活对象请求数据。

#### 6. STA 返回消息循环

任务处理结束后：

```text
向调用方返回“发布成功”
        ↓
try_recv() 确认没有更多任务
        ↓
重新进入 GetMessageW()
```

此时最关键的状态是：

```text
IDataObject：仍然存活
STA 线程：正在泵送 Windows 消息
Explorer：可以随时请求数据
```

### 三、Explorer 执行粘贴

#### 1. Explorer 获取剪贴板对象

用户在 Explorer 中点击粘贴后，Explorer 通过 OLE 获取剪贴板数据对象。

跨进程调用在 Explorer 一侧表现为：

```text
QueryGetData(CF_HDROP)
GetData(CF_HDROP)
```

Explorer 拿到的通常是代理对象，真正的 `IDataObject` 仍然位于 FileSweeper 的 STA 线程。

#### 2. COM/OLE 请求到达 STA

COM/OLE 将跨进程请求调度到创建数据对象的 STA。

```text
Explorer 调用 IDataObject::GetData()
        ↓
COM/OLE 跨进程封送
        ↓
请求到达 STA 的消息调度机制
        ↓
GetMessageW() 被唤醒
        ↓
DispatchMessageW() 交给 OLE 隐藏窗口处理
        ↓
真正调用 IDataObject::GetData()
```

这就是之前失败的位置。

旧实现中 STA 停在：

```rust
clipboard_receiver.recv()
```

所以请求已经到达，但没人运行 Windows 消息泵。现在 STA 停在 `GetMessageW()`，请求到达后会被正常唤醒和分派。

#### 3. IDataObject 返回文件数据

Shell 数据对象返回 Explorer 请求的 `STGMEDIUM`，例如：

```text
CF_HDROP
  → 宽字符文件路径列表

Shell IDList Array
  → 父目录 PIDL + 相对子 PIDL

Preferred DropEffect
  → COPY 或 MOVE
```

Explorer 获取这些信息后，启用并执行实际的文件复制或移动。

操作结束时，Explorer还可能通过 `IDataObject::SetData()` 回告 `Performed DropEffect` 等结果。这类调用同样需要 STA 消息泵。

### 完整时序

```text
应用调用线程                 剪贴板 STA                    Explorer
     │                           │                            │
     │ send(task)                │                            │
     │──────────→ Rust channel   │                            │
     │                           │                            │
     │ PostThreadMessageW        │                            │
     │──────────────────────────→│                            │
     │                           │ GetMessageW 被唤醒          │
     │                           │ try_recv() 取出任务         │
     │                           │ SHCreateDataObject()       │
     │                           │ OleSetClipboard()          │
     │                           │ 保留 IDataObject           │
     │←──────── 发布成功 ────────│                            │
     │                           │ 回到 GetMessageW           │
     │                           │                            │
     │                           │       OleGetClipboard      │
     │                           │←───────────────────────────│
     │                           │                            │
     │                           │       GetData(CF_HDROP)    │
     │                           │←───────────────────────────│
     │                           │ GetMessageW 被唤醒          │
     │                           │ DispatchMessageW           │
     │                           │ IDataObject 返回数据        │
     │                           │───────────────────────────→│
     │                           │                            │
     │                           │       Explorer 执行复制     │
```

最核心的一句话是：

> channel 负责存放 Rust 任务，`PostThreadMessageW` 负责唤醒 STA，`GetMessageW/DispatchMessageW` 负责同时调度应用任务和 Explorer 的 COM/OLE 调用，而线程局部存储负责让 `IDataObject` 在整个过程中持续存活。

## 验证状态

- `npm run check` 通过，包括 TypeScript/Vite 构建、Rust 格式检查、Clippy `-D warnings` 和 17 个 Rust 测试。
- 自动测试覆盖同目录复制/移动分流、同名冲突递增和未开始任务取消；Windows 构建与严格 Clippy 覆盖最终 STA 消息循环和 Shell 数据对象调用。
- 真实桌面已确认资源管理器到应用的文件粘贴，以及应用到资源管理器的文件复制粘贴；后者是本次 STA 消息循环修复的最终端到端验收结果。
- 多文件剪切、跨卷移动、应用复制后退出再粘贴、连续覆盖剪贴板、“在资源管理器中显示”和全屏播放器控制仍需按发布矩阵继续回归。

## 32. v0.6 缩略图缓存闭环与工作区动效（2026-07-17）

### 发现问题

缩略图已经有 JPEG 文件和 JSON 索引，但 `thumbnailCacheGb` 只是设置项，磁盘缓存不会执行上限、孤儿清理或淘汰；损坏 `index.json` 时只会丢弃内存索引。与此同时，前端把 IPC 返回的 Base64 Data URL 放入全局 `Map`，长时间浏览大量目录后会持续占用 WebView 内存。媒体 sidecar 仍以 10 路固定并发运行，无法响应低核心设备、持续慢任务或连续失败。

工作区已经使用虚拟列表，虽然能在恢复工作区时定位焦点，却缺少用户随时回到焦点或列表开头的入口。浏览器的 `scrollTo({ behavior: "smooth" })` 不能指定 Material Design 3 的缓动曲线，因此不能满足统一动效语言的要求。

### 探索问题

曾比较两种前端图片释放方式：Blob URL 可显式 `revokeObjectURL`，但需要为组件挂载、失败重试和共享请求维护引用计数；既有 IPC 已返回 Data URL，而用户也明确不需要 Blob URL。最终保留现有传输协议，改为按字节估算的内存 LRU，避免引入额外生命周期复杂度。

磁盘索引损坏后无法从 JPEG 哈希文件名反推出原文件路径，因此不存在无损“扫描 JPEG 重建映射”的方案。可靠路径是先备份损坏索引、原子写入空索引、删除无索引 JPEG，再在后续可见视频请求中惰性重建。缓存维护还必须重新核对源文件大小、修改时间和 JPEG 是否存在，才能同时识别过期记录与孤儿文件。

对于滚动动效，虚拟器负责挂载可见行但不提供自定义 `cubic-bezier`。因此以当前滚动位置和目标偏移为边界，用 `requestAnimationFrame` 插值；每帧通过二分法反解 `cubic-bezier(0.2, 0, 0, 1)` 的 x 轴参数后计算 y 轴进度。这样能取消前一次动画、保持虚拟器收到正常滚动事件，并在系统要求“减少动态效果”时立即完成。

### 解决方法

1. 为 `ThumbnailIndexEntry` 写入最后访问时间。应用启动、缩略图批次完成和用户保存更小的上限时，统一校验索引、清理孤儿 JPEG、按 LRU 删除缓存文件并原子持久化索引。默认上限由 2 GiB 调整为 512 MiB，设置界面也改为 MB。
2. 损坏索引复制为 `index-corrupt-<timestamp>.json` 后重建空索引；新增测试覆盖备份、孤儿删除、LRU 淘汰和默认容量。
3. 前端 Data URL 缓存改为 64 MiB 有界 LRU。仍在屏幕上的 `<img>` 不受 Map 淘汰影响；再次进入视口时优先从磁盘 JPEG 读取，而非重新解码视频。
4. sidecar 并发上限根据可用 CPU 核数初始化为 1–6；重复失败或持续慢任务会降低并发，连续快速完成后再逐步恢复。批量请求大小调整为 12，但实际 sidecar 数量由许可池控制。
5. 工作区右下角增加仅显示图标的“回到焦点”和“回到开头”悬浮按钮，说明仅在悬停或键盘聚焦时显示。滚动、页面进入、工作区切换、预览栏、弹窗、遮罩和反馈卡片统一使用 MD3 Standard 曲线；不对虚拟列表的每一个文件卡片添加入场动画，避免大目录滚动抖动。

### 验证与结论

- `npm run check` 通过：TypeScript/Vite 构建、Rust 格式检查、Clippy `-D warnings` 与 20 个 Rust 测试均成功。
- 自动测试覆盖缓存容量的 LRU 淘汰、孤儿清理、损坏索引备份/重建及 512 MiB 默认值。
- 真实桌面已确认右下角两个图标按钮、MD3 曲线滚动及页面/弹窗动效有效。
- 冷缓存、热缓存、首屏、滚动与内存回落仍需在固定参考机、5,000+ 文件目录中记录量化基线；在此之前不能宣称性能验收完成。

## 33. 缓存清理误删正在生成的缩略图（2026-07-17）

### 发现问题

实测大目录生成缩略图时，日志反复出现“Unable to remove orphan thumbnail `*.tmp.jpg`（os error 32）”，随后部分任务又报“Unable to store the cached thumbnail（os error 2）”。该问题与开头的 Windows 剪贴板 `OpenClipboard` 失败无关，后者属于独立的 Shell 剪贴板占用问题。

### 探索问题

v0.6 新增的缓存维护按 `.jpg` 后缀枚举孤儿文件，而 sidecar 的输出临时文件命名为 `<hash>.tmp.jpg`。因此它也被识别成“无索引 JPEG”。设置保存、启动清理或批次维护若与 sidecar 写入重叠，会尝试删除仍被进程持有的临时文件；即使删除未成功，也可能干扰随后从临时文件到正式 JPEG 的提交。

仅排除临时文件可以消除直接误删，但正式 JPEG 在“原子改名完成、索引尚未写入”的极短窗口仍可能被并发维护当作孤儿。因此需要协调的是缓存目录提交边界，而不是扩大到整个视频解码过程；后者会把多路 sidecar 重新串行化。

### 解决方法

1. 缓存清理改为只识别严格的正式文件名：16 位十六进制哈希加 `.jpg`。`*.tmp.jpg` 不属于缓存条目，不会被孤儿清理触碰。
2. 将缩略图索引与缓存维护锁封装为同一状态。生成流程只在“删除旧正式文件、临时文件改名、写入索引”这一小段持锁；缓存维护按相同顺序先取得维护锁、再取得索引锁。这样避免死锁，也不限制并行解码。
3. 回归测试增加运行中的 `in-progress.tmp.jpg`：清理后它必须保留，同时仍验证正式孤儿删除与 LRU 淘汰。

### 验证与结论

- 本次修复应阻止维护逻辑再访问正在写入的临时 JPEG，并消除正式文件提交与索引写入之间的进程内竞态。
- 仍需使用此前的 G 盘大目录复验连续生成与保存设置同时发生的场景；自动测试负责覆盖命名判定和缓存清理边界，不能替代 Windows sidecar 的真实文件占用验证。

## 34. 后台媒体任务与前端结构过度设计收敛（2026-07-19）

### 发现问题

本轮不是单纯增加功能，而是对 v0.7-v0.8 期间逐渐累积的调度、缓存和前端结构进行一次完整复盘。实际检查后确认了以下问题：

1. **后台媒体调度机制超过真实需求**
   - 既有实现逐步加入了任务优先级、自适应并发、失败反馈、速度反馈、独立 JPEG 读取许可池和固定传输批次上限。
   - FileSweeper 是单应用、单用户桌面工具，不存在多个应用实例或多个租户之间争抢资源的场景。
   - 缩略图任务通常在几秒内完成，也没有必须中途撤销低优先级任务、立即抢占进程的实际需求。
   - 这些状态需要额外的锁、计数器、状态转换和测试，但没有带来对应的可见收益。

2. **逻辑队列长度与实际进程数量被混为一谈**
   - 前端曾通过固定批次长度限制一次保留的缩略图任务数量。
   - 这种限制确实减少了传输数量，但也会让已经进入视口附近、明确需要生成的任务在滚动或目录切换时丢失。
   - 真正需要限制的是同时运行的 sidecar 数量，而不是等待队列中的逻辑任务数量。
   - 如果许可池和工作线程数量有界，无上限逻辑队列不会直接产生无上限 FFmpeg/ffprobe 进程。

3. **滚动处理包含不必要的队列清理**
   - 快速滚动时清空待处理队列，会让刚刚进入过视口的项目失去生成机会。
   - 用户停止滚动后，同一项目可能重新入队，造成重复判断和不可预测的完成顺序。
   - 更符合使用习惯的语义应是“滚动时暂停发现新任务”，而不是“撤销已经发现的任务”。

4. **播放器实时路径存在被后台吞吐影响的风险**
   - 用户快速连续点击进度条，是为了立即观察每个位置的图像内容，而不是只关心最后一次点击。
   - 如果播放器 FFmpeg 与缩略图、ffprobe 共用许可池，后台满载时定位请求可能需要排队。
   - 如果把连续点击合并成最后一次请求，中间位置的画面将无法被即时确认，不符合操作目的。
   - 如果播放器也使用后台三次重试器，失败或启动缓慢时还会进一步延长响应时间。

5. **ffprobe 探测存在多条重复调用链**
   - 排序补全、右栏详情、缩略图 FFmpeg 回退和播放器时长获取分别维护了相似的 ffprobe 逻辑。
   - 同一个文件可能在一次应用会话中被重复启动 ffprobe 探测。
   - 旧缩略图索引只记录 JPEG，不记录已经成功获得的时长、宽度和高度，重启应用后仍需重新探测。

6. **缩略图和元数据虽然共享失效依据，却没有共享索引**
   - 两者都依赖源文件大小和修改时间判断缓存是否仍然有效。
   - JPEG 文件体积较大，需要 LRU 淘汰；时长和分辨率只有少量数字，不应随 JPEG 一起被删除。
   - 继续使用只有缩略图字段的索引，会迫使元数据另建存储或继续重复探测。

7. **复制与拖放入口的边界不够清楚**
   - 拖入、应用内粘贴、“复制到”、快捷键复制/剪切、右键复制/剪切和拖出看起来都属于“复制”，但实际责任不同。
   - 部分入口由应用执行真实文件写入，部分入口只是向 Explorer 提供 OLE/剪贴板数据对象。
   - 旧的直接复制 command 与文件任务引擎并存，使调用者难以确认冲突处理、取消和任务进度是否一致。

8. **菜单存在两套实现和事件桥**
   - 中央工作区已有主题化 WebView 菜单，同时还保留 Rust 原生菜单及事件转发。
   - 原生菜单需要额外 command、事件名、目标类型转换和前端监听，新增操作时需要同步修改两套结构。
   - 菜单视觉、主题和键盘行为也难以保持一致。

9. **前端仍保存后端配置的完整兜底副本**
   - 后端已经负责配置默认值、迁移和合法性验证，前端再维护 `fallbackConfig` 会形成第二套真源。
   - 新增或删除字段时，如果两端没有同步，应用可能在初始化失败后显示一组后端从未认可的值。
   - 并发数合法范围依赖当前机器的逻辑线程数，更不应由前端静态推测。

10. **实时日志推送与文件日志重复维护**
    - 日志已经写入文件和控制台，前端又通过 WebView 日志目标维护实时列表。
    - 这要求保留 `attachLogger`、实时状态、事件过滤和文件快照两条展示链路。
    - 日志面板不是持续打开的监控台，2-3 秒刷新一次已经足够，没有必要让关闭的面板持续接收事件。

11. **`App.tsx` 与 CSS 文件承担过多职责**
    - `App.tsx` 同时包含初始化、导航、工作区、预览、设置、日志、队列、菜单和文件任务界面。
    - 原样式文件还夹杂历史生成的 `--tw-*` 规则、重复选择器和重复媒体查询。
    - 这使一次局部修改需要在超长文件中定位，也增加了无意影响其他区域的风险。

12. **`ffmpeg-next-probe` 容易造成依赖误解**
    - `src-tauri/target/ffmpeg-next-probe` 位于构建输出目录，不属于当前源码模块或 Cargo 依赖。
    - 项目目前继续通过 sidecar 进程使用 FFmpeg，不需要引入 `ffmpeg-next` 或 `libav*` FFI。

### 探索问题

针对上述问题，先逐条追踪调用链和线程边界，再决定哪些机制可以合并、哪些必须保持独立。

1. **重新定义需要共享许可池的工作**
   - 应进入全局后台许可池：缩略图使用的 `ffmpegthumbnailer`、缩略图回退 FFmpeg、后台元数据 ffprobe。
   - 不进入许可池：播放器 FFmpeg、本地流服务、Windows OLE 拖出、剪贴板 STA、`IFileOperation` 文件任务线程、JPEG 文件读取。
   - 判断标准不是“是否与媒体有关”，而是“是否属于可等待的后台 sidecar 吞吐任务”。
   - 播放器需要低延迟；OLE/STA 需要持续处理 Windows 消息；文件操作有自己的串行安全边界，因此不能被后台许可池阻塞。

2. **比较动态并发与固定并发**
   - 动态并发需要根据耗时和失败次数改变容量，但单次慢任务可能来自机械硬盘、网络盘、损坏文件或编码复杂度，不能可靠代表系统应该永久降并发。
   - 动态降容后还需要定义何时恢复，状态容易受偶然样本影响。
   - 固定采用逻辑线程数的三分之二，既能为后台解码保留并行度，也给 UI、播放器和系统留出余量。
   - 用户仍可根据磁盘、CPU 和视频类型在设置中手动调整，因此不需要自动猜测。

3. **明确缩容语义**
   - 运行中把容量从较大值改为较小值时，强制终止已经运行的 sidecar 会带来临时文件、缓存提交和子进程回收问题。
   - 因此缩容只影响后续许可获取：已有任务自然结束，新的任务等到 `in_flight < limit` 后再开始。
   - 扩容则唤醒等待线程，使新增容量立即可用。

4. **拆分“逻辑队列”和“传输窗口”**
   - 逻辑队列使用 FIFO 保存所有已经发现的缩略图任务，并用路径 Set 去重。
   - 前端每次只按当前后台许可池容量向后端发送一批任务，避免一次 IPC 携带数千条路径。
   - 后端再次以许可池容量限制工作线程，最终运行中的 sidecar 数量仍受同一个上限约束。
   - 这使队列可以很长，但进程、线程和单次传输都保持有界。

5. **验证重试是否持有许可**
   - 如果一个失败任务在 100ms 重试等待期间仍持有许可，其他正常文件无法利用该容量。
   - 因此每次尝试遵循“获取许可 → 执行一次 → 释放许可 → 判断是否等待重试”的顺序。
   - “三次重试”按用户最终定义解释为总共尝试三次，而不是首次加三次重试共四次。
   - ffmpegthumbnailer 用尽三次预算后才进入一次 FFmpeg 回退；播放器不套用这个重试器。

6. **设计媒体缓存条目**
   - 索引继续使用原来的 `index.json`，避免创建第二套缓存目录和维护流程。
   - 每项首先保存源文件大小与修改时间，作为缩略图和元数据共同的有效性指纹。
   - `thumbnail` 与 `metadata` 都是独立可选字段：前者保存取帧位置、JPEG 文件名和最后访问时间，后者保存可选时长、宽度和高度。
   - 文件指纹变化时整个条目失效；仅执行 LRU 淘汰时只清除缩略图字段和 JPEG。
   - 旧索引没有版本和元数据字段，需要在读取时迁移，且继续复用损坏备份和原子写入机制。

7. **统一 ffprobe 的复用顺序**
   - 第一步读取源文件元数据并查询媒体缓存索引。
   - 缓存命中则直接返回，不启动任何 sidecar。
   - 缓存缺失才通过后台许可池调用统一 `probe_media_info`。
   - 成功后立即写入缓存，而不是等待应用退出或下一轮批处理。
   - 播放器获取时长时优先使用前端已知时长和缓存；确实缺失时允许使用独立的播放器探测路径，不等待后台池。

8. **区分两类复制操作**
   - 应用实际写文件：拖入、应用内粘贴、“复制到”都进入 `start_file_task`，统一获得任务 ID、状态、冲突命名、取消和结果汇总。
   - Explorer 实际写文件：拖出、复制/剪切到系统剪贴板只负责创建 Shell 数据对象，目标位置和最终写入由 Explorer 决定。
   - 后一类必须保留 OLE/STA 适配层，不能为了表面上的“统一”破坏 Windows Shell 互操作。

9. **选择菜单实现**
   - WebView 菜单已经能使用应用主题和现有图标，扩展操作只需要修改一处动作定义。
   - 删除 Rust 原生菜单后，需要补齐原生菜单默认提供的可用性行为：视口边缘定位、外部点击关闭、Escape、上下方向键、Enter、进入菜单焦点和关闭后恢复焦点。
   - 菜单目标统一为目录、工作区、单个/多个视频，再根据目标隐藏不适用的操作。

10. **选择日志刷新方式**
    - 每次先读文件、再为同一内容二次读文件计算哈希没有必要。
    - 后端一次读取日志文件后，对最多 1 MiB 的返回快照计算 FNV-1a 64 位哈希，并用字符串返回以避开 JavaScript 整数精度限制。
    - 哈希相同时只返回路径、大小、哈希和 `changed=false`；不回传正文。
    - 哈希变化时使用同一次读取的字节构造正文，不再执行第二次磁盘读取。

11. **评估 Tailwind**
    - 当前界面已经拥有稳定的语义类名、主题变量和区域布局，引入 Tailwind 意味着大面积重写 JSX 与样式。
    - 本轮目标是减少重复和降低维护成本，不是重新设计视觉系统。
    - 因此保留现有 CSS，删除无使用者的 Tailwind 生成残留，并按界面区域拆分文件。

### 解决问题

本轮按后端资源边界、缓存、前端行为和结构清理四组完成修改。

#### 1. 配置与固定许可池

- 将配置版本升级到 v3，新增 `backgroundSidecarConcurrency`。
- 默认值由后端按 `max(1, floor(CPU 逻辑线程数 × 2 / 3))` 计算。
- 合法范围由后端设置为 `1..=CPU 逻辑线程数`：
  - 旧配置缺少字段时自动补默认值；
  - 配置中的 0 自动夹紧到 1；
  - 从高核心机器迁移到低核心机器时自动夹紧到当前逻辑线程数。
- `ApplicationState` 返回完整配置和并发上下限，前端设置界面只负责显示。
- 保存设置后立即更新运行中许可池：
  - 扩容后唤醒等待任务；
  - 缩容不取消已运行任务；
  - 后续任务等待实际运行数量降到新限制以下。
- 删除速度、失败次数、动态升降状态和独立缩略图读取许可池。

#### 2. 后台 sidecar 与重试流程

- 缩略图生成和后台元数据探测共用同一个 `MediaSidecarPermits`。
- 每次 sidecar 执行前获取许可，进程结束或启动失败后立即释放。
- ffprobe 最多总尝试三次，仍失败则返回失败并保留文件列表项。
- ffmpegthumbnailer 最多总尝试三次：
  - 每次失败后先释放许可；
  - 重试等待期间其他任务可以获取该许可；
  - 三次均失败后执行一次现有 FFmpeg 回退；
  - FFmpeg 回退也受后台许可池限制。
- 播放器 FFmpeg、OLE、剪贴板 STA 和文件操作线程不使用这一重试器或许可池。

#### 3. 缩略图队列与滚动行为

- 前端逻辑队列改为无上限 FIFO，路径 Set 负责去重。
- 移除固定 12 项的任务总量限制。
- 每次传输窗口使用当前 `backgroundSidecarConcurrency`，只控制一批发送给后端的路径数量。
- 后端工作线程数同样不超过许可池当前容量，实际 sidecar 数始终受全局上限控制。
- 滚动开始后：
  - 不清空待处理队列；
  - 不停止已运行任务；
  - 仅禁止当前滚动过程中发现的新项目入队。
- 停止滚动 180ms 后触发一次可见区域复查并恢复入队。
- 切换工作区时保留旧队列：
  - 旧任务继续完成并写入磁盘缓存；
  - 只有路径仍属于当前工作区时才更新当前 UI；
  - 防止旧工作区结果覆盖新工作区界面。

#### 4. 媒体缓存与统一探测

- 将 `ThumbnailIndex` 原位迁移为版本化 `MediaCacheIndex`，继续使用原来的 `index.json`。
- 新条目包括：
  - 源文件大小；
  - 源文件修改时间；
  - 可选 `thumbnail`；
  - 可选 `metadata`。
- `thumbnail` 保存取帧位置、JPEG 文件名和最后访问时间。
- `metadata` 保存可选的时长、宽度和高度。
- 旧索引读取后自动迁移，未知旧字段由 Serde 忽略，不要求用户清空数据。
- 第一次成功探测元数据后立即原子持久化。
- 工作区扫描直接从缓存索引填充时长和分辨率，减少排序和右栏详情触发的 sidecar。
- 文件大小或修改时间变化时，缩略图和元数据同时失效。
- LRU 淘汰只删除 JPEG 和 `thumbnail` 字段；如果条目仍有 `metadata`，继续保留该条目。
- 统一使用 `probe_media_info`，由缓存优先包装流程复用于批量元数据、右栏详情、排序补全、缩略图回退和播放器时长获取。

#### 5. 播放器响应路径

- 播放器保留单独的 FFmpeg 流，不进入后台 sidecar 许可池。
- 快速点击进度条时不做防抖合并，每次点击都发起对应位置的请求。
- 新定位请求立即替换旧转码进程，防止旧画面随后覆盖最新请求。
- 优先复用前端已知时长或持久化元数据，减少定位前的 ffprobe 等待。
- 使用既有低延迟 FFmpeg 参数和短分片策略，后台缩略图/元数据满载时仍不会因为许可池而阻塞播放器启动。

#### 6. 文件复制、拖放和剪贴板

- 删除未使用的旧直接复制 command、结果类型和纯转发包装。
- 拖入文件统一调用 `start_file_task`，由文件任务引擎实际复制。
- “复制到……”统一选择目标目录后调用 `start_file_task`。
- 应用内粘贴通过读取系统文件剪贴板创建同一类文件任务。
- 快捷键和右键中的复制/剪切统一写入 Windows Shell 文件剪贴板。
- 拖出继续使用 OLE 数据对象；资源管理器决定目标目录并执行实际操作。
- 保留 OLE/STA 与文件任务两条底层路线，是因为责任边界不同，而不是遗留重复代码。

#### 7. 统一主题化菜单

- 删除 `menus.rs`、`show_file_context_menu` 和原生菜单事件处理。
- 删除只服务于原生菜单的 `copy-to-request`、`workspace-refresh-request`、`files-recycled` 等事件桥。
- 新建统一 WebView 菜单组件，目标类型为目录、工作区和视频集合。
- 根据目标提供适用操作：打开、定位、复制、剪切、粘贴、复制到、回收站和刷新。
- 补齐交互细节：
  - 横纵坐标按视口边缘夹紧；
  - 点击菜单外部关闭；
  - Escape 关闭；
  - 上下方向键循环切换菜单项；
  - Enter 使用按钮原生激活行为；
  - 打开后聚焦第一个菜单项；
  - 关闭或执行动作后恢复先前焦点。

#### 8. 外部播放器入口

- 删除 `openUnsupportedExternally` 配置字段和设置界面开关。
- 内嵌播放器正常、加载中或空闲时不显示外部打开按钮。
- 只有播放器进入错误状态时，错误区域才显示“使用外部播放器打开”。
- WebView 文件菜单中的“使用默认应用打开”仍属于明确的用户文件操作，不重新引入播放器偏好。

#### 9. 文件日志轮询

- Rust 日志目标删除 WebView，只保留文件和控制台。
- 前端删除 `attachLogger`、`liveLogs` 和实时日志列表。
- 新增 `poll_log_file(previousHash, maxBytes)`：
  - 最多接受 1 MiB 快照；
  - 计算 FNV-1a 64 位哈希；
  - 哈希按字符串返回；
  - 哈希相同不返回正文；
  - 哈希变化时复用同一次读取结果生成正文。
- 日志面板打开时立即读取一次，此后每 2500ms 串行轮询。
- 面板关闭、页面隐藏或上一次请求尚未结束时不发起新请求。
- 手动刷新忽略旧哈希，确保能够重新获得完整快照。

#### 10. 后端配置单一真源

- 删除前端完整 `fallbackConfig`。
- 应用启动阶段先显示加载壳，等待后端 `load_application_state`。
- 设置值、默认值、迁移结果和并发范围全部使用后端返回值。
- TypeScript 仅保留接口类型和不属于配置的 UI 设计常量。
- 初始化失败时显示明确错误，不使用可能已过期的前端配置继续运行。

#### 11. TS 与 CSS 模块拆分

- `App.tsx` 保留后端初始化、全局配置、工作区/视频协调和三栏布局。
- 拆出的 TS 模块包括：
  - `features/navigation/NavigationPanel.tsx`；
  - `features/workspace/WorkspaceToolbar.tsx`；
  - `features/preview/FileDetails.tsx`；
  - `features/settings/SettingsDialog.tsx`；
  - `features/settings/LogDialog.tsx`；
  - `components/ThemedContextMenu.tsx`。
- CSS 不引入 Tailwind，入口文件只保留主题变量、重置、标题栏和共享规则。
- 区域样式拆为：
  - `navigation.css`；
  - `workspace.css`；
  - `preview.css`；
  - `settings.css`。
- 删除未使用的 `--tw-*` 生成规则、重复媒体查询、实时日志样式和重复选择器。

#### 12. 后端包装与测试文件

- 删除 `main.rs` 中不承担以下责任的纯转发函数：
  - Tauri command 边界；
  - 路径校验；
  - Tauri State 提取；
  - `spawn_blocking` 等线程边界；
  - 必要的状态转换。
- sidecar、媒体处理、工作区和配置模块由调用者直接复用公开到父模块的实现。
- 本轮新增的结构回归全部放入独立 `src-tauri/src/regression_tests.rs`。
- 既有业务模块中的历史单元测试保持不动；没有把新增回归继续塞入 `main.rs` 或媒体业务函数旁边。
- `src-tauri/target/ffmpeg-next-probe` 不进入源码、不加入 Cargo 依赖，也不作为本轮清理目标处理。

### 验证

#### 1. 自动质量门槛

- 执行统一入口 `npm run check`，结果全部通过。
- 前端部分：
  - TypeScript `tsc --noEmit` 通过；
  - Vite 生产构建通过；
  - 新拆分的 TS/CSS 模块均能被正确解析和打包。
- Rust 部分：
  - `cargo fmt --check` 通过；
  - `cargo clippy --all-targets -- -D warnings` 通过；
  - 32 个 Rust 测试全部通过，无忽略和失败。

#### 2. 独立回归测试覆盖

- 默认并发公式：验证 1、2、4、8、32 个逻辑线程下的三分之二结果。
- 配置夹紧：验证 0 被夹紧到 1，过大值被夹紧到本机逻辑线程数。
- 运行时容量调整：验证持有许可时缩容不会取消已有任务，随后仍可扩容。
- 并发上限：同时启动多组线程，观测到的最大持有许可数不超过设置值。
- 重试预算：验证成功发生在第三次时总调用次数恰好为 3。
- 重试间释放：在第一次失败和第二次尝试之间，由另一任务成功取得同一许可。
- 旧索引迁移：无版本旧缩略图条目能够迁移到新媒体缓存索引。
- 损坏恢复：非法 JSON 会被备份并重建为空索引。
- LRU 语义：淘汰 JPEG 后元数据字段仍然保留，正在生成的 `.tmp.jpg` 不被孤儿清理删除。
- 日志哈希：覆盖首次读取、内容不变、追加、限制尾部大小和截断后的变化。
- 转码所有权：所有者释放后活动转码登记能够被清除。

#### 3. 静态清理检查

- 检索确认不存在 `openUnsupportedExternally`。
- 检索确认不存在 `show_file_context_menu` 和原生菜单事件桥。
- 检索确认不存在 `attachLogger`、`liveLogs` 和旧日志读取 command。
- 检索确认不存在固定缩略图批次上限常量。
- 检索确认不存在独立 `ThumbnailReadPermits`。
- 检索确认 CSS 中不再存在 `--tw-*` 生成变量。
- `git diff --check` 通过，没有空白错误。

#### 4. 仍需真实桌面验收的部分

- **大目录队列**：数千视频连续快速滚动，确认队列不丢失、不重复，停止滚动后继续补齐。
- **进程峰值**：在任务管理器中确认后台 ffprobe、ffmpegthumbnailer 和回退 FFmpeg 总数不超过设置值。
- **播放器实时性**：缩略图与元数据满载时快速连续点击进度条，确认每次点击都及时给出对应画面且无孤儿 FFmpeg。
- **Explorer 互操作**：复验拖入、拖出、复制、剪切、粘贴、移动、同名冲突和取消。
- **STA/OLE 消息循环**：确认后台 sidecar 满载不会阻塞 Explorer 获取剪贴板数据对象。
- **菜单**：复验边缘定位、外部点击、Escape、方向键、Enter、焦点进入与恢复。
- **日志**：确认面板每 2.5 秒轮询、哈希不变时不刷新正文、关闭或页面隐藏后停止请求。
- **视觉**：复验深浅主题、不同 DPI、左右栏缩放、网格/列表和设置界面无明显回归。

### 总结

本轮重构得到的结论可以归纳为以下几点：

1. **逻辑任务数量与实际进程数量必须分开管理**
   - 队列负责完整保留已经发现的工作；
   - 许可池负责限制真正昂贵的后台进程；
   - 不再通过丢弃任务来控制并发。

2. **后台吞吐与实时交互必须采用不同资源边界**
   - 缩略图和元数据可以等待许可；
   - 播放器定位不能等待后台任务；
   - OLE/STA 不能被普通工作池阻塞。

3. **缓存应按数据生命周期拆字段，而不是拆成重复系统**
   - 缩略图和元数据共享源文件指纹；
   - JPEG 可以被 LRU 淘汰；
   - 小型元数据应继续保留并复用。

4. **所谓“统一”应统一业务入口，而不是抹平平台边界**
   - 应用实际执行的复制统一到文件任务引擎；
   - Explorer 执行的复制继续使用 OLE/STA；
   - 两者职责清楚比强行共用一条底层函数更重要。

5. **后端配置、菜单、日志和样式都应只有一个主要真源**
   - 配置默认值和范围以后端为准；
   - 菜单只保留 WebView 主题化实现；
   - 日志只以文件为展示数据源；
   - CSS 保留现有语义系统，不额外引入 Tailwind。

6. **自动检查只能证明结构和核心逻辑闭合**
   - 当前 32 个测试和完整质量门槛均已通过；
   - Windows Shell、WebView 播放、真实编码和大目录资源占用仍必须完成桌面矩阵；
   - 在这些人工验收完成前，本轮状态应表述为“实现完成、自动验证通过、真实桌面验收待完成”。

## 35. 全业务链路日志补全与静默回退治理（2026-07-19）

### 发现问题

#### 1. 已有日志只能说明“应用还在运行”，不能还原业务过程

- 原有输出主要集中在少数媒体处理和文件操作入口。
- 工作区切换、配置保存、缩略图队列、元数据缓存、播放器定位、菜单动作等业务缺少一致的开始、结束和结果日志。
- 多个异步操作只在最终失败时通知 UI，没有记录请求对象、请求代次、完成数量或实际采用的处理路线。
- 当用户反馈“偶尔没有反应”时，日志无法区分：
  - 请求根本没有发起；
  - 请求已经过期并被主动忽略；
  - 后端失败；
  - 后端已经自动回退且 UI 最终看似正常；
  - 结果已经生成，但当前工作区不再需要更新 UI。

#### 2. 失败与回退被合并后，最关键的诊断信息丢失

- ffprobe、ffmpegthumbnailer 和 FFmpeg 回退属于多阶段处理；只记录最终失败，会丢失具体失败阶段和尝试次数。
- 缩略图时间点无法探测时会使用默认时间点继续生成；如果不记录，错误媒体、ffprobe 失效和正常默认行为无法区分。
- 系统文件剪贴板读取不到 `Preferred DropEffect` 时会按复制处理；如果不记录，剪切被当作复制的问题只能从结果猜测。
- 缓存索引读取失败后会备份并重建；如果只记录“缓存未命中”，索引损坏会长期被误认为普通缓存失效。
- 播放器可能从直接播放切换到 FFmpeg 转码；如果回退过程无日志，用户只能看到短暂等待或最终画面，无法知道直接播放路线已经失败。

#### 3. 启动阶段日志存在时序空窗

- 配置文件和媒体缓存索引在 Tauri 日志插件安装前读取。
- 这段代码即使调用 `log::warn!` 或 `log::error!`，也不能保证内容已经写入应用日志文件。
- 配置损坏恢复、工作区状态损坏恢复和媒体缓存索引重建恰好都发生在这个阶段。
- 因此仅增加日志宏仍然会形成“源码看似有日志，用户日志文件实际没有”的静默失败。

#### 4. 不能通过无差别打印解决覆盖不足

- 工作区可访问性检查和日志轮询每 2.5 秒触发一次，成功日志会持续刷屏。
- 日志面板以日志文件哈希判断是否需要刷新；如果每次成功轮询本身再写一条日志，就会改变文件哈希，造成永久自激刷新。
- 缩略图、元数据和快速定位都可能在短时间内产生大量任务；日志必须能串起任务，但不能把正常守卫条件全部提升为警告。
- 文件路径属于诊断所需上下文，但日志中不应记录媒体内容、剪贴板二进制数据或 sidecar 完整输出正文。

### 探索问题

#### 1. 按业务生命周期而不是按函数数量检查日志

- 将业务链路按下列阶段逐一检查：
  1. 请求进入；
  2. 参数校验；
  3. 状态或队列登记；
  4. 资源获取；
  5. 外部进程或系统 API 调用；
  6. 重试、回退或过期淘汰；
  7. 成功结果；
  8. 失败结果与清理。
- 这种检查方式发现了若干“函数有日志但业务仍断链”的情况，例如：
  - ffmpegthumbnailer 有进程失败日志，但没有明确写出“释放许可后准备第几次重试”；
  - 工作区扫描有完成日志，但前端丢弃旧响应时没有说明原因；
  - 缓存重建有警告，但发生在日志插件初始化之前；
  - 剪贴板最终回退到复制有提示，但没有保留 Windows API 的原始 HRESULT。

#### 2. 为失败日志保留可操作上下文

- sidecar 失败至少需要：任务标签、媒体路径、当前尝试次数、总尝试上限和错误原因。
- 并发相关问题至少需要：许可池容量、当前占用数、等待或释放发生在哪个阶段。
- 批处理至少需要：请求项数、去重后项数、成功数、失败数和工作线程异常。
- 缓存问题至少需要：源路径、索引路径、命中或失效原因、备份路径和重建结果。
- 播放器定位至少需要：媒体路径、目标秒数、直接流或转码流路线、旧进程停止结果和新进程登记结果。
- 文件任务至少需要：任务编号、操作类型、源数量、目标目录、逐项结果、取消和终态。
- 前端异步竞态至少需要：被忽略的是成功响应还是失败响应，以及为什么它已经过期。

#### 3. 明确日志级别

- `error`：当前业务已经失败，或关键系统能力不可用。
- `warn`：发生自动回退、部分项目跳过、清理失败、状态文件恢复或结果可能与用户预期不同。
- `info`：用户可感知业务的开始、完成、路线切换和批次汇总。
- `debug`：缓存命中、队列长度、焦点移动、菜单定位、事件代次和高频但有诊断价值的内部状态。
- 不把正常的“没有可处理项”“组件未选中”“守卫条件返回”一律记为警告，避免真正异常被淹没。

#### 4. 检查日志本身的失败路线

- 前端 `writeClientLog` 调用 Tauri 日志插件也可能失败。
- 不能在日志失败回调中再次调用同一个日志函数，否则会递归。
- 最终采用浏览器控制台作为独立兜底，并附带原始错误对象。
- 日志文件轮询成功不写日志；只有读取失败才由后端记录，从机制上避免哈希自激。

### 解决问题

#### 1. 后端命令与配置业务

- 为应用状态加载、管理员状态检测、目录读取、工作区扫描和可访问性异常补充入口与结果日志。
- 配置保存记录关键字段、许可池调整、缓存维护结果和持久化失败。
- 音量、静音、列表列配置、最近工作区、工作区焦点、排序和收藏变更均记录请求、成功和失败。
- 配置值被夹紧时记录请求值和最终接受值，避免低核心机器迁移时无感改变并发数。
- 原子写入记录目标路径、成功结果、写入失败和临时文件清理失败。

#### 2. 工作区扫描与文件监听

- 记录扫描代次开始、取消、被新扫描取代、完成数量和错误。
- `.nomedia` 抑制、目录读取失败、条目元数据读取失败和跳过原因均在合适级别留痕。
- 记录 watcher 建立、清除、事件类型、事件路径数量和向前端发送失败。
- 高频可访问性轮询只记录异常和不可访问结果，不记录每次正常结果。
- 前端对过期的工作区成功响应和失败响应分别记录 `debug`，确保快速切换目录时可以证明旧结果是被有意丢弃，而不是凭空消失。

#### 3. 全局 sidecar 许可、重试与回退

- 许可池调整记录调整前容量、调整后容量和当前占用数。
- 每次 sidecar 尝试记录：
  - 等待许可；
  - 取得许可并开始；
  - 成功；
  - 失败；
  - 重试等待前已经释放许可。
- ffprobe 和 ffmpegthumbnailer 均明确记录“总共最多三次尝试”，不会把第一次执行外再重试三次。
- ffmpegthumbnailer 三次均失败后，记录切换到 FFmpeg 回退；回退开始、成功和失败分别可见。
- 缩略图时间点探测失败或没有时长时，记录使用默认时间点的原因。
- 播放器 FFmpeg 继续走独立路线，其启动、替换、停止和退出日志与后台 sidecar 日志分开，便于确认播放器没有等待全局许可。

#### 4. 媒体缓存和元数据

- 索引读取区分以下失败阶段：
  - 文件读取失败；
  - JSON 解析失败；
  - 当前版本反序列化失败；
  - 旧版本反序列化失败。
- 索引损坏时记录原始错误、备份路径、是否成功重建以及回退为空索引的事实。
- 旧索引迁移持久化失败时记录“本次继续使用内存结果、下次启动会再次迁移”。
- 缓存维护记录无效条目、缺失 JPEG、孤儿 JPEG、LRU 淘汰数量、总字节数和限制值。
- 缩略图索引引用不存在的 JPEG 时明确记录回退到重新生成。
- 元数据记录缓存命中、失效、探测、持久化和写入失败；缩略图淘汰不会伪装成元数据丢失。
- 文件修改时间读取失败或早于 Unix epoch 时，记录缓存指纹回退为零。

#### 5. 播放器直接流、转码流和快速定位

- 直接流记录请求、路径解析、HTTP 状态和响应构建错误。
- 转码流记录目标时间、进程启动、PID、进程登记、客户端断开后的停止和等待失败。
- 新定位请求替换旧进程时，记录旧所有者清理、停止结果和新进程接管。
- 前端每次进度条定位都记录目标秒数，不做防抖或合并。
- 直接播放失败后切换 FFmpeg 的动作使用警告日志，转码 URL 获取成功或失败分别记录。
- 旧异步请求晚于新请求返回时记录“结果已过期并忽略”，避免误判为播放器没有响应。
- 外部播放器按钮只在错误态出现，其请求、成功和失败也均记录。

#### 6. 文件任务、回收站和 Windows Shell

- 回收、重命名、复制、移动、取消、剪贴板写入、剪贴板读取、粘贴和拖出均记录入口、结果和错误。
- 文件任务记录任务编号、逐项成功或失败、终态和前端事件发送失败。
- COM/OLE 初始化不再只记录布尔值；失败时保留 HRESULT，并说明文件操作或剪贴板能力可能不可用。
- 剪贴板 STA 线程启动接收端提前消失、消息循环失败和唤醒失败均记录。
- `Preferred DropEffect` 注册失败、格式缺失、数据读取失败或内存读取失败时，都明确记录回退到复制。
- `CF_HDROP` 中单个路径无法解析时记录项目索引；全部无法读取时返回明确错误。
- 终止 FFmpeg 进程树不再忽略 `taskkill`/`kill` 的非零退出状态。
- 临时复制文件、缓存文件和进程清理失败均记录，不再用无条件忽略掩盖残留。

#### 7. 前端交互与菜单

- 应用初始化、目录选择、收藏切换、文件单选/多选/范围选择、回收、重命名、复制到和文件任务取消均补充日志。
- 缩略图记录视口入队、滚动暂停、滚动恢复、批次分发、成功/失败汇总和旧工作区结果只写缓存不更新 UI。
- 元数据记录批次分发、旧响应忽略和失败汇总。
- 设置面板记录打开、草稿校验、字段增删、保存、失败和放弃未保存修改。
- 主题化菜单记录打开目标、键盘导航、动作开始、动作结束、失败、关闭和焦点恢复。
- 预览器记录加载、播放、暂停、结束、静音、音量提交、倍速、全屏和外部打开。
- 前端日志插件写入失败时改用 `console.error` 兜底，避免形成新的静默失败或递归日志。

#### 8. 启动诊断重放

- 配置恢复和工作区状态恢复会把诊断暂存到启动诊断队列，同时输出到 stderr。
- Tauri 文件日志插件就绪后，`.setup` 阶段取出诊断并按原级别重新写入文件日志。
- 媒体缓存索引加载返回可选诊断，主程序在 `.setup` 中重放索引损坏、备份和重建结果。
- 初次缓存维护和本地视频流服务器启动失败同样先保留错误，再在文件日志可用后补写。
- 这样既不改变配置/缓存的启动顺序，也消除了初始化前的文件日志空窗。

#### 9. 噪声与递归保护

- `poll_log_file` 成功路径故意不写日志，因为写日志会改变被轮询文件的哈希。
- 日志面板关闭、页面隐藏或已有请求未完成时仍不发起新请求。
- `get_file_task` 正常轮询不逐次记录，只记录查询失败和任务终态事件。
- 工作区可访问性正常轮询不输出成功日志，只在异常和状态转变时记录。
- 正常缓存命中使用 `debug`，自动回退使用 `warn`，最终失败使用 `error`。

### 验证

#### 1. 自动检查

- 执行 `npm run check`，结果通过。
- `tsc --noEmit` 通过。
- Vite 生产构建通过，共转换 1606 个模块。
- `cargo fmt --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过，没有遗留 warning。
- Rust 测试 32 个全部通过，0 失败、0 忽略。
- 新增回归仍集中在独立的 `src-tauri/src/regression_tests.rs`，没有把本轮验证代码写回业务模块。

#### 2. 静态审计

- 检查了 Rust 中的 `Err(_)`、`.ok()`、`unwrap_or` 和 `let _ =` 等容易隐藏错误的写法。
- 对确实表示失败或回退的分支补充错误原因；保留的忽略项仅限 Windows API 清理返回值、可选解析和有明确外围日志的情况。
- 检查前端所有 `catch` 和 Promise `.catch`，业务失败均有文件日志或独立控制台兜底。
- 确认日志轮询成功路径不产生日志，避免哈希自激。
- 确认高频工作区可访问性成功探测不刷日志。

#### 3. 仍需真实桌面观察的日志场景

- 人为移走 ffprobe，确认三次尝试、许可释放和最终失败顺序清晰。
- 人为移走 ffmpegthumbnailer，确认三次失败后仅执行一次 FFmpeg 回退。
- 使用内嵌不支持的视频，确认直接播放错误、FFmpeg 回退和外部打开入口日志连贯。
- 快速连续点击进度条，确认每次目标时间、旧转码终止和最新转码接管均能对应。
- 构造损坏配置、工作区状态和媒体缓存索引，确认启动后的文件日志包含备份与恢复详情。
- 在 Explorer 中复验复制、剪切、粘贴和拖放，确认 OLE/STA HRESULT、DropEffect 路线和文件任务编号可追踪。
- 打开日志面板静置，确认日志文件未因轮询成功而每 2.5 秒自行增长。

### 总结

1. **日志覆盖的目标不是“每个函数都打印”，而是每条业务链路都能还原。**
   - 请求、资源获取、外部调用、结果、清理和回退必须能够按上下文串联。

2. **自动回退必须比最终成功更显眼。**
   - 最终画面或文件操作成功，不代表首选路线正常；警告日志需要保留失败阶段和采用的备用路线。

3. **重试日志必须说明尝试预算和许可生命周期。**
   - 只有这样才能判断是 sidecar 自身慢、许可饥饿，还是重试等待占住了并发资源。

4. **启动日志要考虑日志系统尚未就绪。**
   - 关键诊断需要暂存并重放，不能假设一次 `log::error!` 调用必然进入文件。

5. **高频成功日志会降低可观测性。**
   - 轮询、可访问性探测和状态查询应记录异常与状态变化，避免持续成功输出淹没真正错误。

6. **日志系统自身也要有独立回退。**
   - 前端插件失败使用控制台，后端启动前使用 stderr 加启动诊断重放；任何一层失败都不再无声消失。

## 36. 2026-07-19：`main.rs` 与 `App.tsx` 的第二次结构重拆

### 发现问题

1. 第一轮功能重构虽然完成了许可池、媒体缓存、日志、菜单和复制链路，但修改过程中继续把新增命令、状态和界面协调塞进两个入口文件。
2. `main.rs` 一度增长到数千行。即使业务可以运行，入口文件同时承担数据结构、缓存持久化、命令实现、工作区监听、播放器流和 Windows Shell，任何修改都需要理解几乎整个后端。
3. `App.tsx` 同时维护工作区生命周期、虚拟列表、缩略图队列、元数据、文件任务、拖动手势、菜单、日志、设置和预览 UI。函数和 JSX 交错，局部修改容易触碰无关状态。
4. 单纯按“代码类型”搬运仍可能制造新的巨型工具文件，因此本次不能只追求入口行数下降，还要检查新文件是否形成可解释的业务边界。
5. 第一轮重拆后，独立回归测试仍直接读取转码控制器的私有字段，说明测试边界没有随模块边界一起收紧。

### 探索问题

1. 先按职责列出 `main.rs` 中的内容：
   - Tauri 启动与状态装配；
   - 配置和应用级命令；
   - 文件命令；
   - 媒体命令；
   - 日志命令；
   - 数据模型；
   - 媒体缓存持久化；
   - 工作区监听；
   - 后台 sidecar 许可池；
   - 播放器流；
   - Windows OLE/STA。
2. 判断哪些函数只是纯转发。媒体路径验证、流路径解析、查询参数编码和流服务器启动都没有额外验证、状态转换或线程边界，不应为了“统一调用入口”再包一层。
3. 对 `App.tsx` 不按 React API 类型拆分，而按用户可观察到的业务域拆分：导航、工作区视图、缩略图、元数据、文件任务、手势、菜单、监听、设置、日志和预览。
4. 对跨域状态保留在 `App.tsx`：当前配置、当前工作区、当前选择以及左右中面板装配。只属于一个域的计时器、请求代次、队列和事件订阅进入相应 hook。
5. 对展示代码继续区分：标题栏、预览栏、文件任务卡和元数据加载层只接收明确 props，不读取巨型 Context，也不自行调用不相关后端命令。
6. 检查拆分后文件体积和依赖方向，而不是把“文件少”当作目标。较长模块必须能用单一业务职责解释，例如文件任务 hook 负责整条文件任务交互，工作区手势 hook 负责完整指针状态机。

### 解决问题

#### 1. Rust 入口重新定位为组装层

- `main.rs` 缩减到 276 行，只保留模块声明、依赖装配、Tauri 插件和 command 注册。
- 数据契约移入 `models.rs`，缓存迁移和原子持久化移入 `storage.rs`。
- 应用、文件、媒体和日志 command 分别进入 `application_commands.rs`、`file_commands.rs`、`media_commands.rs`、`log_commands.rs`。
- 工作区 watcher 回归 `workspace.rs`；后台 sidecar 固定许可池回归 `media_processing.rs`；播放器控制器和本地流服务器回归 `media_stream.rs`；OLE/STA 拖放与剪贴板回归 `windows_shell.rs`。
- 删除文件任务启动的无验证转发包装，`main.rs` 直接装配文件任务队列。
- 删除 `runtime_api.rs` 中媒体路径判断、路径解析、URL 编码和流服务器启动的纯转发；该模块现在只保留配置存储的状态访问边界。
- FFmpeg 进程树终止函数移回 `media_stream.rs`，使播放器进程的登记、替换、终止和释放由同一模块负责。

#### 2. 前端按业务域拆分

- `App.tsx` 从 895 行继续降到 673 行；它保留后端初始化、全局状态和三栏布局装配。
- `useWorkspaceController` 独立处理工作区切换、请求代次、旧响应丢弃、记忆持久化、刷新和断连状态。
- `useWorkspaceViewState` 负责搜索、排序、视图模式、虚拟列表与焦点记忆；`useThumbnailQueue` 负责无限 FIFO、去重和滚动暂停；`useMediaMetadata` 负责缓存缺失项的批量确保。
- `useFileTasks`、`useWorkspaceGestures`、`useWorkspaceMenu`、`useWorkspaceKeyboard` 和 `useWorkspaceMonitoring` 分别管理文件任务、指针状态机、主题化菜单、键盘入口和文件系统监听。
- `useSettingsController` 与 `useLogViewer` 分别拥有设置草稿/列配置和日志哈希轮询生命周期。
- `AppTitlebar`、`PreviewPanel`、`WorkspacePanel`、`FileTaskCard` 与 `MetadataLoadingOverlay` 仅承担展示和本域交互，不复制后端配置或建立全局 Context。

#### 3. 收紧测试边界

- 回归测试继续集中在独立的 `src-tauri/src/regression_tests.rs`，没有把桌面回归代码塞回业务模块。
- 测试不再直接读取 `TranscodeController.active_processes` 私有字段；仅在 `cfg(test)` 下提供只读进程路径查询，生产构建不会暴露额外接口。
- 在 `VALIDATION.md` 追加前端 hook 自动化候选项和真实桌面重拆矩阵，明确构建通过不能替代 FFmpeg、OLE/STA、WebView 焦点和 DPI 验收。

### 验证

1. `npm run build` 通过，TypeScript 类型检查和 Vite 生产构建成功。
2. `cargo fmt --check` 通过。
3. `cargo clippy --all-targets -- -D warnings` 通过。
4. `cargo test` 的 32 个测试全部通过，0 失败、0 忽略。
5. 完整入口 `npm run check` 通过。
6. 静态行数复核：`main.rs` 276 行，`App.tsx` 673 行；新增前端文件按导航、预览、工作区、设置和日志业务域组织。
7. 本轮只完成自动检查；真实桌面 FFmpeg 快速定位、Explorer OLE/STA、长队列、菜单焦点与多 DPI 仍按 `VALIDATION.md` 留待人工矩阵验证。

### 总结问题

1. **入口文件的职责是组装，不是收容。** 新命令和新 UI 如果不先确认归属，功能每增加一次，入口就会重新膨胀。
2. **拆分标准应是状态所有权和失败边界。** 同一队列、请求代次或外部进程生命周期应放在一起；仅仅把若干函数复制到 `utils` 不能降低理解成本。
3. **纯转发函数会隐藏真实依赖。** 没有验证、转换、缓存或线程边界的包装应删除，让调用方明确依赖实际业务模块。
4. **组件拆分和 hook 拆分解决不同问题。** 组件隔离展示，hook 隔离状态机和副作用，两者都需要明确输入输出。
5. **测试也必须遵守模块边界。** 测试为了方便直接访问私有字段，会让业务实现无法安全演进；测试专用只读接口比扩大生产可见性更合适。
6. **行数只是报警器，不是架构目标。** 本轮记录行数用于防止入口再次失控，最终判断仍以模块是否能用单一职责解释、失败是否可追踪为准。

## 37. 2026-07-19：缩略图失败后立即切换 FFmpeg

### 发现问题

1. 后台 sidecar 最初统一采用三次总尝试，规则简单，但忽略了 ffprobe 和 ffmpegthumbnailer 的失败成本并不相同。
2. ffprobe 没有直接替代结果时，短暂失败值得重试；ffmpegthumbnailer 已有 FFmpeg 回退路线，重复运行三次通常只是在重复同一个失败。
3. 冷缓存目录中每个失败缩略图都会占用多轮全局许可。失败文件较多时，单文件的两次额外尝试会累积为整批明显延迟。
4. 旧实现还在 `resolve_sidecar("ffmpegthumbnailer")` 失败时直接返回，实际上没有进入已设计好的 FFmpeg 回退路线。

### 探索问题

1. 保留全局许可池不变，因为 ffmpegthumbnailer 和 FFmpeg 回退都属于后台外部进程，仍需限制总并发。
2. 不修改通用三次重试器，避免误伤 ffprobe；为具有即时回退能力的首选工具增加单次执行入口。
3. 检查许可生命周期：单次 ffmpegthumbnailer 结束后先释放许可，再探测缓存元数据并启动 FFmpeg 回退，避免一条任务同时占用多个许可。
4. 检查失败可观测性：日志必须明确写出“最多一次”和“立即回退”，并保留 sidecar 解析失败或进程失败的原始原因。

### 解决问题

- 新增 `run_background_sidecar_once`，只获取一次许可、执行一次操作并立即释放；成功和失败日志统一标记 `attempt=1/1`。
- 缩略图首选路线改用单次执行函数，不再调用三次重试器。
- ffmpegthumbnailer 返回失败、没有生成图片或 sidecar 无法解析，都会进入相同 FFmpeg 回退分支。
- FFmpeg 回退仍只执行一次；回退前的统一媒体信息探测仍可使用 ffprobe 的三次总尝试。
- 新增独立回归测试，直接断言单次执行函数在失败时只调用操作一次。

### 验证

- 定向执行包含 `sidecar` 的 7 项测试，全部通过。
- 完整执行 `npm run check` 通过：前端生产构建成功，Rust 格式和 Clippy 严格检查成功，33 项 Rust 测试全部通过。
- 仍需在真实冷缓存大目录中比较首屏和全目录缩略图完成时间。

### 总结问题

1. **重试次数不应只按工具类型统一。** 有廉价可靠回退的操作，应优先降低失败后的等待时间。
2. **回退入口必须覆盖“工具不存在”。** sidecar 查找失败和进程执行失败对用户都是首选路线不可用，不应产生不同的最终行为。
3. **日志要表达真实预算。** `attempt=1/1` 比笼统的失败信息更容易确认应用没有暗中重复运行工具。

## 38. 2026-07-19：日志轮询导致阅读位置回到开头

### 发现问题

1. 日志面板每 2.5 秒轮询一次文件，即使哈希未变化，也会先把 `loading` 切换为真。
2. `LogDialog` 在 `loading` 时用单行“正在读取日志…”替换整个 `<pre>` 正文。
3. 正文区域高度瞬间从大量日志缩短为一行，浏览器会把原有 `scrollTop` 夹紧到 0；轮询结束恢复正文时，滚动条因此停在日志开头。
4. 问题表面像是 React 刷新了整个页面，实际是同一个滚动节点的内容在两次渲染之间被清空和恢复。

### 探索问题

1. 后台轮询的正常状态不需要可见加载动画；哈希未变化时理想行为是完全不更新日志正文和加载状态。
2. 手动刷新和首次读取仍需要加载反馈，但已有正文不能为了显示反馈而被替换。
3. 日志追加时需要区分两种用户意图：已经停在底部的用户希望跟随新日志，主动向上查看历史的用户希望保持阅读位置。
4. 仅依赖浏览器默认滚动保持不够稳妥，因为级别过滤或最大日志快照截断会改变内容高度。

### 解决问题

- 周期后台轮询不再切换 `loading`，只有首次读取或用户手动强制刷新才更新加载状态。
- 日志已经有正文时，加载状态只通过 `aria-busy` 和刷新按钮体现，不再替换 `<pre>` 内容。
- `LogDialog` 保存日志容器的 `scrollTop`，正文变化后的布局阶段将其恢复到合法范围。
- 用户滚动到距离底部 8 像素以内时标记为“跟随末尾”；后续日志追加自动滚到底部。用户离开底部后，新日志不会打断当前阅读位置。

#### 触发链路复盘

1. 定时器调用 `pollLogs(false)`。
2. 旧实现立即执行 `setLoading(true)`，因此即使后端最后返回 `changed: false`，前端也至少发生“开始读取”和“读取结束”两次状态更新。
3. 第一次状态更新让 `LogDialog` 重新渲染，条件表达式选择加载文案，原日志正文从 DOM 文本中暂时消失。
4. `<pre>` 的 `scrollHeight` 降低后，WebView 将超出范围的 `scrollTop` 自动夹到 0；这个变化不是 React 主动设置，但发生在浏览器布局阶段。
5. 后端返回哈希未变化时不会更新 `snapshot`，但 `setLoading(false)` 仍触发正文恢复。此时浏览器没有旧滚动位置可恢复，于是用户看到日志跳到开头。

#### 方案取舍

1. **没有采用“每次刷新后无条件滚到底部”。** 这会解决回到开头，却会持续抢走正在阅读历史日志的用户位置。
2. **没有采用给日志节点增加动态 `key`。** 更换 `key` 会强制卸载并重建节点，反而会确定性丢失滚动位置、文本选择和辅助技术上下文。
3. **没有暂停日志轮询。** 问题不在轮询频率，而在轮询时错误地改变可见正文；保留 2.5 秒周期仍能及时显示新的失败日志。
4. **没有只依赖 React 节点复用。** 普通文本追加时浏览器通常会保留滚动位置，但过滤级别改变、日志截断和占位文案切换都可能改变高度，因此仍显式保存滚动状态。
5. **采用 `useLayoutEffect` 而非普通 `useEffect`。** 滚动恢复在浏览器绘制前完成，可避免用户先看到跳到其他位置、随后又被拉回的闪动。

#### 滚动状态规则

- `savedScrollTop` 保存用户最后一次滚动后的绝对位置。
- 当 `scrollHeight - scrollTop - clientHeight <= 8` 时，认为用户处于底部；8 像素容差用于吸收缩放和小数布局产生的误差。
- 正文更新且用户处于底部时，将 `scrollTop` 设置为新的 `scrollHeight`，继续显示最新日志。
- 正文更新且用户不在底部时，恢复保存的位置；若过滤后的正文变短，则夹紧到新的最大可滚动范围。
- 首次打开没有正文时可以显示“正在读取日志…”，已有正文后的手动刷新只标记 `aria-busy`，不改变文本内容。
- 后台哈希轮询不切换 `loading`；哈希不变时 `snapshot` 也不更新，从而不产生无意义的日志正文渲染。

#### 失败与边界处理

- 后台读取失败仍会更新错误提示并写入文件日志，但不会清空上一次成功读取的正文，用户可以继续查看已有诊断。
- 手动刷新由 `pollInFlight` 串行保护，和定时轮询不会并发修改哈希或加载状态。
- 关闭日志面板后组件卸载，滚动状态随本次查看会话释放；下次打开按新的日志快照重新开始，不把旧文件位置错误套用到新快照。
- 日志快照最多读取尾部 512 KiB；当旧日志从快照头部被截掉时，绝对像素位置只能保持视觉位置，不能保证仍对应同一行。需要跨截断精确定位时，应改为记录行标识或字节偏移，本轮没有引入这项复杂度。

### 验证

- `npm run build` 通过，TypeScript 类型检查和 Vite 生产构建成功。
- 已在 `VALIDATION.md` 追加中间位置、底部跟随、手动刷新和日志级别切换的交互回归项。

### 总结问题

1. **轮询不等于必须展示加载态。** 后台增量检查若没有新数据，应尽量不触发可见状态变化。
2. **不要用占位文案替换已有的可滚动正文。** 高度变化会破坏滚动位置、文本选择和用户阅读上下文。
3. **自动跟随只能尊重用户意图。** 位于底部时跟随新内容，离开底部后保持位置，比每次强制滚底或固定像素更符合日志阅读习惯。

## 39. 2026-07-19：大范围重构的提交前整体审查

### 发现问题

本轮不是只检查最后两处小改动，而是重新沿着 v0.8 重构后的完整业务链路审查模块边界、异步生命周期和失败路径。静态检查及逐段阅读发现以下问题：

1. `useThumbnailQueue`、`useFileTasks` 和原生拖入监听都通过异步 Promise 取得取消函数。如果 effect 已经清理、Promise 随后才返回，旧实现会保存一个再也没人调用的取消函数，StrictMode 或快速切换工作区时可能遗留重复监听。
2. `refreshWorkspace` 会先判断列表目标是否属于当前工作区，但旧实现随后仍无条件用返回列表的路径集合过滤全局选择。旧目录结果晚到时，列表虽然没被覆盖，新工作区的选择却可能被清空。
3. 原位重命名完成后无条件选中新路径。用户若在文件操作期间切换目录，新路径会被错误写入另一个工作区的选择状态。
4. 快捷键复制/剪切经过 `useFileTasks.writeSelectionToFileClipboard`，右键菜单却直接调用 Tauri command。两条路线最终都到同一后端，但前端的通知、日志和异常处理存在重复，违背了此前要求的入口统一。
5. 日志读取只有全局“是否在途”布尔值，没有查看会话代次。快速关闭并重新打开日志面板时，旧会话结果可能晚到并写回新会话，或阻止新会话立即读取。
6. React StrictMode 下，应用恢复 effect 会执行两次。工作区请求代次能够阻止旧扫描覆盖新扫描，但管理员提示、启动日志和额外后端调用仍可能重复。
7. FFmpeg 缩略图回退使用 `result?` 直接向上返回。进程启动后若生成了部分文件再失败，临时 `.tmp.jpg` 不会经过后续清理分支。
8. sidecar 超时后的 `taskkill` 只记录命令启动错误，`taskkill` 成功启动但返回非零状态时没有日志，仍属于静默清理失败。
9. `MediaSidecarPermit::drop` 在许可池锁中毒时直接返回，没有记录许可无法归还的原因。
10. `file_commands.rs` 在拆分后仍保留了路径规范化、回收/重命名入队和拖出调用的纯转发函数，没有增加验证、状态转换或线程边界。
11. CSS 拆分后 `.log-output` 仍存在两个相邻的同名规则，可以无损合并。

### 探索问题

#### 1. 异步监听的正确清理模型

- Tauri 的 `listen` 和 WebView 拖放注册都异步返回取消函数，不能假设它们会在 React cleanup 前完成。
- effect 内需要维护 `active` 标记：注册完成时若仍活跃就保存取消函数，否则立即调用刚取得的取消函数。
- 文件任务监听属于应用级单例，不应随着工作区路径变化反复注销和注册；回调通过 ref 读取当前工作区即可。

#### 2. 工作区结果的所有权

- 只在 `setWorkspace` 的函数式更新中判断路径不够，因为选择、焦点等相关状态位于其他 setter。
- 在应用任何工作区相关状态前，需要先用同步 ref 比较“请求目标路径”和“当前工作区路径”；不匹配时整组结果都必须丢弃。
- 重命名和文件任务完成回调同样不能持有旧 render 的工作区对象，需要通过 ref 读取完成时的当前路径。

#### 3. 日志轮询的会话隔离

- 单一布尔值只能表达“某个请求正在运行”，无法区分这个请求属于已关闭面板还是新打开面板。
- 使用递增 `session` 标识查看会话；请求捕获发起时的代次，返回时只允许更新相同代次的状态。
- 在途标记也按会话记录，使新会话可以立即发起读取，同时旧请求只能自行结束并被忽略。

#### 4. 失败清理的控制流

- `?` 适合没有额外清理责任的传播；一旦外部进程可能留下临时文件，就必须显式匹配结果，在返回错误前完成清理和日志记录。
- 清理函数同时记录成功和失败，便于区分“回退失败但临时文件已清除”与“失败并留下残留”。
- Windows 进程树终止需要区分三种结果：命令执行成功且状态成功、命令执行成功但状态失败、命令本身无法启动。

### 解决问题

#### 前端生命周期与入口统一

- 为缩略图事件、文件任务事件和原生拖入注册补充 `active` 清理协议，覆盖异步注册晚于 cleanup 的竞态。
- 文件任务监听改为应用生命周期内注册一次，通过 `workspacePathRef` 刷新当前工作区，不再因目录切换产生监听空窗。
- `useWorkspaceController` 增加当前路径 ref；刷新和断连结果不属于当前目录时，列表、选择和焦点全部跳过更新。
- 重命名完成后仅在用户仍位于原工作区时更新选择，否则只记录成功结果并跳过跨目录选中。
- 抽出 `writeFilesToClipboard(paths, operation)`；快捷键包装和右键菜单都调用该函数，保留拖出 OLE 的独立适配路线。
- 日志轮询增加查看会话代次，旧会话的成功、失败和 loading 收尾都不能写回新会话。
- 应用启动恢复增加单次 guard，避免 StrictMode 重复执行管理员检测和工作区恢复。
- `useThumbnailQueue` 在渲染阶段同步更新经过 `useMemo` 缓存的当前文件路径 Set，缩短工作区切换时旧结果误入 UI 的窗口。

#### 后端失败路径与冗余清理

- 新增统一的失败缩略图临时文件清理函数；FFmpeg 回退错误、未生成图片和缓存提交失败都会在返回前清理并记录阶段。
- sidecar 超时终止现在检查 `taskkill` 的退出状态，非零状态记录警告。
- 许可归还遇到锁中毒时记录错误，不再静默返回。
- 删除 `file_commands.rs` 中四类纯转发包装，command 直接依赖实际的文件操作或 Windows Shell 模块。
- 合并相邻的 `.log-output` CSS 规则，不改变视觉效果。

### 验证

1. `npm run build` 通过，TypeScript 类型检查和 Vite 生产构建成功。
2. `cargo fmt --check` 通过。
3. `cargo clippy --all-targets -- -D warnings` 通过。
4. Rust 测试增加到 34 项，全部通过，0 失败、0 忽略。
5. 新增 `failed_thumbnail_fallback_removes_partial_output`，回归测试继续位于独立的 `src-tauri/src/regression_tests.rs`。
6. `git diff --check` 留到提交前再次执行，确保文档和代码没有空白错误。
7. 自动检查无法替代的工作区快速切换、StrictMode 监听数量、Explorer STA/OLE、真实 FFmpeg 超时和日志滚动交互，已追加到 `VALIDATION.md`。

### 总结问题

1. **模块拆分之后，最容易遗漏的是生命周期边界。** 代码离开巨型入口后更容易阅读，但跨模块的晚到结果、异步注册和 cleanup 必须显式建模。
2. **“列表没有被旧结果覆盖”不代表整个工作区状态安全。** 选择、焦点、预览和通知也要服从同一个工作区所有权检查。
3. **同一个后端 command 不等于同一条前端业务路线。** 快捷键和菜单必须复用相同的前端函数，才能共享通知、日志和错误处理。
4. **错误传播前先确认是否承担清理责任。** 外部进程、临时文件、许可和系统句柄都不能仅靠 `?` 提前退出。
5. **测试数量不是审查完成的证明。** 自动测试覆盖确定性规则，真实桌面仍需观察 WebView、Windows Shell 和 FFmpeg 进程行为。

## 40. 2026-07-19 至 2026-07-20：发布收尾、文件系统状态与壁纸遮罩

### 发现问题

本轮从三个用户可见缺陷开始，随后扩展到 v0.9 收尾能力与界面细节。问题并非独立存在，而是共同暴露出异步所有权、Windows Shell 生命周期、持久化边界和视觉层级的缺口。

1. 播放器在用户手动暂停后，晚到的原文件流地址、转码流地址、`canplay`、回退转码或旧媒体元素事件仍可能调用 `play()`，造成无操作自动继续播放。
2. 左栏目录树只在展开时枚举，外部 Explorer 或应用内操作改变目录结构后，已加载子项缓存不会失效；删除、移动和重命名还会遗留展开路径、焦点或工作区路径。
3. 左栏缺少目录回收站入口。目录删除不仅涉及 Shell 操作，还要清理收藏、最后工作区、排序、焦点、目录树缓存和预览状态；失败时这些 UI 状态不能先被删除。
4. NSIS 卸载后安装目录内的 `data` 残留。审查生成脚本后确认，Tauri 默认卸载器只删除主程序和 sidecar，随后执行非递归 `RMDir "$INSTDIR"`；其“删除应用数据”复选框只处理 `%APPDATA%/%LOCALAPPDATA%` 下的 bundle ID，而本应用的数据实际位于 `$INSTDIR\data`。
5. v0.9 仍缺窗口状态、背景图、数据管理、诊断导出、关于信息和破坏性操作的主题化确认。此前设置仅保存业务偏好，不能承担高频窗口几何写入。
6. 设置页打开后日志显示每次都会访问 `ffmpeg`、`ffprobe` 和 `ffmpegthumbnailer`。根因是“关于信息”命令在每次打开设置时通过 `-version` 启动三个 sidecar，而不是仅仅读取缓存路径。
7. 背景图初版只铺在应用根层；标题栏、导航、工作区和预览面板仍使用不透明纯色，所以壁纸只在两个面板拖动条的空隙中露出，视觉上像两条无意义的彩色竖线。
8. 背景透明度初版为组件预留了固定最小 alpha，虽然保证了可读性，却使滑块的 100% 仍无法得到纯透明表面，不符合“0% 实色、100% 纯透明”的预期。
9. 设置页“媒体”区域的数字输入组宽度被全局 `.number-input` 规则覆盖；“支持的视频格式”行的 `align-items` 也被导入顺序靠后的全局 `.setting-row` 覆盖，导致控件和文字不对齐。

### 探索问题

#### 1. 播放器异步事件的授权边界

- 仅依据 HTMLMediaElement 的 `paused` 状态无法区分“用户明确暂停”和“新源加载过程中的暂时暂停”。
- 流请求、转码回退和媒体事件是异步的；即使 React 当前选中项未变，旧请求仍可能在新会话中返回。
- Seek 是否恢复播放也不能由自动播放设置单独决定，必须继承 Seek 发起瞬间的播放意图。

#### 2. 目录树监听的范围与状态清理

- 递归监听全部磁盘目录会造成不必要的资源占用，也会偏离树的惰性加载语义。
- 只监听当前工作区无法覆盖左栏已展开但未激活的节点。
- 路径删除的判断必须按完整路径段进行，不能用普通前缀匹配，否则 `Archive` 会误影响 `Archived`。

#### 3. NSIS 默认脚本的可扩展性

- 需要确认 hook 的插入顺序：若在注册表清理后才删除 `data`，失败时会出现“仍有文件但卸载入口已消失”。
- 需要区分普通卸载与覆盖升级；升级绝不能删除 `data`。
- Tauri 默认确认页仍包含面向默认 AppData 的复选框，完整移除该 UI 需要维护自定义 NSIS 模板；仅用 hook 无法重写该页面。

#### 4. 背景图和遮罩的持久化边界

- 不可保存原图绝对路径，否则原图移动后背景失效；必须复制到受管的 `data/backgrounds`。
- 设置草稿尚未应用时不应写入配置；导入动作需要在“应用”阶段才进入持久化配置。
- 窗口尺寸、位置、最大化状态与面板布局不应写入 `config.json` 或 `workspace-state.json`，否则会和设置草稿、工作区焦点产生写入竞争。

#### 5. 透明度的正确数学语义

- 根层背景的可见度与各组件遮罩透明度必须同步，否则壁纸即使在根层可见，仍会被子组件固定颜色遮挡。
- 若 alpha 设为固定最小值，例如 `.84`，滑块达到 100% 仍不是纯透明；若要求端点精确，必须使用 `a = 1 - value / 100`。
- 拖动条在纯透明时不能保留实色轨道，否则会重新形成两条壁纸断层；但其 12px 命中区仍保留，悬停或拖动时用主题色提示可操作性。

### 解决问题

#### 播放器、目录树与目录删除

- 播放器引入显式播放意图和媒体请求代次。用户暂停立即撤销意图；旧直连/转码响应、`canplay`、回退和旧 `play` 事件都必须通过当前代次与意图检查。意外 `play` 会被立即暂停。
- 转码 Seek 只继承 Seek 前的播放意图；暂停状态定位后保持暂停，唯一选中项的初始自动播放语义不变。
- 后端为所有已展开目录建立非递归监听，前端按 300ms 合并事件后重新读取受影响节点。失效子树会清理目录缓存、展开状态和失效焦点，仍存在路径尽量保持展开。
- 新增目录回收站命令，拒绝根目录和无效路径，复用 STA `IFileOperation + FOFX_RECYCLEONDELETE`。成功后清理目录及后代关联的收藏、最后工作区、排序和焦点；失败只记录明确错误并保留现有 UI。
- 左栏目录菜单只在可删除子目录显示“移到回收站”，删除当前工作区或其祖先时停止预览、释放转码并清空工作区状态。

#### 发布、Shell 与状态维护

- 新增 `src-tauri/installer-hooks.nsh`，通过 `NSIS_HOOK_PREUNINSTALL` 在注册表清理之前检查主进程并删除 `$INSTDIR\data`；升级模式跳过该步骤。删除失败会 `Abort`，从而保留卸载入口和注册表信息。
- 已检查生成的 NSIS 脚本包含 `installer-hooks.nsh`。完整 NSIS 构建在当前命令时限内未结束，且真实安装、文件占用、权限失败和自定义目录仍需人工验收。
- 剪贴板 STA 队列新增 flush 任务。主窗口关闭时在拥有 `IDataObject` 的 STA 上执行一次 `OleFlushClipboard` 并释放进程内引用，避免运行期间提前刷新，同时支持退出后 Explorer 继续粘贴。
- 新增 `window-state.json`，独立保存窗口位置、尺寸、最大化、左栏比例与预览栏状态；启动时按当前显示器工作区和最小尺寸夹紧。前端以延迟写入保存布局，避免拖动时持续落盘。
- 新增受限的数据维护命令：统计应用数据占用、清空缩略图与空索引重建、清理 30 天前日志、导入/读取受管背景图、读取关于信息和导出脱敏诊断 JSON。所有路径限制在应用 `data` 目录。
- 新增主题化确认对话框，目录/视频回收站、缩略图清空和旧日志清理不再使用浏览器原生确认框；Escape、取消、焦点进入和焦点恢复均有明确行为。
- 关于信息使用 `OnceLock` 缓存 sidecar 版本。首次读取仍会运行 `-version`，随后打开设置只返回缓存，不再重复启动三个 sidecar。

#### 壁纸遮罩与设置页对齐

- 深浅模式分别提供固定的中性 RGB 值；不以青绿、晴空等主题色参与遮罩计算，主题色只用于选中、焦点和拖动高亮。
- 在 `App.tsx` 根元素动态注入 `--surface-base-rgba`、`--surface-raised-rgba`、`--surface-control-rgba`、`--surface-resize-rgba` 与 `--border-subtle-rgba`。`theme.ts` 不再写死这些 alpha。
- 背景滑块使用严格线性公式 `a = 1 - backgroundOpacity / 100`：0% 时组件为实色，50% 为半透明，100% 时壁纸面对的面板、边框与拖动轨道为纯透明。拖动命中区域仍保留，悬停/拖动时显示主题色。
- 标题栏、导航、工作区、工具栏、预览、播放器控件、搜索框、卡片和缩略图占位背景改用动态 RGBA 变量，使壁纸连续穿过三栏而不会只暴露在分隔条中。
- 数字输入组使用更高优先级的 `.setting-row .number-input` 网格规则，统一为与下拉框相同的控制列宽度；格式列表行使用 `.setting-row.extension-setting` 覆盖全局垂直居中，并让标题与输入框首行顶部对齐。

### 验证

1. `npm run check` 通过：TypeScript、Vite、Vitest、Rust 格式、Clippy 严格警告和 Rust 测试均成功。
2. 前端单元测试增加主题化确认对话框；已有播放器晚到流和目录菜单测试继续通过。
3. Rust 测试增加背景路径防遍历、诊断日志尾部长度等回归，测试总数提升至 38 项并全部通过。
4. `git diff --check` 通过。
5. `npm run tauri -- build --bundles nsis` 在当前工具 64 秒时限中断；已从生成的 `installer.nsi` 确认 hook 被 include，但不将其视为安装包验收成功。

### 尚待真实桌面验收

1. 原文件直连与 FFmpeg 转码视频在自动播放开关两种状态下，手动暂停、Seek、快速切换和退出后均不应自行恢复播放或留下孤儿进程。
2. Explorer 与应用内的新建、删除、重命名、移动需验证展开目录树的监听刷新、有效展开状态保留和失效路径清理。
3. 目录回收站需验证空目录、嵌套目录、收藏目录、当前工作区、文件占用、权限不足、取消确认和根目录隐藏入口。
4. NSIS 需在干净 Windows 环境验证默认/自定义安装、覆盖升级保留 `data`、普通卸载删除 `data`、占用和权限失败。默认 Tauri 卸载确认页的旧复选框 UI 仍存在；若要移除该文案，需要后续维护完整自定义模板。
5. 背景图需在深浅模式、0/50/100% 透明度、不同图片尺寸、三栏拖动和 100%/125%/150% DPI 下检查文字可读性、壁纸连续性和拖动线悬停可见性。
6. `window-state.json` 需验证多显示器/DPI 变化后窗口仍落在可见工作区，布局恢复不影响工作区状态。

### 总结问题

1. **异步播放的“当前元素状态”不是用户意图。** 必须以显式意图和请求代次作为唯一播放授权，才能屏蔽晚到响应和事件乱序。
2. **目录结构变化是路径状态问题，不只是重新枚举。** 监听刷新必须同时更新树缓存、展开集合、焦点、工作区和配置中的后代引用。
3. **安装器默认数据目录假设必须与应用实际存储策略一致。** 将数据放在安装目录旁时，卸载策略需要显式补齐，并把失败发生在注册信息被移除之前。
4. **设置页面的“信息展示”也可能有副作用。** 版本查询若执行外部程序，应缓存结果，不能把打开设置变成反复启动 sidecar。
5. **透明度是跨层状态，而非单个背景属性。** 根壁纸、面板、控件、边框和拖动轨道必须使用同一滑块语义；若产品要求纯透明端点，就不能以固定最小 alpha 代替。
6. **CSS 模块化不等于隔离。** 被主样式表后置的同优先级规则仍会覆盖模块规则；关键布局应使用足够具体的选择器并在截图场景中复验。

## 41. 2026-07-20：设置持久化默认值与 Sidecar 许可证更正

### 发现问题

1. 背景透明度虽然已接入设置草稿与配置文件，但默认值仍是 100%，与产品要求的默认 50% 不一致。
2. 设置页的大部分控件经“应用”统一保存，播放器音量/静音和列表列布局则走独立即时保存命令；缺少一个覆盖全部可调偏好的回归测试，后续新增字段时容易漏出持久化范围。
3. 先前发布清单把 FFmpeg/ffprobe 表述为 LGPL 兼容构建。实际运行 `ffmpeg -version` 后发现二进制带有 `--enable-gpl --enable-version3`，该表述不成立；且“打开许可证”依赖的安装包 `LICENSES` 目录尚未作为 bundle resource 明确配置。

### 探索问题

1. 不能为改变默认值而覆盖用户已经保存的透明度；配置中已有 `backgroundOpacity` 的用户应继续保留自己的值，只有缺失字段的新配置使用 50%。
2. 仅测试某一个设置项不能证明 `save_configuration` 覆盖全部 `Preferences`；测试应写入每个可调字段，再从实际原子写出的 `config.json` 读取断言。
3. 许可证义务取决于实际二进制的构建选项，而不是项目名称或预期来源。安装器资源路径也不能假定与可执行文件同级，应优先使用 Tauri 的资源目录解析。

### 解决问题

1. 将 `default_background_opacity()` 改为 50；serde 仅在旧配置缺少字段时调用默认函数，故不会改变已有用户选择。
2. 新增“全部可调偏好字段持久化”Rust 回归测试，覆盖外观、主题、缓存、取帧、播放、音频、扫描开关、扩展名、后台并发、列表列、背景文件名和透明度。
3. 新增 `LICENSES/THIRD_PARTY_NOTICES.md` 并将整个目录加入 Tauri bundle resources；关于页优先通过 `resource_dir()` 定位该目录，开发运行时再回退到可执行文件同级位置。
4. 发布清单改为准确标识当前 FFmpeg/ffprobe 为 GPLv3+ 构建，并要求替换二进制时同步重新核验许可证、来源、哈希和源代码获取说明。

### 验证与待验收

1. 自动检查将覆盖默认值与全字段 `config.json` 持久化回归。
2. 仍需在真实 NSIS 安装包中确认 `LICENSES` 被复制、关于页可打开目录，并在更换 sidecar 时按实际构建补齐完整许可证文本和源代码获取材料。

### 总结问题

1. **默认值是兼容性策略的一部分。** 通过 serde 的缺失字段默认值迁移，才能同时满足新默认行为和既有用户设置不被改写。
2. **许可证必须以实际分发二进制为准。** 发布清单、界面入口和安装包资源必须共同指向同一份、经核验的许可证材料。

## 42. 2026-07-20：将媒体二进制改为用户自行安装，项目代码采用 MIT

### 发现问题

1. 即使仓库忽略了 `sidecars/*.exe`，Tauri 的 `externalBin` 仍会把构建机本地文件写入 NSIS 安装包；“不提交二进制”不等于“不分发二进制”。
2. 当前本机 FFmpeg full build 实际启用了 GPL 选项。项目若只想开源自有代码并采用 MIT，必须把自有代码许可、安装包内容和用户取得的工具三者清晰分开。
3. 运行时解析器已经支持 `<应用目录>/sidecars`，但 README 仍指示构建机准备并内置 sidecar，用户无法按同一约定完成手动安装。

### 探索问题

1. 只加 Git 忽略规则不能阻止构建系统把本机文件纳入发布物，必须移除 bundle 的 `externalBin`。
2. 自动下载会重新模糊分发边界，因此安装辅助工具只能复制用户已经从上游获得的文件，不能内置下载 URL、镜像或网络请求。
3. 用户下载包中的原始名称通常为 `ffmpeg.exe` 与 `ffprobe.exe`，而应用的 Windows x64 解析约定含 target triple，需要提供可复现的重命名步骤。

### 解决问题

1. 删除 Tauri `externalBin` 配置，补充安装包验收项：发布物不得包含 `ffmpeg*.exe`、`ffprobe*.exe`、`ffmpegthumbnailer*.exe`。
2. 在仓库根目录新增 MIT `LICENSE`，并在 npm、Cargo 元数据及安装包许可证资源中声明 MIT；第三方许可证说明改为“用户可选安装工具”的来源说明，不再把它们表述为随包组件。
3. 收紧 `.gitignore`：整个 `sidecars` 目录仅保留说明文件，下载目录、压缩包和相关可执行文件均不进入版本控制。
4. 新增纯本地 `scripts/install-local-media-tools.ps1`。它接收用户已下载的目录和应用安装目录，复制、重命名到 `sidecars`，不访问网络且保留原始下载文件。
5. README 记录官方/上游下载入口、许可证判断、文件名、默认及自定义安装目录和脚本调用方式；ffmpegthumbnailer 缺失时继续使用既有 FFmpeg 回退能力。

### 验证与待验收

1. `git ls-files` 已确认没有任何媒体二进制，`git check-ignore` 已确认本机 sidecar 和下载目录均受忽略规则覆盖。
2. 仍需在干净 Windows 环境打包并打开 NSIS 安装包，确认安装包不含媒体二进制；安装后手动运行脚本并验证三种工具缺失、仅 FFmpeg/FFprobe、完整工具三种路径。

### 总结问题

1. **版本控制排除与发布排除是两道独立防线。** Git 忽略文件无法替代构建清单审查。
2. **MIT 仅覆盖项目自有代码。** 用户自行取得的媒体工具保留其原许可证，应用调用或文件重命名不会改变该事实。

## 43. 2026-07-23 至 2026-07-24：v2.0 通用文件工作区、预览链与桌面关闭链收口

### 本轮范围

本轮以 `V2.0开发计划.md` 为实现依据，将原先围绕视频建立的工作区主链扩展为通用文件工作区，实际交付范围包括：

1. 工作区枚举和管理视频、图片、文本/代码及其他普通文件。
2. 保留现有视频播放器、FFmpeg 回退、媒体信息和缩略图行为。
3. 新增图片预览、图片缩略图和图片保护阈值。
4. 新增受限文本读取、编码识别、Prism 语法高亮和截断提示。
5. 将工作区、文件操作、焦点、排序、界面文案及 IPC 主链中的通用 `video` 语义迁移为 `file`。
6. 修复新预览模块接入后前端未进入应用状态加载阶段的问题。
7. 修复应用关闭时 Explorer 文件剪贴板活对象不能正确移交的问题。
8. 将中栏宽度约束修正为“一张卡片宽度”的动态最小宽度。
9. 修复高像素图片默认显示不完整、缩放/角度状态不可见，以及内容格式与 `.png` 扩展名不一致时解码失败的问题。

音频、PDF、Quick Look、Office、压缩包和富文本 Markdown 不在本轮范围内，与 v2.0 开发计划的明确边界保持一致。

### 发现问题

#### 1. 工作区主链仍以视频为唯一数据对象

1. 后端 `WorkspaceListing` 只返回 `videos`，扫描阶段只保留配置中的视频扩展名。
2. 前端选择、焦点、搜索、排序、虚拟列表、缩略图队列、文件任务和预览栏都以 `FileEntry` 为中心。
3. 回收、重命名、外部打开和拖出操作会再次校验视频扩展名，因此即使扫描层加入普通文件，后续文件操作仍会拒绝图片、文本和未知文件。
4. 许多用户可见文案仍是“视频”“没有匹配的文件”“受支持的文件”，与通用工作区目标冲突。
5. 视频时长、分辨率和 FFmpeg 进程是视频专属能力，不能因为通用化而无条件应用到图片、文本和其他文件。

#### 2. 配置和缓存缺少跨类型身份

1. 配置只保存视频扩展名，无法区分图片、文本和未知文件。
2. 文本高亮需要“扩展名 → Prism 语言”的显式映射，否则只能依赖不稳定的自动识别。
3. 图片原图解码可能消耗大量内存，需要文件体积和总像素两道可配置阈值。
4. 旧缩略图缓存只用取帧位置区分视频缩略图。图片缩略图接入后，如果缓存身份不包含预览类型和处理器版本，视频与图片会共享含义不完整的键。
5. 配置升级如果直接覆盖原文件，一旦新增字段迁移或校验失败，就会破坏仍可使用的旧配置。

#### 3. 新预览模块让启动日志停在后端初始化完成

现场日志最后一条为：

```text
FileSweeper backend initialized: version=0.8.0, last_workspace=<none>, favorites=0, sidecar_concurrency=10
```

后续没有出现 `Loading application state`。

这条日志位于 Tauri `.setup` 的末端，只能证明 Rust 后端、日志插件、配置快照和窗口已完成初始化，不能证明 React 已成功挂载。正常情况下，前端 `App` 的第一个 `useEffect` 会立即调用 `load_application_state`，后端随后记录 `Loading application state`。该日志完全缺失，说明故障边界位于：

```text
Tauri setup 完成
  -> WebView 加载前端入口
  -> 顶层模块求值
  -> React createRoot
  -> App useEffect
  -> invoke(load_application_state)
```

检查本轮新增依赖后，问题集中到 `PreviewPanel -> TextPreview -> Prism 语言组件` 的顶层导入。Prism 的语言组件不是独立纯模块，而是对 Prism core 的副作用扩展；`cpp`、`tsx` 等语法还依赖 `c`、`jsx`、`typescript` 等基础语法。如果依赖语法早于 core 或晚于派生语法初始化，模块会在 React 挂载前抛错，因此后端看起来像“卡住”，实际没有收到任何前端状态加载请求。

#### 4. 关闭窗口时剪贴板移交失败

关闭日志包含：

```text
Unable to flush the Explorer file clipboard:
不允许执行此操作，因为发出调用的应用程序不是剪贴板上的数据的所有者。
(0x80004005)
```

应用使用 `OleSetClipboard` 发布 Explorer 可识别的 `IDataObject`，并把活对象保留在剪贴板 STA 线程上，以支持 OLE 延迟渲染。旧关闭逻辑只要本地槽位中存在对象，就无条件调用 `OleFlushClipboard`。

问题在于，本地仍持有一个 `IDataObject` 引用，不等于它仍是系统剪贴板当前所有者。用户可能在关闭应用前已经由其他程序复制了新内容。此时：

1. 系统剪贴板所有权已经转移。
2. 本地线程仍持有旧对象引用。
3. `OleFlushClipboard` 只允许当前所有者执行。
4. 无条件刷新因此返回 `0x80004005`。

这不是数据序列化失败，也不是 STA 线程未初始化，而是关闭逻辑缺少所有权分支。

#### 5. 中栏宽度约束方向和计算均不完整

中栏需要的是最小宽度，而不是最大宽度。目标是在拖动左右分隔条或缩小窗口时，至少保留一张固定宽度卡片及其布局空间，同时允许中栏继续向更宽方向增长。

旧计算存在三个问题：

1. 使用固定 `252px`，只粗略包含 `220px` 卡片和部分内边距。
2. 没有计入滚动条预留空间。
3. 预览栏开关会改变分隔条数量，但最小百分比没有随之重新计算。

`react-resizable-panels` 的 `minSize` 使用百分比，因此不能直接写固定像素；必须根据面板组当前像素宽度动态换算。

#### 6. 图片默认倍率基于固定范围，高像素图片无法完整显示

图片预览最初沿用库的固定缩放范围 `0.1～8`。当原图宽高远大于预览框时，完整显示所需倍率可能低于 `10%`。例如预览框宽度为 800px、图片宽度为 16000px 时，横向适应倍率只有 `5%`；最小倍率被夹到 `10%` 后，图片必然被裁切。

此外，默认状态没有同时根据容器宽度和高度计算，旋转 90° 后也没有交换参与适应计算的宽高。

#### 7. 图片变换状态不可见

用户可以通过滚轮、按钮和拖拽改变图片状态，但预览区没有持续显示当前倍率和旋转角度。仅凭画面无法判断当前是原始 100%、适应窗口还是经过多次旋转后的状态。

#### 8. 部分 `.png` 报无效 PNG 签名

现场文件报错：

```text
Unable to read image dimensions:
Format error decoding Png: invalid PNG signature
```

读取文件头后发现其起始字节为 `FF D8 FF`，这是 JPEG 签名；真正的 PNG 签名应为：

```text
89 50 4E 47 0D 0A 1A 0A
```

因此文件不是损坏的 PNG，而是“JPEG 内容 + `.png` 扩展名”。旧实现使用 `ImageReader::open(path).into_dimensions()`，解码器按扩展名选择 PNG，随后自然在签名校验阶段失败。图片缩略图解码若继续使用扩展名，也会出现同一问题。

### 探索与方案取舍

#### 1. 通用化只改变通用边界，不重写视频专属状态机

本轮没有把 `PreviewPlayer`、FFmpeg 和媒体元数据符号机械改名。采用的边界是：

- 工作区扫描、选择、焦点、排序、文件操作和通用预览入口使用 `FileEntry`。
- 视频播放器、转码控制器、时长/分辨率探测继续保持视频专属语义。
- TypeScript 移除了冗余的 `VideoEntry` 兼容别名；播放器直接接收 `FileEntry`，但其视频播放接口与调用名保持不变。
- 只有焦点文件为视频时，删除前才停止转码预览；图片、文本和其他文件不触发 FFmpeg 清理。

这样可以消除通用工作区中的残留视频假设，又不在同一轮重写已经稳定的播放意图、手动暂停和转码接管状态机。

#### 2. 扫描阶段只做扩展名分类，不打开文件内容

大目录进入速度依赖扫描阶段只读取目录项和文件系统元数据。因此分类规则保持为：

```text
videoExtensions 命中 -> video
imageExtensions 命中 -> image
textExtensions 命中  -> text
其余普通文件          -> other
```

内容签名探测只用于用户已经选中或进入缩略图队列的图片，不进入目录扫描主链。

`.nomedia` 的语义同步调整为“抑制媒体”，即视频和图片被隐藏，但文本和其他普通文件仍可出现在工作区。这样 `.nomedia` 不会把通用文件工作区整体变成空目录。

#### 3. 配置迁移采用缺失字段默认值、迁移备份和集中校验

配置版本从 4 升级到 5。新增字段使用 serde 默认值兼容旧配置，但版本迁移落盘前仍创建 `config-migration` 备份。扩展名统一执行：

1. 转小写。
2. 补全前导 `.`。
3. 排序和去重。
4. 按视频、图片、文本的顺序消除跨组冲突。
5. 删除不属于文本扩展名或未打包 Prism 语言的映射。

图片保护范围在后端统一校验：

- 文件大小：`1～512 MiB`。
- 总像素：`1～500 MP`。
- 默认值：`50 MiB`、`100 MP`。

#### 4. 图片格式兼容集中在唯一读取入口

没有在 `inspect_image_preview`、缩略图尺寸读取和缩略图完整解码三个位置分别堆叠“PNG 失败再试 JPEG”的分支，也没有按扩展名枚举回退解码器。

新增唯一的 `open_image_reader_by_content`：

```text
ImageReader::open(path)
  -> with_guessed_format()
  -> dimensions 或 decode
```

`with_guessed_format()` 根据文件头选择已启用的真实格式。图片预览检查和缩略图生成都复用该入口，因此扩展名误标只修一次，错误信息仍保留在调用方的尺寸读取或完整解码阶段。

#### 5. 缩放库的倍率范围与移动边界

`react-zoom-pan-pinch` 的 API 仍接受 `minScale` 和 `maxScale`。取消旧的 `10%～800%` 范围后，保留可操作的 1% 最小倍率：

```text
minScale = 0.01
maxScale = Number.MAX_VALUE
limitToBounds = true
```

上限不再受 `800%` 截断；默认适应倍率可低于 10%，但不会低于 1%。`limitToBounds` 只约束平移坐标和缩放锚点，防止图片离开预览框，不改变倍率范围。

默认适应倍率使用严格的宽高联合计算：

```text
fitScale = min(
  previewWidth / imageWidth,
  previewHeight / imageHeight
)
```

旋转为 90° 或 270° 时，先交换参与计算的图片宽高。图片加载完成和“适应窗口”操作都会使用同一函数。

#### 6. 中栏最小宽度使用像素目标到百分比的实时换算

中栏像素目标由以下项目组成：

```text
卡片宽度 220
+ 网格左右内边距 32
+ 滚动条预留 10
= 最小内容宽度 262px
```

再根据预览栏状态扣除面板组中的拖动条：

```text
可分配面板宽度 = 面板组宽度 - 拖动条数量 × 12px
中栏 minSize% = 262 / 可分配面板宽度 × 100
```

预览栏打开时有两条拖动条，关闭时有一条。`ResizeObserver` 监听面板组宽度，预览栏开关也会重新建立计算，因此窗口缩放和栏位开关都不会沿用过期百分比。

### 解决问题

#### 1. 通用文件模型和工作区扫描

1. Rust 和 TypeScript 新增 `FileEntry`、`FileKind` 与 `PreviewCapability`。
2. `WorkspaceListing.files` 改为 `WorkspaceListing.files`。
3. 工作区枚举全部普通文件，按扩展名分类为 `video`、`image`、`text`、`other`。
4. 视频继续读取已有缓存媒体信息；图片、文本和其他文件不会触发 ffprobe。
5. 视频缩略图按取帧位置查询，图片缩略图使用独立的 `image-v1` 缓存身份，文本和其他文件不查询缩略图。
6. 列表增加“类型”列，并支持按类型、修改时间排序。
7. 网格缺省图标根据文件类型显示视频、图片或文本图标。
8. 搜索空状态、ARIA 标签、扫描日志和工作区完成日志改为通用文件文案。
9. 工作区焦点字段由 `videoPath` 改为 `filePath`，Rust 使用 serde alias 读取旧字段，实现无损迁移。

#### 2. 通用文件操作

1. `normalize_video_paths` 改为 `normalize_file_paths`，只要求路径存在、规范化且为普通文件，不再要求扩展名属于视频配置。
2. IPC 命令迁移为：

```text
recycle_files
rename_file
open_file_externally
```

3. 复制、移动、剪贴板、粘贴和拖出同样接受所有普通文件。
4. 回收和重命名完成后，工作区从 `files` 集合更新选择、焦点和缩略图状态。
5. 删除焦点视频前仍停止其转码进程；非文件直接进入文件操作队列。
6. 视频播放器错误态的外部打开也统一调用 `open_file_externally`，不再维护视频专用外部打开入口。

#### 3. 配置、依赖和缓存迁移

1. 前端加入 `prismjs`、`react-zoom-pan-pinch` 和 Prism 类型声明。
2. Rust 加入 `encoding_rs` 与按需启用 GIF、JPEG、PNG、WebP、BMP、ICO、TIFF 的 `image` crate。
3. Windows API 功能加入系统 ANSI 代码页读取能力。
4. 设置页增加图片格式、文本格式、图片大小上限和图片像素上限。
5. 配置版本迁移时先创建带用途标记的备份，再原子写入新配置。
6. 媒体缓存版本从 2 升到 3；旧条目默认标记为视频缩略图。
7. 新缓存条目增加：

```text
previewType
processorVersion
```

视频使用 `video-thumbnailer-v1`，图片使用 `image-320-jpeg-v1`，两类缩略图继续共用配额、LRU 和索引持久化。

#### 4. 图片预览

1. `inspect_image_preview` 先检查文件大小，再按真实内容格式读取尺寸并检查总像素。
2. 通过已有本地回环流服务生成图片预览 URL，不把整张原图经 IPC Base64 返回。
3. 前端 `ImagePreview` 提供：

```text
滚轮缩放
按钮放大/缩小
拖拽平移
100%
适应窗口
逆时针/顺时针 90° 旋转
加载失败降级
```

4. 默认倍率按预览框和图片实际宽高计算，允许低于 10%，高像素图片初次打开即可完整显示。
5. 适应操作会同时重置旋转角度并重新居中。
6. 旋转角度规范到 `0°、90°、180°、270°`，避免显示 `360°`、`450°` 等等价角度。
7. 图片预览框内部底部新增悬浮胶囊，通过 `onTransformed` 实时显示：

```text
旋转 90° · 缩放 12%
```

8. 胶囊不拦截指针事件，使用半透明表面、边框、阴影和背景模糊保持深浅主题下的可读性。
9. 图片切换时清空旧 URL、错误、旋转和倍率状态；异步响应通过 `active` 标记阻止旧文件结果更新已卸载预览。

#### 5. 图片缩略图

1. 新增独立的 `generate_image_thumbnails` 命令。
2. 图片批次在 `spawn_blocking` 中执行，不占用 FFmpeg sidecar 许可。
3. 同一图片先检查体积和总像素，再解码并缩放为最长边不超过 320px 的 JPEG。
4. 图片批次内部顺序处理，避免同时展开多张大图。
5. 缩略图先写入 `.tmp.jpg`，完成后在缓存维护锁内替换正式文件并记录索引。
6. 成功项继续发送 `thumbnail-generated` 事件，失败项进入批次失败列表，不影响同批其他图片。
7. 前端可见项队列按 `FileKind` 将视频和图片分发到不同命令；文本和其他文件直接跳过缩略图生成。

#### 6. 文本与代码预览

1. 后端最多读取文件开头 `2 MiB + 1 byte`，多出的 1 byte 用于判断是否截断，返回前再截到 2 MiB。
2. 编码识别顺序为：

```text
UTF-8 BOM
UTF-16 LE BOM
UTF-16 BE BOM
严格 UTF-8
系统 ANSI 代码页
```

3. Windows ANSI 回退根据系统 ACP 选择 GBK、Shift-JIS、EUC-KR 或 Windows-1252。
4. 非 Unicode 内容含空字节时按二进制文件降级，不向 Prism 传入任意二进制数据。
5. 返回结构包含内容、编码、总字节数、是否截断、是否可读和失败原因。
6. 前端根据扩展名映射已打包的 Prism grammar，不进行自动语言探测。
7. 预览显示编码、2 MiB 截断提示、行号和横向滚动；未知映射安全退回纯文本。
8. Prism core 先初始化，`c` 先于 `cpp`，`jsx` 和 `typescript` 先于 `tsx`，避免派生 grammar 在前端顶层模块求值阶段访问未注册依赖。
9. 图片、文本、其他文件和加载失败统一保留“用系统程序打开”入口。

#### 7. 初始化链修复与诊断结论

1. 保留后端 `.setup` 的 `backend initialized` 检查点。
2. 前端 `App` 在调用 `load_application_state` 前记录“开始加载后端应用状态”，成功和失败分别记录结果。
3. 后端命令入口记录 `Loading application state` 和配置版本、根目录、收藏夹等完成信息。
4. 修正 Prism core 和依赖 grammar 的导入顺序，使前端能完成顶层模块求值、挂载 React 并进入 `load_application_state`。
5. 由日志边界确认：若以后再次只看到 `backend initialized` 而看不到 `Loading application state`，应优先检查 WebView 控制台、前端入口和顶层模块，而不是继续在 Rust 配置加载或工作区扫描中排查。

#### 8. Explorer 文件剪贴板关闭移交

1. 在同一个剪贴板 STA 线程上保留活 `IDataObject`，继续支持 OLE 延迟渲染。
2. 关闭时先调用 `OleIsCurrentClipboard` 检查本地对象是否仍为系统剪贴板所有者。
3. 分支规则为：

```text
没有活对象
  -> 直接成功，不执行刷新

对象已被其他程序替换
  -> 不调用 OleFlushClipboard
  -> 释放本地旧对象引用

对象仍是当前所有者
  -> 调用 OleFlushClipboard 物化数据
  -> 成功后释放本地对象

所有权检查返回异常 HRESULT
  -> 保留明确错误，不伪装成刷新成功
```

4. 该修复消除了“不是剪贴板所有者却尝试刷新”的 `0x80004005`，同时保证应用仍为所有者时，关闭后 Explorer 可以继续读取已经物化的文件列表和 `Preferred DropEffect`。
5. 增加“没有活剪贴板对象时关闭刷新为 no-op”的 Windows 回归测试。

#### 9. 中栏一张卡片最小宽度

1. 将卡片宽度、网格水平内边距、滚动条预留和拖动条宽度提取为明确常量。
2. 根据面板组实时宽度把 262px 内容目标换算为 `minSize` 百分比。
3. 右侧预览栏打开和关闭时分别按两条、一条拖动条计算。
4. 使用 `ResizeObserver` 在窗口或面板组尺寸变化时更新。
5. 该限制只约束最小宽度，不限制中栏最大宽度；用户仍可继续向两侧扩大工作区。

#### 10. 伪 PNG 内容格式修复

1. 尺寸检查改为通过 `open_image_reader_by_content` 打开。
2. 图片缩略图的尺寸读取和完整解码同样复用该入口。
3. JPEG、PNG、GIF、WebP、BMP、ICO、TIFF 在扩展名错误但内容签名可识别时，优先采用真实格式。
4. 新增回归样本：用 JPEG 编码写入名为 `jpeg-content.png` 的文件，再读取尺寸并断言为 `3 × 2`。
5. 修复没有加入多层捕获、重复重试或按格式枚举的防御性代码；格式判断集中在一处。

### “video” 残留命名处理原则

本轮没有把源码中所有字符串 `video` 一律删除，而是按语义处理：

1. 已迁移为通用文件语义：
   - `WorkspaceListing.files`
   - `FileEntry`
   - `filePath`
   - `recycle_files`
   - `rename_file`
   - `open_file_externally`
   - 普通文件路径规范化
   - 工作区搜索、空状态、ARIA 和扫描日志
2. 继续保留视频语义：
   - `PreviewPlayer`
   - `VideoStreamServer`
   - FFmpeg/ffprobe/ffmpegthumbnailer
   - 视频转码、Seek、播放意图、时长和分辨率探测
   - `videoExtensions` 与 `managedVideoExtensions`
3. TypeScript 的 `FileEntry` 仅作为既有播放器的兼容别名，并带弃用说明；通用工作区代码使用 `FileEntry`。

该规则避免两种错误：一是普通图片和文本继续被视频校验拒绝，二是把真正只服务视频的转码和播放器代码改成含义模糊的通用名称。

### 验证

#### 1. 自动检查

完整执行 `npm run check`，结果全部通过：

- `tsc --noEmit` 通过。
- Vite 生产构建通过，共转换 1655 个模块。
- Vitest 3 个前端测试全部通过。
- `cargo fmt --check` 通过。
- `cargo clippy --all-targets -- -D warnings` 通过。
- Rust 41 个测试全部通过，0 失败、0 忽略。
- `git diff --check` 通过。

#### 2. 新增或覆盖的关键回归

1. JPEG 内容使用 `.png` 扩展名时，按内容签名读取尺寸成功。
2. 没有活 Explorer 文件剪贴板对象时，关闭刷新安全返回。
3. 旧缓存条目补齐 `previewType` 和 `processorVersion` 后仍可加载。
4. 既有播放器的晚到流、手动暂停和媒体回退测试继续通过。
5. 配置、工作区、`.nomedia`、缩略图缓存、文件任务和 sidecar 许可池的既有 Rust 测试继续通过。

#### 3. 现场证据

1. 报错 `.png` 的文件头为 JPEG 的 `FF D8 FF`，与后端错误“PNG signature 无效”形成完整因果链。
2. 关闭剪贴板错误明确指出调用者不是所有者，与旧代码无条件 `OleFlushClipboard` 的行为一致。
3. 启动日志停在 `.setup` 末端、缺少 `load_application_state` 命令入口日志，证明阻塞点在前端请求发出之前。

### 尚待真实桌面验收

自动检查不能替代以下 Windows/WebView2 场景，发布前仍需人工复验：

1. 使用 Explorer 从应用复制和剪切文件，分别在应用仍持有剪贴板、剪贴板已被其他程序替换两种状态下关闭应用，再验证外部粘贴结果。
2. 在 100%、125%、150% DPI 和不同窗口宽度下拖动左右栏，确认中栏始终至少容纳一张卡片且没有错误的最大宽度限制。
3. 使用超宽、超高和超高像素图片验证默认完整显示、100%、适应、旋转、滚轮缩放及悬浮胶囊状态同步。
4. 使用真实 JPEG 内容误标 `.png` 的文件验证原图预览与缩略图均成功。
5. 使用 UTF-8、UTF-16 LE/BE、GBK 文本和超过 2 MiB 的文件验证编码标签、行号、截断提示和系统打开降级。
6. 在图片、文本、视频、未知文件之间快速切换，确认旧异步结果不覆盖当前预览，视频切走后不残留 FFmpeg 进程。

### 总结问题

1. **后端最后一条初始化日志不等于后端卡死。** 如果下一条应由前端 IPC 触发，必须先用日志边界判断请求是否真正发出。
2. **Prism 语言组件具有初始化依赖。** 顶层副作用模块的顺序错误会发生在 React 挂载前，普通组件错误边界无法捕获。
3. **通用化应围绕业务边界，而不是全局字符串替换。** 工作区和文件操作需要通用模型，播放器和 FFmpeg 仍应保持视频语义。
4. **大目录扫描不能为了分类而读取内容。** 内容签名探测应延迟到预览或缩略图阶段。
5. **格式扩展名只是提示，不是图片内容真相。** 实际解码应在唯一入口按签名选择格式，避免同一兼容逻辑散落到多个调用方。
6. **适应窗口的倍率由两个维度共同决定。** 固定最小倍率会让超高分辨率图片永远无法完整显示。
7. **库要求的数值边界不应演变成产品限制。** 使用机器数值极值满足 API 契约，可以移除用户可见的固定缩放范围。
8. **OLE 对象引用和系统剪贴板所有权是两个状态。** 关闭时必须先确认所有权，再决定物化还是仅释放旧对象。
9. **面板的像素设计目标需要实时换算为百分比。** 卡片、内边距、滚动条和拖动条都属于最小宽度计算的一部分。
10. **开发笔记必须保留发现、定位、取舍、实现和验证的完整链路。** 只有这样，后续遇到相似启动、剪贴板、图片解码或布局问题时，才能复用本轮结论，而不是重新从现象猜测。

## 44. 2026-07-24：极小倍率下图片放大锚点漂移与移动边界修复

### 发现问题

图片预览取消固定 `10%～800%` 倍率限制后，高像素图片可以按预览框缩小到任意所需倍率，但随之暴露了新的交互问题：

1. 用户连续缩小图片，使图片在预览框中只剩很小的可见区域。
2. 鼠标没有悬浮在图片本体上，而是停在预览框的空白区域。
3. 此时使用滚轮放大，图片没有围绕当前可见位置展开，而是向远处偏移。
4. 重复操作后，图片可以完全离开预览框，用户无法继续通过滚轮或拖拽找回原图，只能点击“适应窗口”重置。

问题只在极小倍率下特别明显，因此容易被误认为是倍率下限过低。实际需要限制的是平移和缩放锚点产生的位置，而不是重新限制缩放倍率。

### 探索问题

#### 1. 缩放位置的计算方式

`react-zoom-pan-pinch` 使用鼠标在包装容器中的坐标作为缩放锚点，新的平移位置大致按以下公式计算：

```text
positionX' = positionX - mouseX × (newScale - oldScale)
positionY' = positionY - mouseY × (newScale - oldScale)
```

当鼠标位于图片之外时，`mouseX`、`mouseY` 仍然是合法的容器坐标。图片倍率很小时，图片本身接近一个很小的目标；如果允许位置无限制变化，每次图外放大都会继续叠加相对该鼠标点的位移，最终把图片推到容器之外。

#### 2. 取消倍率限制时错误地同时取消了位置限制

上一轮为了移除 `0.1～8` 的倍率夹取，将配置写成：

```text
minScale = Number.EPSILON
maxScale = Number.MAX_VALUE
limitToBounds = false
```

前两个参数决定可缩放倍率，第三个参数决定平移和缩放后的坐标是否受内容边界限制。它们是相互独立的能力。

把 `limitToBounds` 一并关闭并不是取消倍率限制的必要条件，反而允许图片坐标无限漂移。这是本次问题的直接原因。

#### 3. 没有采用的方案

1. **没有恢复原来的 10% 最小倍率。** 这会重新造成高像素图片无法完整显示，退回上一轮已经修复的问题；后续将最小倍率定为仍可操作的 1%。
2. **没有手写固定最大位移。** 图片尺寸、旋转状态、倍率、预览框尺寸和 DPI 都会影响有效边界，固定像素阈值无法正确覆盖这些组合。
3. **没有在每次滚轮事件后强制调用“适应窗口”。** 这会破坏以鼠标为中心的正常缩放，也会让用户无法查看图片局部细节。
4. **没有额外增加定时复位或多层坐标纠正。** 缩放库已经根据包装容器、内容尺寸和当前倍率维护边界，直接恢复其边界能力即可。

### 解决问题

在 `ImagePreview` 的 `TransformWrapper` 中保留取消旧上限、最小倍率为 1% 的范围：

```text
minScale = 0.01
maxScale = Number.MAX_VALUE
```

同时将位置配置改为：

```text
limitToBounds = true
```

最终行为为：

1. 图片缩小到比预览框更小时，库将其位置约束在容器中的有效居中位置，不能被拖到框外。
2. 鼠标位于图片之外滚轮放大时，按鼠标计算出的新位置会被夹到当前倍率允许的边界。
3. 图片放大到超过预览框时，仍可在有效范围内平移查看局部，但不能把整张图片完全移出预览区。
4. “100%”“适应窗口”、按钮缩放、滚轮缩放和拖拽平移继续使用同一个变换状态。
5. 高像素图片默认适应倍率仍可低于 10%；最小倍率固定为 1%，避免接近 0% 的失控和不可操作状态。

本次实际代码改动只有恢复 `TransformWrapper` 的边界约束，没有新增重复坐标状态、事件监听或防御性复位逻辑。

### 验证

修复后完整执行 `npm run check`，结果通过：

- TypeScript 类型检查通过。
- Vite 生产构建通过，共转换 1655 个模块。
- Vitest 3 个前端测试全部通过。
- Rust 格式检查和 Clippy 严格警告检查通过。
- Rust 41 个测试全部通过，0 失败、0 忽略。
- `git diff --check` 通过。

### 真实交互验收步骤

1. 打开一张高分辨率图片并连续缩小到图片远小于预览框。
2. 将鼠标移动到图片之外的棋盘格空白区域。
3. 连续滚轮放大，确认图片始终从预览框内的有效位置放大，不会向远处漂移。
4. 在极小倍率下尝试拖拽，确认图片不能完全离开预览框。
5. 放大到图片超过预览框后拖拽四个方向，确认仍能查看边缘内容，但不能把整张图片丢出视区。
6. 点击“适应窗口”和“100%”，确认居中、倍率胶囊和旋转状态仍同步。

### 总结问题

1. **缩放范围和移动范围是两个独立约束。** 取消倍率上下限不代表应取消内容位置边界。
2. **图外缩放仍会产生合法鼠标锚点。** 如果位置不受限制，极小内容会放大这类锚点带来的累计偏移。
3. **优先使用组件库已经维护的几何边界。** 手写位移阈值会重复状态并遗漏容器、倍率和 DPI 变化。
4. **修复交互回归时要保留原需求。** 本次恢复移动边界，并将最小倍率收敛到 1%，没有恢复妨碍高像素图片完整适应的 10% 下限。

## 45. 2026-07-24：图片切换返回后适应倍率被 100% 状态覆盖

### 发现问题

在右栏按“图片 A → 图片 B → 图片 A”切换后，图片 A 没有重新按预览框宽高适应，而是回到 `100%`。问题在浏览器已缓存图片 A 时更容易复现：用户看到图片文件已回到右栏，但高分辨率图片只显示局部。

### 原因

`ImagePreview` 在同一 React 组件实例内接收新的 `file` 属性，图片、旋转状态和 `TransformWrapper` 都会跨文件复用。缩放库因此可能保留上一张图片的内部变换状态。图片 A 再次命中浏览器缓存时，`img` 的 `load` 事件和缩放库自身的挂载/状态初始化发生得很接近，原本的 `fitImage` 调用可能先执行，随后被库的默认 `scale = 1` 覆盖。

这不是图片宽高读取失败，也不是旧请求覆盖新请求：文件路径、图片 URL 和异步响应均已更新，丢失的是该文件进入预览时应有的独立变换生命周期。

### 解决问题

1. 给 `ImagePreview` 增加 `key={file.path}`。文件路径变更时，React 会卸载旧图片的整个预览实例并为新文件创建新的图片、旋转和缩放状态，旧图片的平移和倍率不会泄漏到当前图片。
2. 图片 `onLoad` 保存当前图片节点，并通过 `requestAnimationFrame` 在浏览器完成当前帧布局后调用 `fitImage`。这样适应计算读取的是已挂载的预览框尺寸，并在缩放库初始状态就绪后设置最终倍率。
3. 旋转、倍率胶囊、1% 最小倍率和移动边界继续由新实例统一管理；没有增加延时重试、额外请求或重复状态。

### 验证

真实交互应按以下顺序验证：

1. 打开高分辨率图片 A，确认默认完整适应。
2. 缩放或平移 A 后切换到图片 B。
3. 再切回 A，确认 A 再次按当前预览框适应，而不是复用 B 或 A 上次的 100%/手动倍率。
4. 连续在多张已缓存和未缓存图片间切换，确认旋转角度、缩放胶囊和图像位置均从当前文件的默认状态开始。

自动验证继续执行 `npm run check`，覆盖 TypeScript 构建、前端测试、Rust 格式/Clippy 和 Rust 回归测试。

### 总结问题

1. **异步图片加载完成不等于变换库状态已经可用。** 需要在布局稳定后再计算依赖容器尺寸的适应倍率。
2. **文件切换应拥有独立的变换生命周期。** 仅重置 React 状态不足以清除第三方组件持有的内部缩放状态。
3. **缓存路径更容易暴露时序问题。** 缓存图片加载极快，不能假设其事件一定发生在组件内部初始化之后。

## 46. 2026-07-24：滚轮缩放的变换动效

### 需求

图片预览的滚轮缩放需要有连续的变换动效，缓动曲线固定为：

```text
cubic-bezier(0.2, 0, 0, 1)
```

动效只服务滚轮缩放，不应让鼠标拖拽平移变得滞后。

### 探索问题

`react-zoom-pan-pinch` 将缩放和平移写入 `TransformComponent` 的外层内容节点：

```text
translate3d(positionX, positionY, 0) scale(scale)
```

图片元素自身的 `transform` 只用于旋转。因此把 transition 写在 `img` 上不会平滑滚轮缩放，必须作用于 `.image-preview-content`。

库的滚轮事件顺序为：

```text
onWheelStart
  -> 根据鼠标锚点计算位置与倍率
  -> 写入内容节点 transform
  -> onWheelStop（停止滚动后）
```

这允许在变换写入前同步添加一个临时 CSS 类，不需要额外维护倍率、位置或定时器状态。

### 解决问题

1. `onWheelStart` 为缩放内容节点添加 `is-wheel-zooming`。
2. 该类只在滚轮期间应用：

```css
transition: transform 140ms cubic-bezier(0.2, 0, 0, 1);
```

3. `onWheelStop` 移除该类，后续静止状态不保留 transition。
4. `onPanningStart` 同样移除该类，确保用户从滚轮立即切换为拖拽时，平移不会继承缩放过渡。
5. 图片本身原有的旋转过渡保持独立，避免将旋转、缩放和平移混入同一 CSS 属性来源。

### 总结问题

1. **变换动效必须施加在实际承载缩放的节点。** 图片元素和缩放内容容器的 transform 职责不同。
2. **滚轮事件回调适合控制短生命周期视觉状态。** 直接切换 CSS 类比增加 React 状态和计时器更贴合库的事件边界。
3. **缩放和平移的动效需求不同。** 滚轮缩放可以平滑插值，拖拽平移应保持直接跟手。

## 47. 2026-07-24：产品整体迁移为 FileSweeper，清除通用工作区残留视频命名

### 发现问题

v2.0 已支持图片、文本和其他普通文件，但产品名、安装标识以及工作区层仍混有大量历史媒体命名：旧品牌、旧包标识、旧条目类型、旧可见列表、旧选择集、旧卡片 CSS 类和“已选择 N 个视频”等。它们会让通用文件能力在代码阅读、界面文案和发布产物中仍呈现为单一视频工具。

同时，视频播放链路本身并不是需要泛化的对象：浏览器 `<video>` 元素、FFmpeg 转码、视频元数据探测、流服务和相应 Tauri 命令已有稳定的调用约定。若为了清除文字而改动这些接口，会把品牌迁移扩大为不必要的媒体回归风险。

### 探索问题

首先按出现位置区分两类名称：

1. **通用文件工作区名称**：选择集、可见列表、卡片/列表 CSS、缩略图组件、文件操作、焦点恢复、右键菜单、拖放和用户文案。这些名称承载的是 `FileEntry`，必须迁移为 `file`。
2. **视频专属接口名称**：`PreviewPlayer` 的播放属性、`get_video_stream_url`、`probe_video_metadata_batch_command`、FFmpeg、媒体流服务、视频扩展名设置与浏览器原生媒体 API。这些名称继续保留 `video`，避免破坏已验证的播放和转码契约。

迁移中还发现 PowerShell 的 `-replace` 默认不区分大小写。一次机械替换会把 Lucide 的 `Video` 图标错误改成小写 `file`，从而被 TypeScript 构建立即捕获。因此后续标识符替换统一使用大小写敏感的 `-creplace`，品牌名称、包名和类型名则使用有序的精确字符串替换。

### 解决问题

1. 产品名、npm/Cargo 包名、Tauri 标识、窗口标题、安装钩子、日志文件名和文档统一改为 `FileSweeper` / `file-sweeper` / `file_sweeper`。
2. 通用工作区删除 `VideoEntry` 别名，统一直接使用 `FileEntry`；`WorkspaceContextMenu`、选择集、可见列表、批量操作、焦点和外部拖入名称全部改为 `files` / `file`。
3. 通用组件重命名为 `FileThumbnail` 与 `FileDetails`；网格、列表、缩略图 CSS 类统一使用 `.file-*`，数据属性同步改为 `data-file-path` 和 `dataset.filePath`。
4. 文件操作、工作区焦点持久化、Windows Shell 拖放、日志和用户提示改为“文件”语义；测试路径也使用 `Files`，不再把测试样本目录误表述为视频目录。
5. 保留视频播放器、FFmpeg、媒体元数据和 Tauri 视频调用名；开发计划与架构文档明确记录这一边界，避免未来把通用命名迁移误解为播放器协议重写。

### 验证

1. 全库检索确认旧品牌的 PascalCase、连字符、小写下划线和全小写形式均为 0。
2. 前端检索确认旧通用标识 `VideoEntry`、`VideoThumbnail`、`VideoDetails`、`selectedVideos`、`visibleVideos`、`videos` 和 `.video-*` 均为 0。
3. 执行 `git diff --check`，无空白错误。
4. 执行 `npm run check`：TypeScript/Vite 构建通过，Vitest 3 个测试文件通过，Rust 格式检查、Clippy 与 41 项 Rust 测试通过。

### 总结问题

1. **产品泛化应优先清理通用边界。** 数据模型、状态集合和用户文案决定用户对产品能力的理解，不能继续携带单一媒体类型的历史名称。
2. **接口名是否迁移取决于语义归属，而非字面相同。** 视频流和转码接口仍是视频专属能力，保留其调用约定比机械改名更安全。
3. **批量重命名必须验证大小写。** 在大小写不敏感替换环境中，构建与全量检索是发现这类错误的必要闭环。

## 48. 2026-07-24：文本预览未严格限制为文件开头 2 MiB

### 发现问题

使用一个约 6 MiB 的 HTML 文件进行真实预览时，页面仍表现得像在读取完整文件。虽然界面已有“仅显示前 2 MiB”的提示字段，后端实现采用的是先通过 `Read::take` 读取、再对字节数组截断的方式，无法用测试直接证明解码和响应内容从未超过限制。

### 探索问题

原实现最多多读取一个探测字节后再调用 `truncate`，理论上返回内容会受限；但“读取到临时缓冲区后再截断”不够直观，也让后续维护者难以从代码结构确认 I/O 上限。更重要的是，原先没有任何回归测试构造超过上限的文本文件并断言实际返回内容长度。

因此将问题拆成两个需要同时满足的条件：

1. 文件句柄只能读取开头 2 MiB，不能先读取完整内容再裁剪。
2. 返回数据必须标记 `truncated = true`，`totalBytes` 保留原始文件大小，且文本内容长度严格不超过 2 MiB。

### 解决问题

1. 抽取同步的 `read_text_preview_file` 辅助函数，命令仍在阻塞工作线程中调用它。
2. 先读取文件元数据，计算 `min(file_size, 2 MiB)`，只分配这一固定长度的字节缓冲区。
3. 使用 `read_exact` 填满该缓冲区；文件超过限制时仅根据元数据设置 `truncated`，不会额外读取探测字节或进行全量读取。
4. 新增 `text_preview_reads_only_the_first_two_mebibytes` 回归测试：生成 2 MiB + 1 KiB 的 HTML 样本，断言可读、已截断、原始字节数正确，并且返回内容恰好为 2 MiB。

### 验证

1. 新增的定向 Rust 测试通过。
2. `npm run check` 全部通过：TypeScript/Vite 构建、Vitest 3 个测试文件、Rust 格式检查、Clippy 和 42 项 Rust 测试均成功。
3. `git diff --check` 通过。

### 总结问题

1. **资源上限应体现在读取动作本身。** “读取后截断”即使功能结果接近，也不如固定长度读取可审计。
2. **截断功能需要超限样本回归。** 仅测试小文件不能证明读取边界在真实大文件上生效。
3. **元数据与内容职责应分离。** 原始大小用于提示和截断状态，实际缓冲区只承载允许进入预览的数据。

## 49. 2026-07-24：取消文本截断，以虚拟滚动解决大文本布局卡顿

### 发现问题

2 MiB 截断可以限制单次读取量，但不能满足完整查看 6 MiB HTML 等文本文件的需求。另一方面，旧前端即使只拿到截断内容，仍会把每一行都拆分、执行 Prism 高亮并挂载到 DOM；调整预览栏宽度时，大量行会参与重排，造成明显卡顿。

### 探索问题

卡顿的根因不在滚动条本身，而在“文件行数 = DOM 节点数”。项目已经使用 `@tanstack/react-virtual` 驱动工作区网格与列表，因此文本预览可以复用同一虚拟化方案。

虚拟化按物理行工作，压缩 HTML/JSON 常见的超长单行仍会在进入视口时触发 Prism 的同步解析。经确认，文本预览保持源码不换行与横向滚动；当单行 UTF-8 字节长度超过 256 KiB 时，完整显示原文但不执行 Prism 高亮。

### 解决问题

1. 后端删除 2 MiB 上限和 `truncated` 响应字段，完整读取、解码并返回可预览文本；编码识别、二进制拒绝和原始字节数继续保留。
2. 文本预览改为固定 `20px` 行高的虚拟列表。仅视口附近的行创建 DOM，行号由虚拟行索引显式渲染，不再使用完整 `<ol>`。
3. 文本头部与滚动容器分离；文件切换时同时重置纵向和横向滚动位置。
4. 普通可见行继续按语言映射调用 Prism；超过 256 KiB 的单行用 React 文本节点渲染，并显示“该超长行未进行语法高亮”。
5. 虚拟器完成容器测量前渲染少量首批行，避免首次进入预览时出现空白。

### 验证

1. Rust 回归测试构造 6 MiB 文本，确认返回完整内容和正确原始字节数。
2. 组件测试确认 10,000 行文本只渲染少量可见行，且首行行号正确。
3. 组件测试确认超过 256 KiB 的单行内容完整保留、没有 Prism token 节点，并显示降级提示。
4. `npm run check` 通过：Vite/TypeScript、4 个前端测试文件共 5 项测试、Rust 格式检查、Clippy 和 42 项 Rust 测试均成功。

### 总结问题

1. **完整读取与完整挂载是不同问题。** 取消内容截断后，虚拟滚动仍能将布局成本限制在可见行范围内。
2. **物理行虚拟化需要处理压缩文本。** 对超长单行降级高亮能避免让单个 Prism 调用重新引入卡顿。
3. **固定行高是虚拟行定位的前提。** 保持源码不换行与横向滚动，能够使行号、复制语义和滚动高度都稳定可预测。

## 50. 2026-07-24：代码预览缺少配色与可切换 Prism 主题

### 发现问题

文本预览已经会调用 Prism，将源码转换为带有 `token` 类名的标记，但实际界面中的关键字、字符串和注释仍然使用同一种文字颜色，视觉上像是完全没有语法高亮。

### 探索问题

Prism 的解析器只负责生成 token 标记，不携带颜色规则。项目没有引入任何 Prism 主题 CSS，因此 token 虽然存在，也没有对应的颜色样式。

Prism 包已自带多套主题。若把主题 CSS 静态导入，全部规则会在启动时加载，既不能按用户选择切换，也可能让多个主题的全局规则互相覆盖。因此需要让设置项只触发当前主题的样式加载，并在切换时替换旧规则。

### 解决问题

1. 在偏好设置模型中增加 `codeTheme`，默认使用 Tomorrow Night；旧配置通过 serde 默认值平滑迁移，保存前会校验主题标识。
2. 在设置窗口新增“代码配色”下拉选项，提供 Tomorrow Night、默认浅色、Dark、Funky、Okaidia、Twilight、Coy 和 Solarized Light 八个 Prism 内置主题。
3. 文本预览通过 Vite 的 `?raw` 动态导入按需获取所选主题 CSS，并维护唯一的 `<style>` 节点；切换主题时覆盖该节点内容，而非不断追加样式。
4. 将当前主题从应用配置传递到文本预览，使 Prism 输出的 token 立即获得相应配色，同时不改变虚拟滚动、超长单行降级和横向滚动行为。

### 验证

1. 组件测试确认初始主题样式节点被加载，切换主题后仍只有一个样式节点，不会累积重复规则。
2. 生产构建产物包含独立的 Prism 主题代码块，确认主题不会作为常驻静态样式一并加载。
3. `npm run check` 通过：Vite/TypeScript、4 个前端测试文件共 6 项测试、Rust 格式检查、Clippy 和 42 项 Rust 测试均成功。
4. `git diff --check` 通过。

### 总结问题

1. **解析与呈现是两个独立层次。** 存在 token 标记不代表用户能看到语法颜色，解析器之外还需要明确的主题规则。
2. **可切换的第三方样式应集中托管。** 使用单一可替换样式节点能避免动态切换时的规则残留与优先级混乱。
3. **主题设置应持久化并校验。** 默认值负责兼容历史配置，后端校验则防止无效标识写入后造成预览状态不可恢复。

## 51. 2026-07-24：从仅文件工作区转为目录文件管理器

### 发现问题

产品扩展为通用文件管理后，中栏仍只列出当前目录的文件，子文件夹只能在左侧树状导航中逐级点击。一个目录若主要由子文件夹构成，中栏会显得近乎空白，用户看到的工作区与磁盘真实结构脱节；原有复制、剪切、重命名和回收站链路也只接受普通文件。

### 探索问题

现有目录树已经采用惰性读取，当前目录扫描也只读取直接子项，适合作为文件管理器的基础。关键不是递归扫描整个目录，而是把“当前目录的项目”抽象为文件夹与文件的联合列表，并让虚拟列表、选择、操作和预览各自在该模型上处理自己关心的部分。

文件夹缩略图若递归搜集内容或单独持久化拼贴图，会在大目录、网络目录和内容变更时引入明显 I/O 与缓存失效成本。因此采用与资源管理器视觉接近、但范围严格受控的策略：仅在网格可视文件夹中读取直接子媒体，最多四项，并复用已有媒体缩略图缓存与生成队列。

### 解决问题

1. 后端将目录读取响应改为 `DirectoryListing.items`，以 `entryType` 明确区分 `folder` 和 `file`；文件继续保留预览、尺寸、媒体信息和缩略图字段，文件夹提供名称、路径、时间与可回收性。
2. 中栏网格与列表统一显示两类项目，文件夹固定排在前面，搜索与选择覆盖全部项目；双击或 Enter 进入文件夹，选中文件夹时右栏显示基础信息而非尝试加载文件预览。
3. 增加后退、前进、向上一级和可点击面包屑。导航历史仅存在本次运行中，重启仍使用已有的最后目录记录恢复位置。
4. 文件操作底层泛化为项目操作：复制、移动、重命名、系统文件剪贴板和回收站可接收文件夹与文件混合选择；复制或移动目录到自身/子目录会被拒绝，父目录已被选择时自动排除其重复子项。
5. 新增文件夹代表媒体批量接口与网格文件夹拼贴组件。可视卡片一次最多请求四个直接图片或视频文件，缺失缩略图仍沿用现有并发队列生成；无来源、等待或失败时稳定回退为普通文件夹图标，不生成新的合成缩略图缓存。

### 验证

1. Rust 新增混合目录列举测试，确认直接子文件夹和文件同时进入响应。
2. Rust 新增文件夹代表来源测试，确认只返回直接图片/视频文件且最多四项，不会读取嵌套目录或普通文本。
3. `npm run check` 全部通过：TypeScript/Vite 构建、4 个前端测试文件共 6 项测试、Rust 格式检查、Clippy 和 44 项 Rust 测试均成功。
4. `git diff --check` 通过。

### 总结问题

1. **通用文件管理的主视图必须呈现目录结构。** 左侧树适合定位，不应成为进入子目录的唯一通道。
2. **联合项目模型比在文件模型上堆可选字段更清晰。** 文件夹不需要缩略图队列、媒体探测或文本预览，文件也不需要伪造目录操作字段。
3. **文件夹缩略图应以可视性和直接子项为边界。** 复用单文件缓存能得到熟悉的视觉反馈，同时避免递归扫描与额外失效机制。

## 52. 2026-07-24：文件夹代表缩略图的网格填充与双图变形

### 发现问题

文件夹代表缩略图首次接入后，四张来源图片虽然能显示，却只占缩略图区域中的小块位置。修正内部图片尺寸后，四张图能够覆盖区域，但仍保留了过大的封面内边距。移除内边距后又发现：只有两张来源图片时，第一张被拉成长图，视觉比例失真。

### 探索问题

问题不是图片的 `object-fit` 设置，而是三层布局规则相互叠加造成的：

1. 拼贴复用了列表视图的 `compact-thumbnail`，其固定 `42×26px` 尺寸覆盖了网格单元的拉伸行为。
2. 文件夹拼贴容器最初为模拟“封面层叠”保留了内边距和纵向偏移，导致即使图片填满单元，也没有覆盖整个缩略图区域。
3. 为一、两张图片增加的跨行/跨列规则会让首图跨越多个网格单元；两张图片的首图跨两行，正是截图中被拉伸的直接原因。

用户确认需要的是稳定的 2×2 宫格，而非按图片数量改变单张占用面积。

### 解决问题

1. 在文件夹拼贴作用域内覆盖紧凑缩略图固定宽高，使每个子缩略图使用 `width: 100%`、`height: 100%` 并填满所属网格单元。
2. 拼贴容器改为完整占据父缩略图区域：移除额外内边距和下移变换，仅保留 `2px` 格间距；子缩略图不再单独绘制边框和圆角，由文件夹容器统一裁切。
3. 删除一张、两张来源的跨格 CSS 规则。现在无论来源数量为 1、2、3 或 4，均使用固定 2×2 网格，每张图恰好占一个格子，剩余格子保持背景色。

### 验证

1. 四张来源图片能够从缩略图区域边缘铺到边缘，只有网格间距可见。
2. 两张来源图片各占顶行一个单元，第一张不再跨行拉伸。
3. 每次样式修正后均执行 `npm run build` 与 `git diff --check`，均通过。

### 总结问题

1. **组件复用时必须检查尺寸约束。** 列表缩略图的固定尺寸适合紧凑行高，却不适合作为可拉伸网格单元。
2. **代表缩略图应优先保持规则稳定。** 对来源数量做跨格“智能布局”会放大图片比例差异；固定宫格更易预测，也更接近用户对文件夹缩略图的理解。
3. **视觉修复应逐层定位。** 先解决子项尺寸，再消除容器留白，最后清理数量特例，避免把多个问题误归因于同一条样式规则。

## 53. 2026-07-24：文本预览的中英文字体可配置

### 发现问题

文本预览原先在样式表中固定使用 Cascadia Code、Consolas 和通用等宽字体。该顺序无法让用户按自己的阅读习惯选择代码字体，也没有明确指定中文字符的优先回退字体；在不同 Windows 字体环境中，中文正文可能落到不一致的系统回退字体上。

### 探索问题

字体选择只应影响文本预览，不应意外改变文件名、列表或设置界面的排版。浏览器会按 `font-family` 栈逐字符回退，因此可将英文等宽字体放在第一位、中文字体放在第二位，再以通用 `monospace` 作为最终兜底。配置字段还必须带 serde 默认值，保证旧版配置文件没有新增字段时仍能正常启动。

### 解决问题

1. 在偏好设置中增加 `textPreviewLatinFont` 与 `textPreviewCjkFont`，默认分别为 `Consolas` 和 `Microsoft YaHei`（微软雅黑）；后端为字段提供默认值，并在保存前去除首尾空格、拒绝空值。
2. 在设置窗口“外观”区域、代码配色之后新增英文和中文两个字体输入框。保存后沿用原有偏好设置保存链路，重新打开应用时也能恢复所选字体。
3. 将这两个设置从应用根部传到 `TextPreview`，仅在文本预览容器上设置 `"英文", "中文", monospace` 字体栈。英文字形优先使用所选等宽字体，缺失的中文字符再落到所选中文字体，普通界面不受影响。
4. 扩展偏好设置持久化测试，确认自定义的两种字体会以 camelCase 字段写入配置文件；组件测试确认传入的字体组合实际应用到预览容器。

### 验证

1. 选择 `JetBrains Mono` 和 `Microsoft YaHei UI` 后，文本预览容器的字体栈为 `"JetBrains Mono", "Microsoft YaHei UI", monospace`。
2. Rust 配置持久化测试覆盖两个新增字段，旧配置则通过字段默认值自动获得 Consolas 与微软雅黑。
3. 执行 `npm run check`，覆盖 TypeScript/Vite、前端测试、Rust 格式检查、Clippy 与 Rust 测试；随后执行 `git diff --check` 确认没有空白错误。

### 总结问题

1. **中英文混排应显式定义字体回退顺序。** 只设置一款英文字体不能保证中文显示一致，双字段组合既保留了代码等宽感，也使中文阅读结果可控。
2. **偏好项必须兼顾新旧配置。** 新字段的默认值与保存校验分别负责读取兼容和写入有效性，避免升级后因历史配置或空输入导致预览不可用。
3. **样式作用域需要收紧。** 将字体栈放在文本预览根节点，能满足个性化需求而不扰动文件管理器的其他 UI。

### 后续修正：`code` 元素未继承字体栈

首次实现后，预览容器虽然拥有正确的字体栈，但实际源码行由 HTML 原生 `<code>` 元素承载。该元素的浏览器默认等宽字体规则会覆盖继承结果，导致缺失的中文字符再次回退到系统宋体。

修正时为文本预览的代码列和行号列显式设置 `font-family: inherit`，使两者稳定继承预览容器上的“英文、中文、通用等宽”字体栈。这样既不会影响 Prism 的 token 配色，也不会影响其他界面的 `<code>` 元素。

## 54. 2026-07-24：工作区快捷滚动提示层级

### 发现问题

“回到焦点”的悬浮提示会与下方“回到开头”按钮重叠，后者遮住了提示的一部分。

### 解决与验证

为悬浮或键盘聚焦的快捷按钮及其提示伪元素设置更高的 `z-index`，使当前提示绘制在相邻按钮之上。`npm run build` 与 `git diff --check` 均通过。

### 总结问题

相邻的浮动控件共用提示区域时，激活控件需要显式提升层叠顺序。

## 55. 2026-07-24：搜索框迁移至应用顶栏

### 发现问题

工作区工具栏同时容纳导航按钮、搜索框、面包屑、排序和视图切换，搜索框固定占宽后压缩了较长目录路径的可见区域。

### 解决与验证

将“搜索当前文件夹”输入框及其状态回调移到应用顶栏；工作区工具栏移除搜索控件，保留目录导航与面包屑。搜索范围、禁用状态和筛选逻辑不变，面包屑获得中栏可用空间。`npm run check` 与 `git diff --check` 均通过。

### 总结问题

高频全局操作适合放在顶栏，目录上下文信息应优先保留在工作区工具栏中。

## 56. 2026-07-24 至 2026-07-25：音频预览、封面读取、实时频谱与主题切换稳定性

### 发现问题

本轮围绕 v2.0 音频预览完成实现与连续修正，问题并非单一播放器功能，而是覆盖了文件识别、标签封面、网络边界、预览性能、主题样式和设置保存行为：

1. 工作区原本没有完整的音频分类、音频封面缩略图与内嵌预览链路。硬性需求是读取标签内嵌缩略图，并在网格视图显示。
2. MP3 预览出现 `Failed to fetch`。文件信息中的 `640 × 640` 曾被误解为音频分辨率，实际是内嵌封面的尺寸。
3. 文件信息对所有文件套用媒体字段，无法明确区分视频尺寸、音频封面尺寸、图片尺寸和文本信息；同时文件操作和媒体模块的日志不足以定位异步失败。
4. 初版音频预览使用整曲波形，不符合“仅显示当前播放实时跳动波形、进度由独立进度条控制”的交互要求。
5. 80 MiB FLAC 的首播约需 10 秒。虽然本地服务支持 Range 请求，预览仍表现为整文件等待。
6. 亮/暗模式下封面、频谱遮罩、进度条、预览卡片间距与阴影存在可见问题；亮色主题下白色频谱柱尤其缺乏对比度。
7. 第一次应用亮/暗模式后出现短暂停顿，并且工作区选择、预览与加载状态仿佛被整体重置。
8. 用户提供的 `KOTOKO - Floating up.mp3` 没有显示封面，而外置播放器却能显示一张较模糊的图，需要判断封面来源是否为网易云 `Comment` 标签中的 Base64 数据。

### 探索问题

#### 1. 音频文件与封面来源

工作区扫描需要把配置中的音频扩展名识别为 `FileKind::Audio`。网格场景不适合直接读取原始封面：原图可能很大，多个可视卡片同时解码会拖慢滚动。因此后端使用 `lofty` 从标签中优先选择 `CoverFront`，无正面封面时取第一张图片，再生成 320px JPEG 缓存供网格使用。

但预览区与网格有不同目标。预览区显示缓存 JPEG 会丢失原封面的分辨率、格式和细节，不能满足“直接读取原始缩略图”的期望。结论是需要区分两条路径：网格读取派生缓存，预览读取标签原图。

对问题 MP3 的实际检查结果如下：FFprobe 只识别到一条 MP3 音频流；手动枚举 ID3v2.3 帧仅发现 `TSSE`、`COMM`、`TALB`、`TIT2`、`TPE1`、`TPOS`、`TRCK`，没有 `APIC`/`PIC` 等图片帧。`163 key(Don't modify)` 的 Base64 内容解码后为 400 字节私有加密数据，开头不是 JPEG、PNG、GIF 等图片签名。因此该字段不是封面；外置播放器显示的是同目录外置封面或自身/在线缓存，而非 MP3 内嵌图片。

#### 2. MP3 的 `Failed to fetch`

前端运行在 Vite/WebView 端口，而本地媒体服务绑定随机 loopback 端口。WaveSurfer 通过 `fetch()` 读取音频地址时跨越这两个 origin；服务端虽然返回 HTTP 200，但缺失 CORS 响应头，浏览器仍将请求拒绝为 `TypeError: Failed to fetch`。这不是 MP3 解码格式本身失败。

#### 3. 整曲波形与加载性能

最初 WaveSurfer 被用作播放器。检查其 `load(url)` 的实现发现：它会 `fetch` 完整资源为 Blob、转为 `ArrayBuffer`、调用 `decodeAudioData`，并在完成后才发出 `ready`。即使界面已不显示整曲波形，80 MiB FLAC 仍会被完整下载和解码，这正好解释了与文件大小相关的约 10 秒首播等待。

实时频谱的第一版还使用了 128 点 FFT，并线性从 64 个频率桶抽样。高频尾部因分辨率低和线性映射而显得被“截断”。这与音频是否真的缺少高频、以及 FLAC 是否由有损源转制，是不同层次的问题。

#### 4. 主题切换造成工作区重置

主题变量本身仅在应用根节点上更新 CSS 自定义属性。短暂首帧卡顿主要来自整页重绘、缩略图与文字的首次亮/暗绘制缓存建立。真正造成“状态重置”的是设置保存后的无条件工作区重开：`useSettingsController.apply` 无论修改主题、字体还是扩展名，都会调用 `activateWorkspace`。后者会重置元数据、重新扫描目录、清除缩略图显示覆盖，并按工作区记忆重新设置选择项和预览项。

### 解决问题

#### 1. 音频能力与文件信息

1. 增加音频扩展名配置、`FileKind::Audio`、工作区扫描与可见缩略图队列的音频分发；网格缩略图使用标签封面生成的 320px JPEG 缓存。
2. `FileDetails` 改为按文件种类展示字段：视频显示视频尺寸，音频显示时长与内嵌封面尺寸，图片显示图片尺寸，文本显示只读预览模式；公共的名称、大小、日期、类型和路径继续保留。
3. 媒体元数据探测只针对选中的音频/视频运行，避免在每次选择普通文件时触发无关探测。
4. 在文件操作、缩略图、媒体流、转码回退、封面读取与配置保存路径补充细粒度日志，日志包含目标路径、回退状态、耗时、缓存/原图来源和失败原因。

#### 2. 直连播放与格式回退

1. 在 loopback 媒体路由上添加 CORS，允许 WebView 对动态本地端口执行媒体读取。
2. 音频先请求原文件直连流；浏览器无法加载或解码时，再调用 FFmpeg 生成 AAC fMP4 预览流。直连路径保留原始 FLAC/MP3 音质，回退仅用于兼容性，代价是 AAC 192 kbps 与不可 Range 的实时流。
3. 纠正界面术语：音频“分辨率”显示为“内嵌封面尺寸”，不会再把封面尺寸误报为音频属性。

#### 3. 实时频谱与播放控件

1. 音频预览改为封面为主体；鼠标悬停封面或预览区空闲一段时间后显示频谱层。进度控制使用与视频一致的独立 range 进度条，支持拖动、键盘定位、音量和静音。
2. 移除预计算整曲波形。使用同一媒体元素的 `AudioContext + AnalyserNode` 绘制当前帧频谱柱，频谱不表达歌曲整体进度。
3. FFT 调整为 2048 点，展示 30 Hz 至 `min(20 kHz, 采样率 / 2)` 的对数频段；每个可视柱取对应频段的峰值，减少低分辨率和线性映射造成的高频视觉误判。日志记录采样率、FFT 大小和展示范围，便于区分可视化问题、解码回退和源文件高频内容。
4. 实时频谱遮罩固定为深色半透明黑。亮色主题下封面默认仍按原图显示，只有频谱层出现时才加深遮罩，保证白色频谱柱可读。

#### 4. 首播性能优化

1. 移除 WaveSurfer 依赖及其许可证说明。WaveSurfer 不会扩大浏览器原生格式兼容范围，却会为整曲波形强制下载并解码全部音频。
2. 改用原生 `HTMLAudioElement`，设置 `preload="metadata"`，以 `loadedmetadata` 和 `canplay` 驱动“可用”和自动播放状态；浏览器借助已有 Range 服务按需取首段和后续数据。
3. 保留同一元素的实时频谱、进度、音量、静音和 FFmpeg 回退行为。日志分别记录流地址就绪、元数据就绪、首段可播放的耗时，避免再把网络、解码和 UI 等待混为一谈。

#### 5. 原始标签封面与网格缓存分层

1. 新增 `read_audio_embedded_cover` Tauri 命令。它校验音频路径后复用 `lofty` 提取逻辑，将原始 JPEG、PNG、GIF、BMP 或 TIFF 字节以原 MIME 类型编码为 data URL；不缩放、不重编码。
2. `AudioPreview` 优先调用该命令显示原图。若标签无封面、封面格式不支持或读取失败，才回退到已有的网格 JPEG 缓存；缓存也不存在时才请求后台生成。
3. 保留 32 MiB 标签封面安全上限，防止单个恶意标签把 data URL 和渲染内存无限放大。
4. 新增回归断言：含 ID3 APIC GIF 的 MP3 同时验证原始字节与 `data:image/gif;base64,...` 返回值，确保预览不会暗中改写格式。

#### 6. 主题、布局与设置保存

1. 音频预览卡片改为四边一致留白，底部与操作栏之间保留同样间距；外阴影由 `0 12px 32px` 收敛为 `0 3px 10px`，降低溢出感。
2. 进度条和音量条不再仅依赖 WebView 原生 `accent-color`。为音频与视频共用的滑块明确绘制主题化背景轨道、强调色进度、滑块圆点和 Firefox/WebKit 伪元素；亮色模式下不再留下黑色轨道。
3. 设置保存仅在缩略图取帧位置、视频/音频/图片/文本扩展名分组变化时重开当前工作区。主题、强调色、背景、字体、代码配色、音量等纯外观或行为设置只保存配置并保留当前工作区、选择和预览状态。

### 验证

1. 多次执行 `npm run check`：TypeScript/Vite 构建、4 个前端测试文件共 7 项测试、Rust 格式检查、Clippy 与 45 项 Rust 测试均通过。
2. 针对新增原始标签封面路径执行 `cargo test media_processing::tests::embedded_id3_cover_is_read_from_an_audio_tag`，通过 1 项目标回归测试；其余 44 项在完整检查中同时通过。
3. 构建主包在移除 WaveSurfer 后从约 535.7 kB 降至约 494.1 kB。该体积变化不是首播加速的唯一指标，但确认整曲波形库已不再进入运行时包。
4. 直接对 `F:\CloudMusic\KOTOKO - Floating up.mp3` 使用 FFprobe 与 ID3 帧枚举：确认无内嵌图片帧，确认网易云 Comment Base64 不是图片数据。这个文件应当依赖“外置封面图”策略，而不是标签提取策略。

### 总结问题

1. **网格与预览不能共用同一种图像资产策略。** 网格优先低成本派生缩略图，预览优先原始标签封面；两者通过明确的主路径与回退路径协作，而不是互相替代。
2. **播放器库的隐式工作必须与产品交互对齐。** 不显示整曲波形时，仍让波形库完整下载和解码文件会把实现便利转化为用户可感知的等待。原生媒体元素足以承担按需播放，分析器承担实时可视化即可。
3. **“格式兼容”与“首播性能”要分层处理。** 原文件直连先保证音质与速度，FFmpeg 回退再覆盖浏览器无法解码的格式；不能为了少数回退场景让所有音频都先被转码或整曲解码。
4. **主题更新不应触发数据生命周期。** 外观设置只改变 CSS 变量；工作区重开只应由影响目录结果或缩略图语义的配置触发。把两者混在统一保存流程中，会把一次视觉切换放大为数据刷新与选择重置。
5. **第三方播放器显示封面不等于文件携带封面。** 必须检查 `APIC/PIC`、附图流和原始标签帧。私有识别数据、播放器缓存、在线匹配和同目录外置图片都可能制造“文件自带封面”的错觉。
6. **面向问题的日志是本地媒体应用的诊断基础。** 记录请求、元数据、首段缓冲、转码回退、标签封面来源和工作区刷新原因，才能把 CORS、解码、缓存、封面和布局问题快速区分开。

## 57. 2026-07-25：原生视频异常重播与中、右栏预览解耦

### 发现问题

在已确认没有触发转码回退、始终使用 `HTMLVideoElement` 直连原文件流的情况下，视频仍会约每 10 秒从 `0:00` 重新开始播放。此前已为播放器增加“用户手动暂停后拒绝晚到 `canplay`、流地址和回退事件再次 `play()`”的播放意图保护，但该现象仍然存在，因此不能再归因为流地址竞争或 FFmpeg 回退。

用户进一步提出中栏文件列表与右栏预览不应以同一个易变文件对象为边界，而应当能够独立工作。这一要求既是架构改进，也是排查异常重播的关键方向。

### 探索问题

1. 检查工作区轮询后确认：轮询本身只调用 `refreshWorkspace`，不调用播放器的 `play()`、暂停或切换接口。因此它不是直接播放源。
2. 继续检查刷新后的状态更新，发现媒体元数据探测会通过 `setWorkspace` 映射并替换条目对象。文件路径不变，但 `FileEntry` 的对象引用会变。
3. 右栏原先接收中栏派生出的完整 `selectedItem`/`FileEntry`。`PreviewPlayer` 早期又以完整 `video` 对象作为流加载 effect 的依赖；对象引用一变，就会被解释成“切换了视频”。该 effect 会清空流地址、进度和播放状态，重置播放意图，并重新请求原生流，表现即为从头自动播放。
4. 工作区轮询、缩略图结果和元数据补全都可能替换条目对象，因此“约 10 秒”并不是媒体定时器，而是某次异步工作区数据更新恰好暴露的间接触发时机。
5. 修复播放器 effect 仅依赖 `videoPath` 后，视频资源不会因对象引用更新而重建；但右栏入口仍由中栏把完整对象传入。为避免其他预览模块重现同类问题，需要将组件边界进一步改为稳定路径。

### 解决问题

1. `PreviewPlayer` 的缩略图和流加载生命周期均以 `videoPath` 为准，视频时长仅作为初始展示值。同一路径的元数据更新不会重置媒体元素、进度、播放意图或直连流。
2. `PreviewPanel` 不再接收 `selectedItem` 和由其派生的缩略图路径；改为接收 `selectedPath`、工作区 `items` 以及缩略图覆盖表。右栏根据稳定路径解析当前展示条目，缩略图也在右栏按路径决定。
3. 应用根部向右栏传递选择锚点（无锚点时才取当前多选集合的第一项）这一原始值，而不是中栏的 `FileEntry` 对象。中栏刷新后的对象替换只会更新右栏的文件信息显示，不会构成新的预览会话。
4. 音频标签封面读取 effect 的依赖由完整 `file` 对象收敛为 `file.path`。音频原始封面、视频直连流、图片预览和文本预览由同一路径语义管理，避免元数据补全重复发起预览资源读取。
5. 保留已有的播放意图保护：即使真正发生切换，手动暂停也不会被晚到的原文件流、转码流、`canplay` 或旧媒体事件重新播放。

### 验证

1. TypeScript 检查与 Vite 生产构建通过。
2. Vitest 共 4 个测试文件、7 项测试通过。
3. Rust 格式检查、Clippy 和 45 项 Rust 测试通过。
4. 静态检查确认右栏的媒体资源 effect 均以文件路径作为生命周期条件；工作区刷新只会使右栏重新解析同一路径的显示数据，而不再触发视频流重建。

### 总结问题

1. **不可将对象引用变化等同于实体切换。** 工作区列表的数据补全本来就会产生新对象；媒体会话的身份必须使用文件路径等稳定标识，而不是 React props 的对象引用。
2. **轮询常是间接触发器，而非直接根因。** 排查“周期性”问题时，需要从轮询调用链继续追踪它触发的状态写入、组件依赖和副作用，不能因为轮询没有直接调用 `play()` 就停止分析。
3. **中栏与右栏应共享身份，不共享生命周期。** 中栏负责列表与选择，右栏负责预览会话；两者只共享选中文件路径。文件信息可随工作区数据更新，播放器、图片阅读器和文本阅读器则只在路径真正改变时重建。
4. **防竞态与身份建模缺一不可。** 播放意图保护解决“同一会话中的晚到事件”，路径化生命周期解决“错误创建新会话”。仅修其中一层，仍可能出现无操作自动播放或从头重播。

## 58. 2026-07-25：v1.3 PDF 预览、JBIG2 兼容与诊断闭环

### 发现问题

项目原先已完成视频、图片、音频和文本预览，但 `v2.0设计草案.md` 中的 PDF 仍停留在按需接入阶段。实现右栏 PDF 预览后，测试一个来自 Z-Library 的《计算机辅助几何设计的数学方法》扫描版 PDF 时，应用显示为空白页；同时用户截图指出 PDF 工具栏中的页码数值输入框仍保留 WebView 默认样式。随着页面渲染、Worker、密码状态和资源释放的日志增加，原有 1 MiB 单文件轮转上限也不足以保留一次完整诊断过程。

本轮还讨论了目录、全文搜索和文字层。结论是 FileSweeper 的定位是“选中即预览”，而非完整 PDF 阅读器；保留连续滚动、翻页、页码、缩放和外部打开，不增加目录、搜索、文字选择/复制、注释、打印或下载管理。

### 探索问题

#### 1. PDF 预览能力与生命周期边界

1. 先核对正式 v2.0 计划与设计草案：正式计划曾排除 PDF，设计草案则明确指定 `pdfjs-dist`、应用自有工具栏、Vite Worker、当前页及相邻页按需渲染和严格释放生命周期。
2. 检查现有架构后确认：工作区已经使用通用 `FileEntry`，图片、音频和文本已有右栏入口，后端已有受限的本地预览 URL，因而 PDF 不需要新建文件读取服务、转码流程或 Rust sidecar。
3. 实现时将 `.pdf` 固定映射为 `FileKind::Pdf`，优先于可编辑的文本后缀；PDF 不生成网格缩略图，也不受 `.nomedia` 的媒体隐藏规则影响。
4. 右栏采用 PDF.js API 而不是 `viewer.html`：单个预览会话拥有 Worker、加载任务和文档；虚拟列表只挂载可视页与相邻页，渲染队列最多并行两页。切换文件、关闭右栏、工作区失效或文件操作会取消 RenderTask、销毁加载任务、Worker 及其端口。
5. 加密文档只在当前会话展示密码输入并允许重试。密码不进入日志、URL、配置或缓存；取消输入会落入可重试错误态并保留系统打开入口。

#### 2. 空白扫描页不是“大页面加载慢”

1. 对问题文件执行 `pdfinfo`：文件约 7.6 MB、368 页、未加密，页面为 383.811 × 584.06 pt，明显小于 A4，并非超大页面或大文件阻塞。
2. 使用 Poppler 将封面、第 2 页、第 184 页和第 368 页栅格化后均能正常显示，证明源文件本身没有损坏。
3. 使用 PDF.js 读取操作列表时出现 `Ensure that the wasmUrl API parameter is provided`、`JBig2 failed to initialize` 与 `ignoring XObject`。该书是 JBIG2 压缩的扫描图像 PDF，解码器未加载后图像对象被跳过，故画布虽完成渲染流程却看起来是空白。
4. 检查 `pdfjs-dist/wasm` 后确认需要随应用提供 `jbig2.wasm`、`openjpeg.wasm`、`qcms_bg.wasm` 及两个无 WASM 回退脚本；仅打包 `pdf.worker.min.mjs` 不足以覆盖这些图像和色彩解码路径。

#### 3. 页码输入框与日志保留策略

1. 页码框已经有基础颜色和边框，但没有禁用 number input 的原生外观、没有与命令按钮一致的高度和 hover/focus 反馈，也没有清理 WebKit 微调按钮，因此在截图中仍像系统默认控件。
2. 日志插件的 `max_file_size` 位于 Tauri 初始化路径，原值为 `1024 * 1024`。它控制单个轮转文件大小，不等于日志读取快照或“清理旧日志”的保留期限；扩大它不改变日志内容筛选和 30 天旧日志清理策略。

### 解决问题

#### 1. PDF 预览与兼容资源

1. 锁定 `pdfjs-dist@6.1.200`，扩展 TypeScript 与 Rust 的 `FileKind`、工作区扫描、文件信息和右栏路由；新增 `PdfPreview` 与组件测试。
2. 将 PDF.js Worker 与 WASM 解码资源本地打包；`PdfPreview` 使用当前 WebView 地址解析 `./pdfjs/wasm/` 并通过 `getDocument({ url, worker, wasmUrl })` 传入 Worker。Tauri CSP 同时允许 `worker-src 'self' blob:` 和 `connect-src 'self'`，使生产包内 Worker 能读取自身 WASM 资源。
3. 将 JBIG2、OpenJPEG、qcms 的上游 BSD 许可证文本放入 `LICENSES/`，并在第三方许可证说明中记录 PDF.js、WASM 资源、许可证来源和用途。
4. 在 PDF 会话、预览 URL、WASM 地址、Worker 创建、文档加载、密码需求/错误、单页排队/开始/完成/取消/失败、加载任务释放和会话销毁处记录路径、页码、缩放及原因。所有日志刻意不写入密码。

#### 2. 预览 UI 与日志容量

1. 页码输入框改为应用内控件：固定 32px 高度、主题化背景与边框、等宽数字、hover/focus-visible 反馈，显式使用 `appearance: textfield` 并移除 WebKit 微调按钮，视觉上与相邻翻页按钮一致。
2. 将 Tauri 日志单文件轮转上限从 1 MiB 提升至 10 MiB。增大的空间用于保存一次 PDF 解码与跨页渲染过程的细粒度日志，仍由既有的按日期清理机制限制长期占用。
3. 将应用、Tauri 与 Cargo 版本统一升级为 `1.3.0`。

### 验证

1. 对问题扫描 PDF 的 Poppler 栅格化验证首、中、末页均正常，排除源文件损坏和页面尺寸问题；用户在应用中复测 JBIG2 文件后确认预览成功。
2. Vite 生产构建确认 `dist/pdfjs/wasm/` 包含 JBIG2、OpenJPEG、qcms 与回退资源；PDF 组件测试断言 `getDocument` 收到本地 `wasmUrl`。
3. 执行 `npm run check`：TypeScript/Vite 构建、5 个 Vitest 文件共 10 项测试、Rust 格式检查、Clippy 与 47 项 Rust 测试全部通过。
4. 本次页码输入框和日志容量调整后再次执行完整检查，并以 v1.3.0 提交。

### 总结问题

1. **PDF Worker 能启动不代表所有 PDF 都可显示。** 扫描类 PDF 常依赖 JBIG2、JPEG2000 或色彩管理 WASM；只打包主 Worker 会在普通文字 PDF 中掩盖缺失资源问题，必须以真实压缩格式样本验证。
2. **先区分“源文件问题”和“解码链问题”。** 文件大小、页尺寸、加密状态、外部渲染器输出与 PDF.js 警告共同构成证据链，比仅凭空白画面猜测性能问题可靠得多。
3. **预览产品应坚持边界。** 目录、全文搜索和 TextLayer 能力会把轻量右栏预览演变为阅读器；在当前产品目标下，优先保证多格式稳定显示、缩放、翻页和外部打开比扩张文档阅读功能更有价值。
4. **细粒度日志需要匹配轮转容量。** 事件应记录可复现诊断所需的路径、阶段、页码、缩放和错误，不记录密码等敏感内容；同时要给单次复杂预览保留足够连续的日志窗口。
5. **原生输入控件必须显式主题化。** 只设置背景和边框无法覆盖 `number` 的平台外观；外观归一化、微调按钮移除、聚焦可见性和数字排版应作为一个整体处理。

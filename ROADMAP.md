# VideoSweeper 开发路线

> 路线基线：2026-07-16。版本状态严格区分“已编码、已构建、已自动验证、已桌面验收”。

## 路线原则

1. 真实文件系统始终是唯一权威，不引入媒体库、标签、哈希去重或全局搜索。
2. 先建立测试和性能基线，再重构或优化；不以编译成功替代桌面验收。
3. 文件写入保持单文件原子边界，所有 Shell 写操作继续由单一 STA 队列串行执行。
4. 每个版本只解决一个主要风险面，功能扩张不得挤占正确性和发布门槛。

## v0.3：可验证的模块化

目标：保持核心交互语义、补齐工作区状态连续性，并把项目变成可持续迭代、可自动回归的代码库。

- 建立 Rust 单元测试，覆盖配置校验、路径/扩展名归一化、缩略图缓存身份、文件名校验和冲突命名。
- 建立统一质量命令，至少执行 TypeScript/Vite 构建、Rust 格式检查、Clippy 和测试。
- 后端按配置、工作区、缩略图、媒体流、文件操作、Windows Shell、菜单和共享类型拆分。
- 前端拆出共享类型、格式化工具、缩略图、播放器和目录树组件；根组件只负责跨域编排。
- 按工作区持久化排序字段、升降序与最后视频焦点，通过同一偏好开关联动恢复，保证焦点定位使用原排序语义。
- 清理未承载业务的 SQL、Store、Zustand 和无效构建依赖。
- 统一 npm、Cargo 与 Tauri 版本为 `0.3.0`。
- 完成 v0.2 文件监听、框选、菜单和播放切换的真实桌面回归。

验收：`npm run check` 全部通过；拆分前后核心交互和日志语义一致；真实桌面回归表完成。

## v0.4：配置与文件系统正确性

- 已编码并自动验证：后端集中管理配置状态，并将设置与工作区焦点/排序拆成两个原子文件，避免整份配置写入相互覆盖。
- 已编码并自动验证：配置采用真正的原子替换，补充版本迁移、损坏备份和恢复测试。
- 已编码并自动验证：落实 `.nomedia` 祖先目录语义、目录断连、恢复和监听刷新竞态处理。
- 已编码并自动验证：将目录树枚举与工作区视频扫描分离，以后端单目录监听替代 WebView 动态授权。
- 已编码并自动验证：删除当前焦点视频前终止并等待其 FFmpeg 转码进程退出。

验收：自动质量门槛已通过；仍需真实桌面验证异常退出、可移动盘/网络路径断连恢复，以及转码中删除。

## v0.5：Windows 文件任务与剪贴板

- 已编码并自动验证：将 STA 队列升级为带任务 ID、逐项进度、取消和逐文件结果的文件任务引擎，前端提供任务进度卡与取消入口。
- 已编码并自动验证：增加移动任务，处理同路径、同名递增、部分失败和未开始项目取消；移动使用“复制到临时文件、长度校验、无覆盖提交、删除源文件”，删除失败时回滚目标。
- 已编码并自动验证：实现资源管理器兼容的 `CF_HDROP` 与 Preferred DropEffect 宽字符载荷，并在剪贴板占用时进行有限重试。
- 已编码：完成 `Ctrl+C`、`Ctrl+X`、`Ctrl+V` 和工作区右键菜单入口；仍需真实桌面完成与资源管理器的双向互操作矩阵。
- 已编码并完成 Web 布局检查：右栏播放器全屏后显示底部渐变悬浮控制层，包含播放、进度、音量、速率和退出全屏，并在播放时自动隐藏。

验收：自动质量门槛和冲突命名、未开始任务取消、`CF_HDROP` 载荷测试已通过；仍需真实桌面完成复制、剪切、粘贴、取消、资源管理器双向互操作和播放器全屏交互矩阵。任何失败均不得覆盖或损坏源文件。

## v0.6：缩略图缓存闭环

- 已编码并自动验证：实施缓存上限、孤儿清理、损坏索引重建和 LRU 淘汰。
- 已编码并自动验证：将 Base64 全局缓存改为有界 LRU 缓存，不引入 Blob URL。
- 已编码并自动验证：根据 CPU 核数及持续慢任务、失败率调整 sidecar 并发，不再把 10 路视为固定最优值。
- 已编码并完成桌面验收：工作区右下角提供“回到焦点”和“回到开头”悬浮图标；按 Material Design 3 Standard 曲线平滑滚动，并补充页面、弹窗和反馈控件的进入动效。
- 已建立统一性能记录口径并完成一次 5,000 文件扫描基线；参考机 Debug 首次扫描 48.5 ms、立即重复扫描 49.4 ms。该合成基线保留在验收记录中，不作为每次质量检查都创建 5,000 个文件的常驻测试。缩略图冷/热缓存、首屏、滚动与内存回落仍保留真实桌面复测项。

验收：自动质量门槛通过；仍需真实桌面验证缓存上限、长时间浏览后的内存回落，以及 5,000+ 视频目录的稳定响应。

## v0.7：大目录与导航性能

- 已编码并自动验证：目录树与视频扫描保持分离，子目录采用惰性发现，不再为每个子目录额外枚举一次。
- 已编码并自动验证：刷新与目录切换使用后端代次取消，前端请求编号阻止旧扫描或旧元数据结果覆盖当前目录。
- 已编码：原生监听事件在前端 300 ms 合并，并每 30 秒完整枚举校准；不再重复设置后端防抖层。
- 已编码并自动验证：缩略图和元数据 sidecar 使用按 CPU 核数限制的自适应并发池；只为当前可见项入队，不增加无实际多应用场景的优先级、随机代次和运行中取消机制。

验收：自动质量门槛、一次性 5,000 文件扫描基线、扫描取消与旧响应隔离测试已通过；仍需真实桌面复测筛选、排序、滚动、监听刷新和快速目录切换。

## v0.8：播放稳定性与安全

- 已编码并自动验证：应用仅保留一个转码预览进程；响应体断开、选中/定位切换、显式停止和主窗口退出均终止并回收 FFmpeg。
- 已建立原文件、回退转码、快速切换、连续定位、中文/UNC 路径与编码真实桌面矩阵。
- 已编码并自动验证：本地流边界重新校验规范路径、常规文件和已启用扩展名；恢复 CSP，能力缩减为打开目录与写日志，不增加没有使用场景的随机流会话。
- 已编码：转码定位在加载态仍接受每次点击并分别请求对应位置；复用已知时长，使用低延迟编码预设与更短分片，新请求立即替换旧进程，不合并定位。
- 已编码并自动验证：覆盖 HEAD、Range、绝对路径/常规文件/扩展名校验和真实子进程停止回收。

验收：自动质量门槛与可自动化的流安全/生命周期测试已通过；仍需真实桌面执行编码、中文/UNC、连续定位、长时间切换和退出后孤儿进程矩阵。

## v0.9：发布候选

- 改善可访问性、主题化确认对话框、错误详情和诊断导出。
- 持久化窗口安全布局、视图和面板状态，并处理显示器/DPI 变化。
- 验证 NSIS 全新安装、覆盖升级、保留数据和卸载策略。
- 固定 sidecar 来源、版本、哈希和许可证，准备代码签名与发布清单。

验收：干净 Windows 环境和升级环境均通过安装、运行与卸载矩阵。

## v1.0：稳定版

- 冻结功能范围，只处理发布阻断和回归缺陷。
- 自动检查、性能基线、权限/DPI/媒体矩阵和安装矩阵全部通过。
- 无已知数据损坏、源文件覆盖、配置丢失和孤儿进程问题。
- 代码版本、安装包版本、变更记录和用户文档一致。

## 明确不做

- 不新增 FFmpeg FFI 或分发 `libav*` 开发库。
- 不做自定义元数据列、业务 SQLite 索引、全局检索、标签或哈希去重。
- 不用没有真实使用场景的功能扩张替代性能与稳定性工作。

## 2026-07-19：v0.7-v0.8 后续结构收敛（覆盖此前同主题方案）

- 后台媒体 sidecar 改为单一固定许可池，默认容量为 `max(1, floor(CPU 逻辑线程数 × 2 / 3))`，并允许在设置中于 `1..=逻辑线程数` 范围内调整。播放器 FFmpeg、OLE 拖放、剪贴板 STA 与文件操作线程继续独立，不占用该许可池。
- 缩略图逻辑队列改为无上限 FIFO 与路径去重：滚动时仅暂停新增入队，不清空已排队任务；工作区切换后旧任务仍可完成并写入缓存。全局许可池只限制实际后台 sidecar 数量。
- ffprobe 与 ffmpegthumbnailer 均为最多三次总尝试，重试间释放许可；缩略图三次失败后仅执行一次 FFmpeg 回退。元数据探测统一到一条缓存优先流程，首次成功后立即原子持久化。

### 2026-07-19 后续调整

- 缩略图生成的延迟策略已收紧：ffmpegthumbnailer 从最多三次改为仅尝试一次，失败或 sidecar 不可用时立即执行一次 FFmpeg 回退；ffprobe 的三次总尝试保持不变。本节覆盖上方对应旧描述。
- 缓存索引升级为版本化媒体索引，同一源文件指纹下分别保存缩略图与可选元数据；源文件变化同时失效两者，缩略图 LRU 淘汰不删除元数据。旧缩略图索引自动迁移。
- 原生菜单与事件桥已移除，目录、工作区和视频统一使用主题化 WebView 菜单；补齐边缘定位、外部点击、Escape、方向键、Enter 与焦点恢复。
- 外部播放器偏好已删除，仅在内嵌播放器错误时提供使用系统默认应用打开的按钮。拖入、粘贴与“复制到”统一进入文件任务引擎；拖出及系统复制/剪切保留 OLE/STA 适配层。
- 日志面板只读取文件：打开时立即读取，之后每 2.5 秒串行计算最大 1 MiB 快照的 FNV-1a 哈希，内容不变时不回传正文；WebView 实时日志订阅已删除。
- 前端配置不再提供完整兜底值，启动与设置范围以后端 `ApplicationState` 为准。CSS 保持现有视觉并拆为导航、工作区、预览、设置四部分；TS 按导航、工作区、预览、设置/日志拆分。

自动质量门槛已通过；真实桌面仍需完成大队列、sidecar 数量、连续定位、Explorer 双向拖放、菜单键盘操作、DPI/主题和日志轮询矩阵。

# FileSweeper 架构文档

> 最后核对：2026-07-16。本文描述当前代码已经采用的架构；`✅` 表示已在真实桌面环境复验，`🟡` 表示已实现并通过构建、仍待人工验收。

## 1. 系统边界

FileSweeper 是 Windows 本地文件管理器。它只枚举当前工作区的直接子项，不建立全盘媒体库；真实文件系统始终是文件状态的唯一权威，配置和缩略图索引均可重建。

- 平台：Windows、Tauri v2、单主窗口、单实例。
- 数据目录：可执行文件同级 `data/`，包含设置与通用状态 `config.json`、焦点与排序 `workspace-state.json`、`logs/`、`thumbnails/` 和损坏文件备份。
- 媒体工具：用户自行安装 `ffmpeg`、`ffprobe`、`ffmpegthumbnailer`；应用只在约定的本地 `sidecars` 目录中查找并调用它们，不随包分发或下载。
- 外部播放：交由 Windows 默认关联程序，不维护 PotPlayer 路径或按格式分流。

## 2. 实际技术组成

| 层 | 当前实现 | 状态 |
| --- | --- | --- |
| 桌面壳 | Tauri v2 + WebView2，NSIS 当前用户安装 | ✅ |
| 前端 | React 19、TypeScript、Vite；状态集中在 `src/App.tsx` 的 hooks | ✅ |
| UI | 手写 CSS + CSS Variables，主题由 `src/theme.ts` 注入 | ✅ |
| 布局与虚拟化 | `react-resizable-panels`、`@tanstack/react-virtual` | ✅ |
| 后端 | Rust 命令入口 + 配置、领域规则、工作区、缩略图/媒体、文件操作、菜单与 Windows Shell 模块 | ✅ |
| 本地视频流 | `axum` + `tower-http`，仅绑定 `127.0.0.1` | ✅ |
| 持久化 | JSON 配置和缩略图 JSON 索引；未注册 SQL/store 插件 | ✅ |
| 文件监听 | Rust `notify` 非递归监听；WebView 不持有文件系统监听权限 | 🟡 |

未采用 Zustand、shadcn/ui 组件或业务 SQLite Schema。前端状态仍由 React hooks 管理，但共享类型、工具、缩略图、播放器与目录树已从根组件拆出。

## 3. 界面结构

```
左栏：收藏夹 + 懒加载目录树
中栏：搜索、排序、网格/列表、虚拟化缩略图、工作区菜单
右栏：选中视频预览、播放控制、文件信息
```

- 目录树只懒加载直接子目录；收藏夹和最后工作区写入配置。
- 中栏支持名称搜索、按名称/创建时间/大小/时长/分辨率排序、网格与列表切换。配置按工作区保存排序字段、升降序和最后文件焦点，并由同一个行为开关控制保存与恢复。
- 时长与分辨率缺失时，右栏只探测当前选中视频；用户按媒体字段排序时才批量探测。
- 中栏空白处可框选；pointer capture 后靠近整个应用窗口顶部或底部 56px 自动滚动，滚动过程中累计已覆盖的虚拟化项目。
- 中栏使用主题化 WebView 右键菜单：文件项提供默认应用打开、资源管理器定位、复制到、回收站和刷新；列表表头、空白区域和无文件工作区提供打开当前文件夹与刷新。左栏目录保持原生菜单。
- 右栏非交互区域获得焦点后，空格播放/暂停，左右键按 5 秒定位；按钮、输入框和下拉框不被快捷键劫持。

## 4. 核心数据流

### 目录与刷新

`选择目录 → Rust 扫描直接文件 → 前端筛选/排序/虚拟化渲染`

目录树枚举与工作区文件扫描使用独立命令；展开树节点不会读取视频或改变活动监听。成功扫描后，后端只保留当前工作区的一个 `notify` 监听器。监听、拖入复制、重命名和手动刷新都进入带代次编号的 `refreshWorkspace`，旧响应不能覆盖新目录。轻量可访问性探测发现断连后会停止预览、清空列表并保留路径，恢复后重新扫描。重新进入或重启恢复工作区时，前端先恢复该目录的排序；媒体字段探测和响应式网格列数稳定后，再按同一可见顺序定位已记忆的文件焦点。

配置由进程内单一 `ConfigStore` 持有。设置、收藏夹和最后工作区写入 `config.json`；工作区焦点与排序单独写入 `workspace-state.json`，焦点落盘不再具备覆盖设置的物理可能。两个文件都先写各自的唯一临时文件并刷新到磁盘，再由 Windows `MoveFileExW(REPLACE_EXISTING | WRITE_THROUGH)` 原子替换。首次加载旧配置时会把内嵌的 `workspaceFocus` 和 `workspaceSort` 自动迁移到独立文件；两类文件分别进行损坏备份和恢复。

### 缩略图

`可见卡片 → 前端队列（滚动停止 180ms）→ 最多 10 个路径/批 → sidecar 许可池 → JPEG + JSON 索引 → 事件回写`

- 缓存校验包含规范化路径、文件大小、修改时间和取帧位置。
- 优先 `ffmpegthumbnailer`；失败时回退 `ffprobe` + FFmpeg。
- JPEG 读取与 Base64 返回使用独立、最多 4 项的许可池。
- 缓存索引位于 `data/thumbnails/index.json`，使用临时文件加原子替换写入。

### 内嵌播放与回退

`选中视频 → 本地 HTTP 原文件地址 → <video> 直连播放`

仅在原文件 `error`，或 `loadedmetadata` / `canplay` 阶段视频尺寸为 0 时，启动 FFmpeg 输出 H.264/AAC fragmented MP4。转码流定位时，将目标秒数作为输入前 `-ss` 重启单个 FFmpeg；原文件直连继续使用浏览器原生定位。

### 文件操作

复制、移动、重命名和回收站删除走文件操作 STA 线程；系统文件剪贴板使用单独的 STA/OLE 线程，避免与大文件任务互相阻塞。复制与移动任务具有任务 ID、逐文件结果、项目级进度和未开始项目取消；写入目标先使用临时文件和长度校验，再以不覆盖的原子改名提交。移动仅在目标提交成功后删除源文件，删除失败会回滚目标；同名文件递增命名。剪贴板写入使用共同父目录的绝对 PIDL 和各文件的一层相对子 PIDL，通过 `SHCreateDataObject` 创建 Shell `IDataObject`，附加标准 COPY 或 MOVE Preferred DropEffect 后经 `OleSetClipboard` 发布。数据对象由剪贴板 STA 线程持续持有，线程通过 `GetMessage`/`DispatchMessage` 常驻消息循环调度 Explorer 的跨进程 COM 请求；应用运行期间不立即调用 `OleFlushClipboard`，也不在发布后同步打开剪贴板自读。剪贴板读取继续兼容资源管理器的宽字符 `CF_HDROP`。拖出仍使用 Shell Item Array 的 `BHID_DataObject` 与 `DoDragDrop`，仅协商 COPY 效果。

右栏播放器的全屏根节点包含媒体画面和控制条。全屏时控制条改为底部渐变悬浮层，播放期间无指针活动会自动隐藏，移动指针后恢复；普通右栏模式仍使用固定控制条。

普通权限资源管理器无法向管理员权限应用拖入文件，这是 Windows UIPI 限制；应用会检测管理员运行状态并提示。

## 5. 验收状态

| 能力 | 状态 | 证据或剩余工作 |
| --- | --- | --- |
| 目录扫描、虚拟网格/列表、缩略图队列 | 🟡 | 已通过构建；持续回归大目录性能 |
| 原文件优先、FFmpeg 回退与转码定位 | 🟡 | `mp4v` “有音频无画面”回退已实播；仍需覆盖快速切换和定位精度 |
| Windows 文件拖出 | ✅ | 已在资源管理器真实复验 |
| 右栏键盘控制、格式列表管理 | 🟡 | 已通过构建，待交互回归 |
| 全窗口边缘自动滚动框选 | 🟡 | 已实现，待高 DPI、标题栏/侧栏附近验证 |
| 主题化工作区右键菜单 | 🟡 | 已实现，待实际菜单布局与操作验证 |
| 工作区文件监听 | 🟡 | 已启用 `watch` 特性；完整重启后待验证外部新增、删除和重命名 |

## 6. v0.1-MVP 开发路线图

采用**垂直切片**方式推进，每个切片产出可演示成果，未实现部分用占位符填充。

| 切片 | 内容 | 产出 | 状态 |
| :--- | :--- | :--- | :--- |
| 1 | Tauri 脚手架 + 三栏布局壳子 + shadcn/ui 配置 | 可运行的空壳应用 | 🟡 三栏与主题已完成，当前未使用 shadcn/ui 组件 |
| 2 | 文件夹扫描 → 缩略图网格（真实 Rust 数据 + react-virtual） | 看到缩略图网格 | ✅ 真实目录扫描、缩略图缓存、视口生成与虚拟滚动已完成 |
| 3 | 点击选中 → 预览窗原生播放 + 自建播放控件 + 进度条 | 视频可预览 | ✅ 先展示缩略图和信息，再加载本地流播放器 |
| 4 | 目录树导航 + 收藏区 | 文件夹可管理 | ✅ |
| 5 | F2 重命名 + Delete 回收站 + 拖入复制 | 文件可操作 | ✅ 写入操作均经单 STA 队列；复制使用临时文件校验与原子改名 |
| 6 | 文件名搜索 + 排序 + 视图切换（网格/列表） | 基本筛选能力 | ✅ 支持名称、日期、大小、时长、分辨率排序；媒体排序时按需探测 |

### v0.2-增强

| 范围 | 实现状态 | 验收结论 |
| :--- | :--- | :--- |
| 原文件优先预览、FFmpeg 回退与转码定位 | 已实现 | 🟡 `mp4v` 音频可播/无画面自动回退已实播；仍需覆盖快速切换与定位精度 |
| 工作区右键菜单、复制到 | 已实现 | 🟡 中央工作区使用主题化浮层菜单；文件项、表头和空白处均可呼出，待桌面回归 |
| 框选、右栏播放快捷键、格式列表管理 | 已实现 | 🟡 框选可在整个应用窗口上下边缘自动滚动并累计命中路径，待高 DPI 回归 |
| Windows 文件拖出 | 已实现 | ✅ 已在真实资源管理器复验；仅提供 COPY 效果，仍受 UIPI 限制 |
| 文件监听刷新、右键手动刷新 | 已实现 | 🟡 后端 `notify` 单目录监听与 300ms 合并刷新已通过构建；待完整桌面回归 |
| 外部播放 | 已定范围 | ✅ 使用 Windows 默认关联程序；不引入 PotPlayer 路径偏好或格式分流 |

### v0.3-v1.0

后续版本不再把所有完善工作压入单一的 v0.3 或 v1.0。v0.3 先建立自动验证、完成模块拆分并补齐工作区状态连续性；配置与文件系统、Windows 剪贴板、缓存、大目录、播放、发布候选分别进入 v0.4-v0.9，最后由 v1.0 冻结功能并通过发布门槛。

完整范围与验收标准见 [`ROADMAP.md`](ROADMAP.md)。

## 7. 后续架构工作

1. 完成 v0.2 已实现功能的桌面验收，再进入 v0.3 的新增范围。
2. 对每项路线保持“已实现 / 已构建 / 已实测”的独立状态，不以文档计划替代验收结论。

## 8. 2026-07-19 当前媒体后台与前端边界

后续实现覆盖上文关于固定 10 路 sidecar、独立 JPEG 读取许可池、动态并发和原生菜单的历史描述：

- 后台 `ffprobe`、`ffmpegthumbnailer` 与缩略图 FFmpeg 回退共用一个固定许可池；容量由后端配置管理，默认是逻辑线程数的三分之二向下取整且至少为 1。播放器转码、OLE 拖放、剪贴板 STA 和文件操作线程不进入该池。
- 前端只维护无上限 FIFO 逻辑队列和路径集合，发送批次按当时许可池容量切片。滚动仅暂停新的可见项入队；既有待处理项、运行项与工作区切换均不会触发取消。
- `index.json` 现为版本化媒体缓存索引，源文件指纹下的 `thumbnail` 与 `metadata` 相互独立。工作区扫描直接带回缓存元数据，统一探测函数只补齐缺失项。
- 后端是配置真源；WebView 启动前只显示加载壳。前端模块分为导航、工作区、预览、设置/日志，CSS 入口仅组织主题/共享规则与四个区域样式文件。
- 所有上下文菜单均为主题化 WebView 菜单。文件内容变化仍由后端事件和手动刷新驱动，菜单本身不再依赖 Rust 原生事件桥。
- 日志输出保留文件和控制台；面板以哈希轮询文件，不存在 WebView 日志推送列表。

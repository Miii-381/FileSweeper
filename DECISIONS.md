# FileSweeper 已确认设计决策

> 本文补充 `ARCHITECTURE.md`。如有冲突，以本文为准。

## 产品边界与安装

- v0.1 仅支持 Windows 10 1803 及以上、Windows 11 和 x64。
- 应用为单实例、单主窗口；再次启动时激活已有窗口。
- 安装器在目标位置创建 `FileSweeper` 子目录。默认目标为 `%LOCALAPPDATA%\FileSweeper`；用户选择自定义位置时，应用与数据均位于该自定义位置下的 `FileSweeper` 子目录。
- 数据库、缩略图、配置、背景图、备份和日志均位于可执行文件同级的 `data` 目录。安装目录不可写时，应用明确报错并退出，不回退到其他数据目录。
- 升级保留既有 `data`，仅替换应用和 sidecar。卸载删除整个 `FileSweeper` 目录及其中数据。
- 项目不随包分发 FFmpeg、ffprobe 或 ffmpegthumbnailer。用户自行下载并安装兼容版本；应用仅按约定路径调用用户提供的工具。项目自有代码使用 MIT 许可证，用户所选二进制文件继续受其各自许可证约束。
- 应用默认以普通权限运行。Windows 禁止普通资源管理器向管理员完整性级别的窗口发送文件拖放；若检测到应用被管理员终端/IDE 启动，必须提示用户以普通权限重启，不能把该系统拒绝伪装成复制失败。

## 真实文件系统与数据恢复

- 真实文件系统始终是唯一权威；当前不使用业务 SQLite，JSON 配置和缩略图索引均可重建。
- v0.1 仅扫描、展示当前目录的直接文件，不递归包含子目录。
- `config.json` 采用草稿提交模型，只保存设置、收藏夹和最后工作区；工作区焦点与排序保存在独立的 `workspace-state.json`。任一文件解析、版本或字段校验失败时，分别备份损坏副本并恢复，不允许焦点落盘覆盖设置。
- 配置损坏备份保存到 `data/backups`；缩略图索引损坏时可从真实文件和缓存目录重建。
- 缩略图缓存指纹由规范化绝对路径、文件大小和最后修改时间组成。指纹不匹配或无对应文件记录的旧缓存必须删除。

## 导航、工作区与目录规则

- 左栏负责目录导航和收藏夹；中央工作区仅显示文件，不显示文件夹。
- 收藏入口位于顶部工具栏星标。打开文件夹不会自动收藏。
- 首次启动显示可跳过的个性化配置；之后不自动扫描磁盘，等待用户选择工作区。后续启动恢复最后工作区。
- 目录树懒加载直接子目录，按名称排序。重解析点可明确进入，但不自动展开或递归跟随。
- 隐藏和系统项目默认不显示，可在设置中开启。
- `.nomedia` 遵守标准语义：目录树仍显示真实目录；包含 `.nomedia` 的目录及其后代中的媒体默认不纳入列表和缩略图索引。全局开关控制是否显示该类媒体，不提供目录级例外。
- 网络路径、映射盘和可移动存储按普通路径处理。位置断开时停止预览、清空工作区并保留路径；恢复可访问后重新扫描。
- 目录切换事件允许合并，仅保留未开始的最后目标；写入型文件操作严格 FIFO。当前工作区使用非递归文件监听，监听事件和应用自身写入造成的变更均按目录 300ms 合并刷新；刷新不重置搜索、排序、视图或仍然存在的选择。

## 文件列表、选择和排序

- 默认按 Windows 文件创建时间降序排序。
- MVP 必须支持按名称、创建日期、大小、时长等字段排序，且大小必须支持升序和降序。
- 排序字段、升降序与最后文件焦点按工作区记忆，并由“记忆工作区排序与文件焦点”单一开关共同控制；恢复顺序必须是排序在前、焦点定位在后，避免同一路径在不同排序中的位置变化造成错位。
- 搜索仅作用于当前目录的直接文件，使用不区分大小写的文件名子串匹配。
- 范围选择、`Ctrl+A`、键盘导航均基于当前过滤与排序后的可见结果，参照 Windows 资源管理器行为。
- 点击工作区网格/列表的空白区域清空选择与选择锚点；点击文件项、列表列头及其内部控件不得误清空选择。
- 工作区焦点下，空格切换当前焦点项选中；右栏预览/文件信息的非交互区域获得焦点后，空格播放或暂停、左右键按 5 秒定位；按钮、输入框与下拉框保留自身键盘语义。
- 列表视图默认显示缩略图、文件名、大小、时长、分辨率、最后修改时间。文件名列固定为首列且不可隐藏；其他列可增删、重排、调宽并持久化。
- 自定义列仅限既有元数据字段；用户自定义元数据列保留到 v0.2。
- 未完成媒体探测的排序字段，在用户主动切换排序时显示等待窗口并锁定操作；探测完成后统一排序。

## 文件操作

- 文件操作的原子边界为单文件，不承诺整个批次是单一事务。
- 所有用户可见的复制、移动、重命名和回收站删除统一使用 Windows `IFileOperation`，由专用 STA 线程串行执行。
- 复制使用目标目录临时文件、长度校验、最终改名的提交流程。复制失败立即清理临时文件；每次工作区枚举时清理该目录中遗留的应用临时文件。
- 文件任务以单调递增 ID 标识，公开排队、运行、完成、取消状态和逐文件结果；取消只阻止尚未开始的项目，不强行中断正在执行的单文件 Shell 操作。
- 移动复用复制校验与无覆盖提交，目标提交后才永久删除源文件；源删除失败时回滚目标，宁可保留重复文件并报告失败，也不先损坏源文件。
- 系统文件剪贴板写入通过共同父目录绝对 PIDL 和一层相对子 PIDL 调用 `SHCreateDataObject` 创建 Shell `IDataObject`，再由独立 STA/OLE 线程调用 `OleSetClipboard`。数据对象必须保持存活，STA 线程必须运行阻塞式 Windows 消息循环以调度 Explorer 的 COM 请求；Rust channel 只保存任务，发送后通过线程消息唤醒，禁止让 STA 阻塞在 `recv()`。应用运行期间不立即调用 `OleFlushClipboard`，也不在发布返回前使用 `OpenClipboard` 同步回读。Shell 提供宽字符 `CF_HDROP` 和 `CFSTR_SHELLIDLIST`，Preferred DropEffect 严格使用 COPY（`0x1`）或 MOVE（`0x2`）。读取外部剪贴板时缺少 Preferred DropEffect 则默认复制。拖出暂时保留 Shell Item Array 的 `BHID_DataObject`，与剪贴板构造路径分开验证。
- “在资源管理器中显示”使用 `SHOpenFolderAndSelectItems`，不拼接 Explorer `/select,路径` 命令行；后者对带空格路径的非标准引号解析会错误回退到 Documents。
- 拖入仅接受受支持的文件；文件夹、非视频和虚拟文件对象跳过并在结果中说明。
- 拖入复制默认目标为当前工作区目录。同名绝不覆盖，自动使用 `文件名 (1).扩展名` 规则；源与目标为同一路径时跳过。
- 批量或超过 400ms 的单文件操作显示模态进度窗口，并锁定目录导航、选择和文件操作。用户可取消未开始任务，当前单文件操作先安全结束。
- 删除永远尝试移入回收站；回收站失败时保留原文件并报告错误。v0.1 不提供永久删除或应用内 Ctrl+Z。
- `F2` 默认在选中文件的网格卡片或列表名称单元内原位编辑扩展名前的文件主名；Enter 或失焦提交，Escape 取消，非法 Windows 文件名不提交。
- 左栏 v0.1 不响应 `Delete` 或 `F2` 等写入型快捷键。
- 原生拖入基于 Webview `CF_HDROP` 事件并要求 `dragDropEnabled` 在 Webview 创建时启用；配置变更后必须完全重启 Tauri 进程。拖放源和目标须处于相同 Windows 完整性级别。

## 媒体识别、缩略图与预览

- 视频扩展名白名单可在设置中编辑。默认包含常见格式，额外纳入 `.3gp`、`.rm` 和 `.rmvb`；匹配不区分大小写。
- 受支持扩展名的文件即使 ffprobe 或缩略图失败也保留在列表中，以占位状态展示。
- 缩略图 sidecar 使用受控许可池，当前上限为 10；JPEG 缓存读取与 Base64 编码使用独立许可池，当前上限为 4。
- 优先级依次为：当前选中视频元数据、当前视口元数据、当前视口及附近缩略图、其余元数据、其余缩略图。缩略图任务不因切换目录丢弃。
- 缩略图缓存默认上限为 2GB，可在设置中调整；优先清理孤儿缓存，再按最长未访问时间淘汰。
- 内嵌视频流使用 `axum + tower-http::ServeFile`，仅绑定 `127.0.0.1`。前端可传入 URL 编码后的绝对路径；后端校验路径存在、为常规文件且扩展名受支持。
- 视频流必须先通过独立 POC：实际 WebView 播放、Seek、Range 变体、HEAD、中文路径与并发切换全部验收通过后才进入主应用。
- 预览默认音量 100%、自动播放开启、不记忆播放进度；音量和静音状态持久化到配置，均可在设置调整。
- 唯一选中项稳定 250ms 后自动播放；多选时显示最后获得焦点的视频但保持暂停。
- 播放器提供播放/暂停、进度拖拽、时间显示、静音与音量、速率和全屏。内嵌预览失败时，每次均由用户明确选择是否调用系统默认外部播放器。
- 本地流服务无法启动时，仅降级预览，文件浏览、缩略图和管理继续可用。

## 设置、外观、日志与验收

- 设置页使用草稿，点击应用后统一校验、应用运行配置并原子写入 `data/config.json`。关闭设置页或退出应用时，如有未提交改动则要求应用、放弃或继续编辑。工作区排序与焦点共用串行持久化队列并原子写入 `data/workspace-state.json`，与设置文件完全分离。
- 设置包括主题、缩略图缓存上限、自动播放、音量、隐藏项目、`.nomedia`、受管扩展名列表（添加、删除、勾选启用）、播放器偏好、列表列布局、窗口布局和背景。
- 默认深色主题，支持跟随系统、深色、浅色。
- 中央工作区未选择目录时展示类似 VS Code 的欢迎页，并提供打开文件夹入口。
- 背景图铺设于整个 GUI 底层，可设置透明度和自定义图片。自定义背景复制到 `data/backgrounds`，不依赖原始路径。
- 窗口尺寸、位置、面板比例、右栏折叠状态和网格卡片尺寸均需持久化，并在显示环境变化时回退到安全布局。
- 日志写入 `data/logs`；控制台和文件保留全量日志。日志面板默认关闭，仅在打开期间订阅 Webview 日志并刷新；面板默认筛选警告及以上，允许用户临时查看信息和调试级别。错误仍使用模态框或 Toast 提示。
- 前端文件监听仅在后端成功列出用户打开的工作区后获得该目录的最小范围授权；不为监听授予 Webview 全盘文件访问。
- 设置页数据管理区后续展示缓存和日志占用，并提供清理缩略图缓存、重建缓存索引、清理旧日志和打开数据目录。
- 设置页包含关于区域，显示应用与 FFmpeg 版本、许可证入口、数据目录和复制诊断信息。
- 自动化测试覆盖纯 Rust 逻辑；实际 WebView 视频播放作为人工验收。文件原子提交、配置/缓存恢复、缩略图队列、目录断连、选择与预览、Range/Seek 均为发布门槛。

## 2026-07-19 决策修订

以下决策覆盖上文与同一主题冲突的历史设定，但保留历史文字用于追溯：

- 取消后台媒体任务的优先级与自适应调度。缩略图任务采用无上限 FIFO、路径 Set 去重；只限制实际 sidecar 并发，不限制待处理数量，也不取消已排队或已运行任务。
- 全局后台许可池默认容量为 `max(1, floor(CPU 逻辑线程数 × 2 / 3))`，设置范围为 1 到逻辑线程数。降低容量时允许已运行任务自然结束。播放器 FFmpeg、OLE、剪贴板 STA 和文件操作线程保持独立。
- ffprobe 与 ffmpegthumbnailer 的重试预算都是总计三次，每次重新获取许可；缩略图仍失败时 FFmpeg 仅回退一次。播放器定位不使用该重试器，也不合并快速定位请求。

## 2026-07-19：缩略图首选工具改为单次尝试

- 本节覆盖上方“ffmpegthumbnailer 总计三次”的旧决策：ffprobe 仍为最多三次总尝试；ffmpegthumbnailer 仅尝试一次，失败或无法解析 sidecar 时立即执行一次 FFmpeg 回退。
- 原因是缩略图具有数量大、可快速回退的特点。连续三次运行同一个失败工具会占用全局许可并放大整批等待时间，收益低于尽快切换实现。
- 单次尝试和 FFmpeg 回退都继续经过后台全局许可池；播放器 FFmpeg 仍不进入该许可池。
- 删除独立 JPEG 读取许可池。缓存图片读取继续使用阻塞线程，但不占 sidecar 许可。
- 缓存索引同时持久化缩略图和时长/宽高元数据。缩略图淘汰只清除 JPEG 与缩略图字段，元数据保留；源文件指纹变化才同时失效。
- 删除外部播放器偏好。外部打开只是内嵌播放器错误状态下的恢复操作。
- 删除 Rust 原生菜单。目录、工作区和文件操作全部由一个 WebView 菜单组件表达；实际文件写入仍由后端文件任务和 Windows Shell 边界执行。
- 配置默认值、已保存值与并发合法范围全部由后端返回，前端不维护完整配置副本。日志面板也不再订阅 WebView 日志，只按 2.5 秒周期哈希轮询日志文件。
- 不引入 Tailwind；现有语义化 CSS 足以维护。只删除遗留 `--tw-*` 规则并按区域拆分。
- 不引入 `ffmpeg-next`。曾出现的 `src-tauri/target/ffmpeg-next-probe` 属于本地可行性探测构建产物，不是应用依赖，也不进入源码或发布物。

## 2026-07-19：设计指标实施状态审计

> 本节只追加状态，不改写上方历史决策。统计时合并重复描述、拆开可独立验收的复合要求，并排除纯设计理由及已被后续章节覆盖的旧指标。

状态说明：

- ✅ 已完成：实现存在，并有自动检查、静态证据或确定性测试支撑。
- 🟡 部分完成：主要代码已经存在，但仍缺真实桌面、安装器、网络路径或 Windows Shell 矩阵验收，或复合指标只完成了一部分。
- ⬜ 未完成：当前代码中没有对应完整实现。
- ↩ 已覆盖：历史指标已被后续决策替代，不计入当前完成率。

### 汇总

| 领域 | 有效指标 | ✅ 已完成 | 🟡 部分完成 | ⬜ 未完成 |
| --- | ---: | ---: | ---: | ---: |
| 产品边界与安装 | 7 | 5 | 1 | 1 |
| 真实文件系统与恢复 | 5 | 5 | 0 | 0 |
| 导航、工作区与目录 | 8 | 7 | 1 | 0 |
| 文件列表、选择和排序 | 10 | 9 | 0 | 1 |
| 文件操作 | 14 | 11 | 3 | 0 |
| 媒体、缩略图与预览 | 14 | 13 | 1 | 0 |
| 设置、外观、日志与工程约束 | 15 | 9 | 2 | 4 |
| **合计** | **73** | **59** | **8** | **6** |

- 严格完成率：`59 / 73 = 80.8%`。
- 若把“代码已落地、仍待真实环境验收”的部分完成项计入，已进入实施阶段：`67 / 73 = 91.8%`。
- 当前主要缺口集中在卸载时清理 `data`、自定义元数据列、任务取消入口、背景图、窗口/布局持久化、数据管理区和关于区域。

### 产品边界与安装（5 完成 / 1 部分 / 1 未完成）

- 🟡 Windows 10 1803+、Windows 11、x64：当前工程和 CI/开发目标为 Windows x64，但最低系统版本与发行包兼容矩阵仍待安装实测。
- ✅ 单实例、单主窗口、再次启动聚焦已有窗口：已使用 `tauri-plugin-single-instance` 实现。
- ✅ NSIS 默认目录和自定义目录安装到 `FileSweeper` 子目录已经真实安装验证。
- ✅ 数据位于可执行文件同级 `data`，不可写时报错且不切换位置：路径解析和启动失败策略已实现。
- 🟡 卸载删除整个应用目录及 `data`：已加入受管 NSIS 卸载钩子，普通卸载清理安装目录内 `data`、覆盖升级保留数据；仍待生成安装包和占用/权限失败矩阵验收。独立升级逻辑不再列为目标，应用与特定版本 FFmpeg/ffprobe/ffmpegthumbnailer 的命令参数深度绑定，媒体工具与应用只能作为同一版本整体替换。
- ✅ FFmpeg、ffprobe、ffmpegthumbnailer 不通过 `externalBin` 或安装包分发；用户按文档自行安装，不提供应用内下载或 sidecar 独立升级。
- ✅ 管理员权限检测与普通权限提示：已实现，并记录拖放完整性级别限制。

### 真实文件系统与数据恢复（5 完成 / 0 部分）

- ✅ 真实文件系统为唯一权威，不使用业务 SQLite；配置和媒体缓存索引均为可恢复 JSON。
- ✅ 工作区只扫描当前目录直接视频，不递归包含子目录。
- ✅ `config.json` 与 `workspace-state.json` 物理分离、分别校验和原子写入。
- ✅ 配置、工作区状态与媒体缓存索引损坏时备份并恢复或重建。
- ✅ 缩略图和元数据按规范化路径、大小、修改时间指纹校验，源文件变化时共同失效。

### 导航、工作区与目录规则（7 完成 / 1 部分）

- ✅ 左栏目录导航与收藏夹、中央文件工作区的职责已经分离。
- ✅ 顶部星标控制收藏；普通“打开文件夹”不会自动收藏。
- ✅ 后续启动恢复最后工作区已经实现；首次安装后的个性化欢迎/配置页经重新评估没有必要，取消该指标。未选择工作区时仍保留普通欢迎页和打开文件夹入口。
- ✅ 目录树按需读取直接子目录并排序，不递归扫描媒体。
- ✅ 隐藏/系统项目默认隐藏，并可由后端设置控制。
- ✅ `.nomedia` 继承、全局显示开关和目录树保留语义已实现并有测试。
- 🟡 可移动存储已按 Windows 盘符路径真实验证可正常读取；网络/UNC 路径因缺少设备和环境暂时无法验证。
- ✅ 工作区请求代次、300ms 文件事件合并、30 秒校准、写入任务 FIFO 和刷新状态保留已实现。

### 文件列表、选择和排序（9 完成 / 1 未完成）

- ✅ 默认按 Windows 文件创建时间降序。
- ✅ 支持名称、创建日期、大小、时长、分辨率排序及升降序。
- ✅ 排序和最后文件焦点按工作区共同记忆，恢复顺序为先排序后定位。
- ✅ 搜索仅匹配当前工作区直接视频的文件名子串，不区分大小写。
- ✅ 范围选择、`Ctrl+A` 和键盘导航基于过滤、排序后的可见结果。
- ✅ 空白区域清空选择，文件项、列头和内部控件不会误清空。
- ✅ 工作区与预览焦点的空格、左右键语义已实现，交互控件保留自身键盘行为。
- ✅ 列表列的显示、隐藏、重排、调宽和持久化已实现，文件名列固定。
- ↩ 用户自定义元数据列已按 `ROADMAP.md` 的“明确不做”决定取消；当前只允许既有元数据字段。
- ✅ 时长/分辨率排序会先确保媒体信息存在，并在探测期间显示阻塞提示。

### 文件操作（11 完成 / 3 部分 / 0 未完成）

- ✅ 文件任务以单文件为原子边界，不承诺整批事务。
- 🟡 回收和重命名使用 Windows `IFileOperation`，且已真实操作验证成功；复制/移动采用自有临时文件校验提交路线，因此原始“所有操作都统一使用 IFileOperation”仍只算部分完成。
- ✅ 复制使用目标临时文件、长度校验、无覆盖提交，失败和工作区扫描时清理残留。
- ✅ 文件任务具有单调 ID、状态、逐项结果和只取消未开始项目的语义。
- ✅ 移动在目标提交后删除源文件，源删除失败会尝试回滚目标。
- 🟡 系统文件剪贴板在应用存活时已经验证可与 Explorer 正常复制/剪切；应用退出后活数据对象失去引用，剪贴板内容大概率失效，持久化交接尚未完成。
- ✅ “在资源管理器中显示”使用 `SHOpenFolderAndSelectItems`。
- ✅ 拖入任务逐项跳过文件夹、非视频和不支持格式，并在任务结果中说明。
- ✅ 拖入默认复制到当前工作区；同名自动编号，移动到同目录时跳过。
- 🟡 非模态任务卡及对 GUI 的锁定已经真实验证；当前缺少用户可用的取消按钮。取消语义必须保留已成功项目，只阻止未开始项目，不能回滚已完成文件。
- ✅ 删除只进入回收站；失败保留原文件，不提供永久删除或应用内撤销。
- ✅ `F2` 原位编辑主文件名，Enter/失焦提交、Escape 取消，后端校验非法 Windows 文件名。
- ✅ 左栏不响应 `Delete`、`F2` 等写入型快捷键。
- ✅ WebView 原生拖入已真实验证成功，Windows 完整性级别限制与拆分重构前一致。

### 媒体、缩略图与预览（13 完成 / 1 部分）

- ✅ 视频扩展名白名单可编辑，默认格式和大小写归一化已实现。
- ✅ ffprobe 或缩略图失败不会移除受支持视频，UI 使用占位状态。
- ✅ 后台媒体任务采用无上限 FIFO、路径去重和固定全局许可池，无优先级或自适应调度。
- ✅ 工作区切换和滚动不会取消已有缩略图任务；滚动只暂停新增入队。
- ✅ JPEG 读取使用阻塞线程，不占后台 sidecar 许可。
- ✅ 缩略图缓存默认 0.5 GiB、可配置，支持孤儿清理、LRU 淘汰和元数据独立保留。
- ✅ ffprobe 为三次总尝试，重试等待期间释放许可。
- ✅ ffmpegthumbnailer 只尝试一次，失败或不可用时立即执行一次 FFmpeg 回退。
- ✅ 本地流服务仅绑定 `127.0.0.1`，校验规范路径、普通文件和扩展名，支持 HEAD/Range。
- 🟡 流服务自动测试已覆盖 HEAD、Range 和路径；真实 WebView Seek、中文/UNC 路径和并发切换仍待桌面矩阵。
- ✅ 音量、静音和自动播放设置持久化，不记忆播放进度。
- ✅ 唯一选择稳定 250ms 后加载；多选保留焦点预览且抑制自动播放。
- ✅ 播放、定位、时间、音量、速率、全屏和错误态外部打开按钮已实现。
- ✅ 本地流服务启动失败只降级预览，不阻断浏览、缩略图和文件管理。

### 设置、外观、日志与工程约束（9 完成 / 2 部分 / 4 未完成）

- ✅ 设置草稿提交、后端统一校验、`config.json` 原子写入及工作区状态独立串行持久化已实现。
- 🟡 主题、缓存、播放、隐藏项、`.nomedia`、扩展名、并发和列表列已实现；窗口布局和背景设置尚缺。
- ✅ 支持跟随系统、深色、浅色主题。
- ✅ 未选择目录时显示欢迎页和打开文件夹入口。
- 🟡 自定义全局背景图、透明度和 `data/backgrounds` 持久化已实现；仍待真实桌面检查图片格式、替换清理和深浅主题可读性。
- 🟡 窗口尺寸/位置与右栏折叠已通过独立 `window-state.json` 保存并在启动时按显示器工作区夹紧；面板比例持久化仍待多 DPI/显示器桌面验收，网格卡片尺寸暂沿用固定布局。
- ✅ 日志只写控制台和文件；面板按 2.5 秒哈希轮询，支持级别过滤并保持滚动阅读位置。
- ✅ WebView 权限清单保持最小范围，文件监听和文件访问通过后端命令完成。
- 🟡 设置页数据管理区已提供占用、缩略图缓存清空、旧日志清理和数据目录入口；仍待真实目录占用与清理失败矩阵。
- 🟡 关于区域已提供应用/sidecar 版本、许可证入口、数据目录和脱敏诊断导出；仍待发行包许可证目录与诊断内容审查。
- 🟡 自动测试已覆盖 34 项 Rust 逻辑和前端生产构建；真实 WebView、FFmpeg、OLE/STA 和大目录矩阵仍是发布门槛。
- ✅ Rust 原生菜单已删除，目录、工作区、单/多文件操作统一使用主题化 WebView 菜单。
- ✅ 配置值、默认值和并发范围均由后端 `ApplicationState` 提供，前端没有完整 fallback 配置。
- ✅ 未引入 Tailwind，CSS 已按导航、工作区、预览和设置拆分，并清理生成残留。
- ✅ 未引入 `ffmpeg-next`，源码和依赖清单均不存在该依赖。

### 已被覆盖的历史指标

- ↩ “缩略图 sidecar 固定上限 10、JPEG 读取独立许可池上限 4”已由可配置全局后台许可池覆盖。
- ↩ “后台媒体任务按选中项、视口和其余任务设置优先级”已由无优先级 FIFO 覆盖。
- ↩ “缩略图缓存默认 2GB”已由默认 0.5 GiB、设置可调覆盖。
- ↩ “日志面板订阅 WebView 实时日志”已由纯文件哈希轮询覆盖。
- ↩ “ffmpegthumbnailer 三次总尝试”已由单次尝试后立即 FFmpeg 回退覆盖。

### 本轮桌面实测结论与后续处理边界

- NSIS 自定义目录安装已确认可用；卸载后残留运行期 `data` 目录是独立待修问题。本轮只记录，不修改安装器。
- 不再规划 sidecar 独立升级：应用命令行调用与指定版本的 FFmpeg、ffprobe、ffmpegthumbnailer 绑定，发布时应随应用整体升级并共同验证。
- 首次安装欢迎/个性化配置页不再实施；未选择工作区时已有的普通欢迎页足够承担入口职责。
- 可移动存储读取已通过实测；网络共享和 UNC 路径因当前缺少设备与网络环境，状态记为“环境阻塞、待验证”，不作为实现失败。
- 回收站删除、重命名及 WebView 拖入已经通过真实桌面验证。
- 系统剪贴板复制/剪切在应用存活期间可用；应用退出后失效的问题留待后续确认 `IDataObject` 生命周期和 Windows 剪贴板持久化交接策略。
- 非模态任务卡和任务期间 GUI 锁定已经通过验证；实测界面缺少可用的取消入口。后续处理时需要同时核对发行包版本、任务状态切换和按钮可见性；取消只影响尚未开始的项目，已经成功的项目必须保留。
- 本轮不修改业务代码；上述待修项仅进入后续测试与开发清单。
- 🟡 本轮已实现播放器播放意图隔离、展开目录的非递归监听和目录回收站删除；自动与单元检查覆盖关键状态转换，Windows WebView/Explorer 真实矩阵仍是完成门槛。

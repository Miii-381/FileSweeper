# VideoSweeper 已确认设计决策

> 本文补充 `ARCHITECTURE.md`。如有冲突，以本文为准。

## 产品边界与安装

- v0.1 仅支持 Windows 10 1803 及以上、Windows 11 和 x64。
- 应用为单实例、单主窗口；再次启动时激活已有窗口。
- 安装器在目标位置创建 `VideoSweeper` 子目录。默认目标为 `%LOCALAPPDATA%\VideoSweeper`；用户选择自定义位置时，应用与数据均位于该自定义位置下的 `VideoSweeper` 子目录。
- 数据库、缩略图、配置、背景图、备份和日志均位于可执行文件同级的 `data` 目录。安装目录不可写时，应用明确报错并退出，不回退到其他数据目录。
- 升级保留既有 `data`，仅替换应用和 sidecar。卸载删除整个 `VideoSweeper` 目录及其中数据。
- FFmpeg 和 ffprobe 作为 LGPL 构建的 sidecar 随包分发。升级与扩展安装在 v0.1 仅使用命令行。
- 应用默认以普通权限运行。Windows 禁止普通资源管理器向管理员完整性级别的窗口发送文件拖放；若检测到应用被管理员终端/IDE 启动，必须提示用户以普通权限重启，不能把该系统拒绝伪装成复制失败。

## 真实文件系统与数据恢复

- 真实文件系统始终是唯一权威；SQLite 是可重建索引和缓存。
- v0.1 仅扫描、展示当前目录的直接视频文件，不递归包含子目录。
- 数据库损坏时备份损坏副本后重建，并从真实文件系统重新索引。
- `config.json` 采用草稿提交模型。配置解析、版本或字段校验失败时，备份损坏副本并重建默认配置。
- 配置和数据库备份均保存到 `data/backups`。
- 缩略图缓存指纹由规范化绝对路径、文件大小和最后修改时间组成。指纹不匹配或无对应文件记录的旧缓存必须删除。

## 导航、工作区与目录规则

- 左栏负责目录导航和收藏夹；中央工作区仅显示视频文件，不显示文件夹。
- 收藏入口位于顶部工具栏星标。打开文件夹不会自动收藏。
- 首次启动显示可跳过的个性化配置；之后不自动扫描磁盘，等待用户选择工作区。后续启动恢复最后工作区。
- 目录树懒加载直接子目录，按名称排序。重解析点可明确进入，但不自动展开或递归跟随。
- 隐藏和系统项目默认不显示，可在设置中开启。
- `.nomedia` 遵守标准语义：目录树仍显示真实目录；包含 `.nomedia` 的目录及其后代中的媒体默认不纳入列表和缩略图索引。全局开关控制是否显示该类媒体，不提供目录级例外。
- 网络路径、映射盘和可移动存储按普通路径处理。位置断开时停止预览、清空工作区并保留路径；恢复可访问后重新扫描。
- 目录切换事件允许合并，仅保留未开始的最后目标；写入型文件操作严格 FIFO。当前工作区使用非递归文件监听，监听事件和应用自身写入造成的变更均按目录 300ms 合并刷新；刷新不重置搜索、排序、视图或仍然存在的选择。

## 文件列表、选择和排序

- 默认按 Windows 文件创建时间降序排序。
- MVP 必须支持按名称、创建日期、大小、时长等字段排序，且大小必须支持升序和降序。
- 搜索仅作用于当前目录的直接视频文件，使用不区分大小写的文件名子串匹配。
- 范围选择、`Ctrl+A`、键盘导航均基于当前过滤与排序后的可见结果，参照 Windows 资源管理器行为。
- 点击工作区网格/列表的空白区域清空选择与选择锚点；点击文件项、列表列头及其内部控件不得误清空选择。
- 工作区焦点下，空格切换当前焦点项选中；右栏预览/文件信息的非交互区域获得焦点后，空格播放或暂停、左右键按 5 秒定位；按钮、输入框与下拉框保留自身键盘语义。
- 列表视图默认显示缩略图、文件名、大小、时长、分辨率、最后修改时间。文件名列固定为首列且不可隐藏；其他列可增删、重排、调宽并持久化。
- 自定义列仅限既有元数据字段；用户自定义元数据列保留到 v0.2。
- 未完成媒体探测的排序字段，在用户主动切换排序时显示等待窗口并锁定操作；探测完成后统一排序。

## 文件操作

- 文件操作的原子边界为单文件，不承诺整个批次是单一事务。
- 所有用户可见的复制、移动、重命名和回收站删除统一使用 Windows `IFileOperation`，由专用 STA 线程串行执行。
- 复制使用目标目录临时文件、长度校验、最终改名的提交流程。复制失败立即清理临时文件；每次工作区枚举时清理该目录中遗留的应用临时文件。
- 拖入仅接受受支持的视频文件；文件夹、非视频和虚拟文件对象跳过并在结果中说明。
- 拖入复制默认目标为当前工作区目录。同名绝不覆盖，自动使用 `文件名 (1).扩展名` 规则；源与目标为同一路径时跳过。
- 批量或超过 400ms 的单文件操作显示模态进度窗口，并锁定目录导航、选择和文件操作。用户可取消未开始任务，当前单文件操作先安全结束。
- 删除永远尝试移入回收站；回收站失败时保留原文件并报告错误。v0.1 不提供永久删除或应用内 Ctrl+Z。
- `F2` 默认在选中文件的网格卡片或列表名称单元内原位编辑扩展名前的文件主名；Enter 或失焦提交，Escape 取消，非法 Windows 文件名不提交。
- 左栏 v0.1 不响应 `Delete` 或 `F2` 等写入型快捷键。
- 原生拖入基于 Webview `CF_HDROP` 事件并要求 `dragDropEnabled` 在 Webview 创建时启用；配置变更后必须完全重启 Tauri 进程。拖放源和目标须处于相同 Windows 完整性级别。

## 媒体识别、缩略图与预览

- 视频扩展名白名单可在设置中编辑。默认包含常见格式，额外纳入 `.3gp`、`.rm` 和 `.rmvb`；匹配不区分大小写。
- 受支持扩展名的文件即使 ffprobe 或缩略图失败也保留在列表中，以占位状态展示。
- 缩略图 sidecar 使用受控许可池，当前上限为 10；JPEG 缓存读取与 Base64 编码使用独立许可池，当前上限为 4。
- 优先级依次为：当前选中视频元数据、当前视口元数据、当前视口及附近缩略图、其余元数据、其余缩略图。缩略图任务不因切换目录丢弃。
- 缩略图缓存默认上限为 2GB，可在设置中调整；优先清理孤儿缓存，再按最长未访问时间淘汰。
- 内嵌视频流使用 `axum + tower-http::ServeFile`，仅绑定 `127.0.0.1`。前端可传入 URL 编码后的绝对路径；后端校验路径存在、为常规文件且扩展名受支持。
- 视频流必须先通过独立 POC：实际 WebView 播放、Seek、Range 变体、HEAD、中文路径与并发切换全部验收通过后才进入主应用。
- 预览默认音量 100%、自动播放开启、不记忆播放进度；音量和静音状态持久化到配置，均可在设置调整。
- 唯一选中项稳定 250ms 后自动播放；多选时显示最后获得焦点的视频但保持暂停。
- 播放器提供播放/暂停、进度拖拽、时间显示、静音与音量、速率和全屏。内嵌预览失败时，每次均由用户明确选择是否调用系统默认外部播放器。
- 本地流服务无法启动时，仅降级预览，文件浏览、缩略图和管理继续可用。

## 设置、外观、日志与验收

- 设置页使用草稿，点击应用后统一校验、应用运行配置并原子写入 `data/config.json`。关闭设置页或退出应用时，如有未提交改动则要求应用、放弃或继续编辑。
- 设置包括主题、缩略图缓存上限、自动播放、音量、隐藏项目、`.nomedia`、受管扩展名列表（添加、删除、勾选启用）、播放器偏好、列表列布局、窗口布局和背景。
- 默认深色主题，支持跟随系统、深色、浅色。
- 中央工作区未选择目录时展示类似 VS Code 的欢迎页，并提供打开文件夹入口。
- 背景图铺设于整个 GUI 底层，可设置透明度和自定义图片。自定义背景复制到 `data/backgrounds`，不依赖原始路径。
- 窗口尺寸、位置、面板比例、右栏折叠状态和网格卡片尺寸均需持久化，并在显示环境变化时回退到安全布局。
- 日志写入 `data/logs`；控制台和文件保留全量日志。日志面板默认关闭，仅在打开期间订阅 Webview 日志并刷新；面板默认筛选警告及以上，允许用户临时查看信息和调试级别。错误仍使用模态框或 Toast 提示。
- 前端文件监听仅在后端成功列出用户打开的工作区后获得该目录的最小范围授权；不为监听授予 Webview 全盘文件访问。
- 设置页数据管理区展示缓存、数据库、日志占用，并提供清理缩略图缓存、重建视频索引、清理旧日志和打开数据目录。
- 设置页包含关于区域，显示应用与 FFmpeg 版本、许可证入口、数据目录和复制诊断信息。
- 自动化测试覆盖纯 Rust 逻辑；实际 WebView 视频播放作为人工验收。文件原子提交、配置/数据库恢复、缩略图队列、目录断连、选择与预览、Range/Seek 均为发布门槛。
